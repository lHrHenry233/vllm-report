{
  "date": "2026-09-23",
  "repo": "vllm-project/vllm",
  "fetched_at": "2026-09-23T12:59:31.345285+08:00",
  "commits": [
    {
      "sha": "88aa0d287dd3abac89741bbea349350a4d49194e",
      "pr_number": 52988,
      "pr_url": "https://github.com/vllm-project/vllm/pull/52988",
      "pr_title": "[Spec decode] Support variable-length decode for Kimi-K3 adaptive ver",
      "pr_body": "## Summary\r\n\r\nMake Kimi-K3's MLA and KDA decode paths capturable as FULL varlen CUDA graphs so they work under adaptive DSpark verification (#47808), which schedules ragged per-request draft budgets and requires device-sourced query lengths. Before this change K3's `FLASHINFER_MLA` reported `UNIFORM_BATCH`/`UNIFORM`, and the decode builders derived per-request query length from a measured/averaged count, so a graph captured on a uniform dummy did not match ragged replay.\r\n\r\n## Changes\r\n\r\n**FlashInfer MLA** (`flashinfer_mla.py`)\r\n- Promote `_cudagraph_support` to `AttentionCGSupport.ALWAYS` and `query_len_support` to `QueryLenSupport.VARLEN`.\r\n- On the causal spec-decode path, pass `cum_seq_lens_q` + `max_q_len` to `trtllm_batch_decode_with_kv_cache_mla` (flashinfer #3238); assert this path runs on trtllm-gen and does not return LSE (mutually exclusive with DCP).\r\n- Flatten non-causal and DCP decode rows by mapping each row to its request with `searchsorted` on the device query offsets instead of `repeat_interleave(uniform_len)`.\r\n- Carry `query_start_loc_device`, `cum_seq_lens_q`, and `max_query_len` on the decode metadata; set `cum_seq_lens_q` only when `max_query_len > 1`.\r\n\r\n**MLA common** (`mla_attention.py` + subclass builders)\r\n- Thread a promised `max_query_len` through `MLACommonMetadataBuilder._build_decode` (clamped to `reorder_batch_threshold`) and the `flashmla`, `flashattn_mla`, `rocm_aiter_mla`, and `dots3_note` builders.\r\n\r\n**KDA** (`kda_metadata.py`)\r\n- Promote `KimiK3KDAMetadataBuilder` to `AttentionCGSupport.ALWAYS`.\r\n- Route every active decode through the spec path by masking on `is_prefilling` rather than draft count.\r\n- Return `True` from `supports_device_cpu_query_lens_mismatch`.\r\n\r\n**KDA RecoverSSM** (`kda_metadata.py`, opt-in via `--use-replayssm`, off by default)\r\n- Make RecoverSSM co-exist with the adaptive path so it can drop the KDA spec-decode SSM state from `num_spec+1` slots to 1 per request (kernels already in #51855):\r\n  - Restore `self.layer_names` on the builder so RecoverSSM's commit context can resolve the per-layer forward context (the GDN base does not retain it; inert when RecoverSSM is off).\r\n  - Demote spec rows whose `query_len` exceeds `num_spec+1` out of the RecoverSSM path instead of raising. Adaptive's cost-table profiler probes token counts past the cudagraph limit by even-splitting them over the capped request slots, producing eager dummy rows longer than RecoverSSM's `num_spec+1` verify buffers; real captured/serving decode rows are always `<= num_spec+1`, so they are unaffected.\r\n\r\n**Mamba-hybrid model state** (`mamba_hybrid.py`)\r\n- Prefer the batch's promised `max_query_len`, and classify spec-decode rows off `is_prefilling` rather than `num_scheduled_tokens == draft_count + 1`.\r\n\r\n**K3 DSpark draft** (`dspark_mla.py`)\r\n- Build the confidence head when the config enables it, load its weights (skipped only when the draft did not build one), and expose `compute_confidence`.\r\n\r\nRelated Issue: https://github.com/vllm-project/vllm/issues/51867\r\n\r\n## Test plan\r\n- [x] Updated the MLA backend metadata tests for the new `_build_decode`\r\n      signature (`test_mla_backends.py`, `test_rocm_aiter_mla_mtp_split.py`).\r\n- [x] Adaptive DSpark spec decode on Kimi-K3 (TP8, fp8 KV).\r\n- [x] RecoverSSM + adaptive on Kimi-K3: boots and serves through full-graph capture; validated against the base-adaptive checks (accuracy/coherence artifacts were overwritten by later runs and are being regenerated).\r\n\r\n## Validation\r\nKimi-K3 (target) + `Inferact/Kimi-K3-DSpark` (draft), TP8 on 8×B300, FP8 KV cache, `attention_backend=FLASHINFER_MLA`, `num_speculative_tokens=7`, `enable_adaptive_verification=true`. (Numbers below are RecoverSSM off; it is opt-in.)\r\n### Accuracy — lossless vs. no-spec (GSM8K, in-tree runner, 1319 questions, 5-shot, temperature 0)\r\n| max-concurrency | no-spec | adaptive |\r\n|---|---|---|\r\n| 16  | 0.949 | 0.949 |\r\n| 64  | 0.951 | 0.949 |\r\n| 128 | 0.948 | 0.948 |\r\nAdaptive matches no-spec within run-to-run noise (invalid-response rate ~0.1% in every\r\nrun), i.e. adaptive verification is lossless at temperature 0.\r\n### Coherence (MTBench, 80 prompts, temperature 1)\r\nAdaptive: 80/80 successful requests, 0 failed, ~19.5K generated tokens; no repetition or degeneration in the saved completions.\r\n### Speculative-decoding health (speed_bench, temperature 1, c16)\r\nAcceptance rate 28.95%, acceptance length 3.03, per-position acceptance decaying 74.5% (pos 0) → 6.9% (pos 6) — non-zero at every position, confirming the draft + confidence head + varlen verification path is exercised end to end.\r\n### RecoverSSM (opt-in) health\r\nAdaptive + RecoverSSM serves through full-graph capture with healthy acceptance length (2.4–2.9 across c1–c256), confirming the RecoverSSM verify/commit path is exercised end to end; matches base-adaptive accuracy/coherence.\r\n\r\n\r\n## Benchmarking\r\n\r\nPareto sweep of the varlen decode path: 16 configurations × 9 concurrency levels = 144 serving runs. Configurations are fixed draft length K=1..7 plus adaptive, each with and without RecoverSSM.\r\n\r\nKimi-K3 (target) + `Inferact/Kimi-K3-DSpark` (draft), TP8 on 8×B300, FP8 KV cache, `attention_backend=FLASHINFER_MLA`, `draft_sample_method=probabilistic`, `num_speculative_tokens=7` for adaptive. `speed_bench` qualitative, `--speed-bench-output-len 2048`, `--max-model-len 16384`, prompt count scaled with concurrency. Axes: per-user token rate (`1000 / median TPOT`) vs aggregate output throughput.\r\n<img width=\"1870\" height=\"1275\" alt=\"pareto_throughput_vs_tps_per_user\" src=\"https://github.com/user-attachments/assets/6d8b1237-65ee-4ecd-9328-02eaa3c0e457\" />\r\n\r\n\r\nOverlaying both families lets a non-RecoverSSM point appear to overtake a RecoverSSM one it never competes with, so each family is also plotted with its own frontier.\r\n\r\n<img width=\"1870\" height=\"1275\" alt=\"pareto_no_recoverssm\" src=\"https://github.com/user-attachments/assets/715e0e75-479d-42d9-8f60-470d1f351db6\" />\r\n<img width=\"1870\" height=\"1275\" alt=\"pareto_recoverssm\" src=\"https://github.com/user-attachments/assets/583a02b0-644c-4d77-b455-b12ae6e6c9d7\" />\r\n\r\n\r\n### RecoverSSM, adaptive off vs on\r\n\r\n| c | off | on | gain |\r\n|---|---|---|---|\r\n| 64  | 2248.6 | 2349.4 | +4.5%   |\r\n| 128 | 2241.5 | 4454.7 | +98.7%  |\r\n| 256 | 2222.9 | 4887.9 | +119.9% |\r\n"
    },
    {
      "sha": "355d789f0d5b29537b7adc43bd72b7c256c63fd5",
      "pr_number": 58173,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58173",
      "pr_title": "[Compilation] Fix QuTLASS compilation with PyTorch 2.13",
      "pr_body": "## Purpose\r\n\r\n`uv pip install --editable . --torch-backend=auto`\r\n\r\nCurrent builing from source will fail with error\r\n\r\n```bash\r\n      /home/yewentao256/.local/share/uv/python/cpython-3.12-linux-x86_64-gnu/include/python3.12\r\n      -isystem\r\n      /home/yewentao256/.cache/uv/builds-v0/.tmpcPlAW7/lib/python3.12/site-packages/torch/include\r\n      -isystem\r\n      /home/yewentao256/.cache/uv/builds-v0/.tmpcPlAW7/lib/python3.12/site-packages/torch/include/torch/csrc/api/include\r\n      -isystem /usr/local/cuda-13.0/include\r\n      -I/usr/local/cuda-13.0/include -I/usr/local/cuda-13.0/include/cccl\r\n      -O2 -g -DNDEBUG -std=gnu++20 -fPIC -MD -MT\r\n      CMakeFiles/_qutlass_C.dir/.deps/qutlass-src/qutlass/csrc/bindings.cpp.o -MF\r\n      CMakeFiles/_qutlass_C.dir/.deps/qutlass-src/qutlass/csrc/bindings.cpp.o.d\r\n      -o\r\n      CMakeFiles/_qutlass_C.dir/.deps/qutlass-src/qutlass/csrc/bindings.cpp.o -c\r\n      /home/yewentao256/vllm-source/.deps/qutlass-src/qutlass/csrc/bindings.cpp\r\n      In file included from\r\n      /home/yewentao256/vllm-source/.deps/qutlass-src/qutlass/csrc/bindings.cpp:17:\r\n      /home/yewentao256/.cache/uv/builds-v0/.tmpcPlAW7/lib/python3.12/site-packages/torch/include/ATen/ATen.h:40:2:\r\n      error: #error \"This file should not be included when either\r\n      TORCH_STABLE_ONLY or TORCH_TARGET_VERSION is defined.\"\r\n         40 | #error \"This file should not be included when either\r\n      TORCH_STABLE_ONLY or TORCH_TARGET_VERSION is defined.\"\r\n            |  ^~~~~\r\n      In file included from\r\n      /home/yewentao256/vllm-source/.deps/qutlass-src/qutlass/csrc/bindings.cpp:18:\r\n      /home/yewentao256/.cache/uv/builds-v0/.tmpcPlAW7/lib/python3.12/site-packages/torch/include/torch/csrc/api/include/torch/types.h:74:2:\r\n      error: #error \"This file should not be included when either\r\n      TORCH_STABLE_ONLY or TORCH_TARGET_VERSION is defined.\"\r\n```\r\n\r\nQuTLASS uses Aten instead of ABI, this PR fixes the issue\r\n\r\n## Test\r\n\r\nNow it works well\r\n\r\n```bash\r\nUsing Python 3.12.13 environment at: /home/yewentao256/.venv\r\nResolved 196 packages in 22.34s\r\n   Building vllm @ file:///home/yewentao256/vllm-source\r\n      Built vllm @ file:///home/yewentao256/vllm-source\r\nPrepared 1 package in 13m 14s\r\nUninstalled 8 packages in 133ms\r\nInstalled 9 packages in 162ms\r\n - flashinfer-python==0.6.18\r\n + flashinfer-python==0.6.18.post1\r\n - humming-kernels==0.1.12\r\n + humming-kernels==0.1.15\r\n - nvidia-cutlass-dsl==4.6.2\r\n + nvidia-cutlass-dsl==4.7.1\r\n - nvidia-cutlass-dsl-libs-base==4.6.2\r\n + nvidia-cutlass-dsl-libs-base==4.7.1\r\n - nvidia-cutlass-dsl-libs-core==4.6.2\r\n + nvidia-cutlass-dsl-libs-core==4.7.1\r\n - nvidia-cutlass-dsl-libs-cu12==4.6.2\r\n + nvidia-cutlass-dsl-libs-cu12==4.7.1\r\n - nvidia-cutlass-dsl-libs-cu13==4.6.2\r\n + nvidia-cutlass-dsl-libs-cu13==4.7.1\r\n - quack-kernels==0.6.4\r\n + quack-kernels==0.6.5\r\n + vllm==0.29.1rc1.dev497+g8bf051cd5.d20260922 (from file:///home/yewentao256/vllm-source)\r\n\r\n```"
    },
    {
      "sha": "73c59d365b9e1e722fbea5e897e8efb646e3e513",
      "pr_number": 55721,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55721",
      "pr_title": "[XPU] Wire up SYCL apply_rotary_emb kernel in ApplyRotaryEmb",
      "pr_body": "## Purpose\r\n\r\n  `ApplyRotaryEmb.forward_cuda` imports `vllm.vllm_flash_attn.layers.rotary`, a CUDA-only\r\n  module, so there is no `forward_xpu` override and every XPU call falls back to the CustomOp\r\n  default `forward_native` (~8 elementwise kernels). `vllm_xpu_kernels` — already a pinned\r\n  dependency (`requirements/xpu.txt`) — ships a SYCL `apply_rotary_emb` kernel that vLLM never\r\n  calls. This adds an 11-line `forward_xpu` wiring it up, following the same per-backend pattern\r\n  already used by `forward_cuda`/`forward_hip` in this class.\r\n\r\n\r\n  ## Test Plan\r\n\r\n  `pytest tests/kernels/core/test_apply_rotary_emb.py -v` on XPU hardware — 3 new XPU-gated\r\n  cases (text / GPT-J interleaved / partial-rotary) comparing `forward_xpu` against\r\n  `forward_native` bit-exact (`atol=0, rtol=0`).\r\n\r\n  Additional correctness sweep across 12 shapes (vision + text, both RoPE styles, partial\r\n  rotary), calling the real `forward_xpu`/CustomOp dispatch path (not the raw kernel).\r\n\r\n  End-to-end check on GLM-4.1V-9B-Thinking (vision-heavy multimodal, eager mode, 4 images @\r\n  1024px, 5 iterations), comparing wall-clock and output text SHA-256 before/after.\r\n\r\n  ## Test Result\r\n\r\n  `test_apply_rotary_emb.py`: 3 new cases pass; 4 pre-existing CUDA-only dispatch tests correctly\r\n  skip on XPU.\r\n\r\n  12-shape correctness sweep: maxerr 0.0 in all 12 cases.\r\n\r\n  GLM-4.1V-9B-Thinking E2E: median latency 2.5414s -> 2.4794s (1.025x). Output text SHA-256\r\n  identical between unpatched and patched arms across all 5 iterations — no behavior change\r\n  beyond speed. RoPE is a small fraction of this model's device time, so the wall-clock win here\r\n  is modest; the kernel-level win (measured separately, not included in this PR) is larger and\r\n  should generalize better on RoPE-heavier models.\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [x] The test plan, such as providing test command.\r\n- [x] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n\r\n"
    },
    {
      "sha": "94f4170df37fe29d68bd2f7e5a496501b78d55ae",
      "pr_number": 55891,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55891",
      "pr_title": "[Bugfix] Set worker runtime threads before profiling and compilation",
      "pr_body": "- Move `set_torch_threads_for_runtime()` to the end of `Worker.load_model`, before memory profiling and compilation.\r\n- Preserve parallel weight loading while keeping the PyTorch thread count consistent between warmup and serving.\r\n- Add a regression using real `torch.compile` that verifies graph reuse across the runtime transition; `.venv/bin/python -m pytest tests/v1/worker/test_gpu_worker.py::test_load_model_preserves_compiled_graphs_at_runtime -q` fails before the fix and passes afterward.\r\n\r\nThis is an independent compilation candidate: [#49919](https://github.com/vllm-project/vllm/pull/49919) (`7635a9002b`) introduced the post-warmup thread-count transition on August 4, 2026, and was already present in the passing [nightly 12674](https://buildkite.com/vllm/amd-ci/builds/12674#01a07b19-ca8d-499f-a279-e15993ff6da9). Investigation of [build 12711's E2E Scheduling failure](https://buildkite.com/vllm/amd-ci/builds/12711#01a08040-53ed-4d18-9de5-61feb19c4848) led to a separate method-level bisect using historical worker methods, current PyTorch dependencies, and stubbed GPU/model operations. That reproducer shows the thread transition can invalidate compiled graphs under the current runtime; it does not establish when this affected production or explain the September 8 CI failure. Both unmodified nightly endpoints and this patch passed the full scheduling test locally, with the relevant configuration still using 16 graph variants, so this candidate should remain outside the confirmed nightly-regression fixes. \r\n"
    },
    {
      "sha": "e581e14002a0f3688c161fecc0ce36edc6866b69",
      "pr_number": 50922,
      "pr_url": "https://github.com/vllm-project/vllm/pull/50922",
      "pr_title": "[ROCm][CI] Stage G gating",
      "pr_body": "Add 12 AMD mirrors for existing CI test groups.\r\n\r\n### `mi355_2`\r\n\r\n- Attention Benchmark Smoke\r\n- MultiConnector (Nixl+Offloading) PD accuracy\r\n- Qwen3-30B-A3B-FP8-block Sync EPLB Accuracy TP1\r\n- LM Eval Qwen3.5 Models\r\n\r\n### `mi355_4`\r\n\r\n- DeepSeek V2-Lite Sync EPLB Accuracy\r\n- Qwen3-30B-A3B-FP8 DP4 Async EPLB Accuracy\r\n- Rust Frontend Distributed\r\n- Distributed Torchrun + Examples\r\n- EPLB Execution\r\n- Qwen3-30B-A3B-FP8-block Sync EPLB Accuracy TP2\r\n- LM Eval Large Models FP8\r\n\r\n### `mi355_8`\r\n\r\n- Distributed DP + EP"
    },
    {
      "sha": "8d16ca6cc21931481f6c6f9ec0365874a80c6bb2",
      "pr_number": 58107,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58107",
      "pr_title": "[CI][Bugfix] Update IPC test caller for #57312's _apply_entries signature",
      "pr_body": "## Problem\n\nNightly [#90376](https://buildkite.com/vllm/ci/builds/90376) fails `kernels-fp8-moe-test-1xh100` on `test_unquantized_flashinfer_trtllm_cached_weights_need_no_method_state` — the IPC consumer dies with `TypeError: IpcModelLoader._apply_entries() takes 4 positional arguments but 5 were given`.\n\n#57312 changed `_apply_entries` from `(model, entries, aliases, device_index)` to `(model, state: WeightCacheState, device_index)`; this test caller wasn't updated.\n\n## Fix\n\nWrap the entries dict in `WeightCacheState(entries, {}, {})` (empty aliases/attrs, matching the old call's semantics).\n\nSigned-off-by: kimi-no-na-wa <kimi-no-na-wa@users.noreply.github.com>"
    },
    {
      "sha": "0eb42dbbfce96477f4ca174980f4d68336fb1971",
      "pr_number": 58098,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58098",
      "pr_title": "[ROCm][Compile] Support BF16 AsyncTP fusion",
      "pr_body": "- Import `AsyncTPPass` on ROCm and match BF16 opaque GEMM with all-gather or reduce-scatter, preserving weight orientation and existing CUDA patterns.\r\n- Add eight two-GPU numerical cases covering fixed/dynamic shapes, non-square GEMMs, rank-dependent inputs, rewrite counts, and output lifetime.\r\n- Add four bounded Llama configuration cases and a pinned trained Qwen comparison requiring positive rewrites on both workers.\r\n- Add one MI355 native mirror and matching legacy job; keep the eight new numerical cases outside the directory already scheduled by #50519/#56679.\r\n- Force fresh Inductor compilation in the trained test so a warm cache cannot bypass its rewrite witness; support is limited to BF16 with explicit sequence-parallel thresholds.\r\n\r\nMI355 validation reached working symmetric-memory primitives but found that ROCm never imported `AsyncTPPass`. The existing GEMM patterns also missed the opaque `rocm_unquantized_gemm` operations emitted by ROCm linear layers. This patch implements that BF16 path with explicit sequence-parallel thresholds and real numerical and serving checks. \r\n\r\nValidation:\r\n\r\n```bash\r\n.venv/bin/python -m pytest -v -s -x tests/compile/rocm/test_async_tp.py\r\n# 8 passed, 222.91s\r\n.venv/bin/python -m pytest -v -s -x tests/compile/fusions_e2e/test_tp2_rocm_async_tp.py\r\n# 4 passed, 182.65s\r\n.venv/bin/python -m pytest -v -s -x tests/compile/correctness_e2e/test_async_tp.py::test_rocm_async_tp_bf16_output_correctness\r\n# 1 passed, 149.49s; worker rewrite counts [112, 112]\r\n```\r\n\r\n\r\nAI assistance: OpenAI Codex assisted with implementation and validation.\r\n"
    },
    {
      "sha": "f9dce295c96fff3abfbf4d8fb34a34ef8c2c70b6",
      "pr_number": 57451,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57451",
      "pr_title": "[ROCm][DSv4][Perf] Fuse the inverse RoPE into the sparse decode reduce",
      "pr_body": "## Purpose\n\nDeepSeek-V4 shares the ROCm sparse decode kernels with V4.1, so the reduce\nepilogue added in #57435 applies here with no kernel work at all — only the\ncall site has to opt in. On decode-only steps the standalone\n`_inverse_rope_gptj_kernel` stops launching entirely; on V4.1 that measured\n201.6 us per step against 8.3 us added to the reduce, for 193 us net.\n\n#57435 has since merged, so this is no longer stacked: the epilogue, the\n`inverse_rope` switch on `rocm_inv_rope_einsum`, and the\n`rocm_inverse_rope_rows_` helper are all on main. What is left here is 27 lines\nin one file.\n\n## What changes\n\n`_forward_decode` passes the positions and the cos/sin cache into\n`rocm_sparse_attn_decode`, so the rotation happens on the fp32 accumulators in\nthe reduce rather than in a separate pass over the already-rounded bf16 output,\nand `_o_proj` no longer rotates.\n\nWhich rows still owe a rotation is batch-dependent — the reduce rotates the\ndecode rows it produced, prefill rows have none, and a decode path that does\nnot fuse rotates nothing — so `forward_mqa` settles it from the count the\ndecode reports, in the eager attention segment. That placement is the point:\nvLLM compiles with `skip_all_guards_unsafe`, so the same value read from inside\nthe compiled `_o_proj` freezes at its trace-time value. In #57435 that mistake\ncost 0.087 GSM8K against a 0.902 baseline, with fluent output and no error.\n\nOne thing V4 has that V4.1 does not: the FP8 `wo_a` path from #54894, whose\n`inverse_rope_group_quant` rotates while it quantizes. Fusing there would\nrotate twice, so the fusion is gated on `self._wo_a_fp8_weight is None` — which\nis exactly the condition under which `_o_proj` takes the BF16 einsum branch\nthat stops rotating. When the FP8 path is active the decode is handed no\npositions, returns a rotated count of 0, the settle pass is skipped, and the\npath is left byte-identical to today. The gate is a load-time constant, not a\nbatch-dependent one, so reading it here raises none of the tracing concerns\nabove.\n\n## Not a duplicate\n\n- #57435 is the same optimization for `deepseek_v41` and was the parent of this\n  branch; it has merged. The V4.1 call site cannot cover V4: they are separate\n  attention classes in separate files.\n- #57078 tiles the standalone inverse-RoPE kernel for sparse prefill. The two\n  compose rather than collide: this PR removes the pass on decode-only steps\n  and leaves prefill rows going through that same kernel, which #57078 makes\n  faster.\n- #54894 moved `wo_a` to an FP8 output projection in this same `_o_proj` and\n  has landed. The textual conflict predicted here is resolved, and the\n  correctness interaction it created is handled by the gate described above.\n\n## Test plan\n\n- `pytest tests/kernels/attention/test_rocm_triton_attn_dsv4.py`\n- A fused-vs-unfused A/B of the real decode kernels on MI355X.\n- A DeepSeek-V4 serving eval. **Not yet run — this is why the PR is a draft.**\n\n## Test results\n\nUnit tests, on the branch, MI355X:\n\n```\n$ pytest tests/kernels/attention/test_rocm_triton_attn_dsv4.py -q\n74 passed in 65.82s\n```\n\nKernel equivalence, MI355X: the real decode kernels run fused, then unfused\nfollowed by `_fused_inverse_rope_gptj`, on identical inputs, at V4's head dims\n(nope 448, rope 64). NoPE lanes are bit-exact and rope lanes agree to bf16\nrounding at every shape tried — `(queries, heads)` of (2, 3), (1, 128),\n(8, 128), (32, 128):\n\n| | max abs diff |\n|---|---|\n| NoPE lanes | 0.0 (32 queries: 4.9e-4) |\n| rope lanes | <= 2.0e-3, against \\|rope\\| up to 0.29 |\n\nThe kernel-level evidence for the epilogue itself, including the decode trace\nand the GSM8K parity runs, is in #57435, and those kernels are untouched here.\n\n## What is not measured\n\n**No DeepSeek-V4 model has been run end to end on this branch.** The V4.1\nsibling was validated with GSM8K at 1319 questions (0.918 against a 0.901\nbaseline) and a decode trace, but that exercises the V4.1 call site, not this\none. What supports this change is the shared kernels plus reading the V4 path:\n`forward_mqa` is the sole attention entry point for\n`DeepseekV4ROCMAiterMLAAttention` (`forward_mha` is not implemented for these\nmodel classes, so `sparse_mla_force_mqa` does not route around it), decode rows\noccupy the leading rows of the output buffer, and the base class slices\n`o_padded[:, :n_local_heads, :]` before `_o_proj`, which is exactly the region\nrotated here.\n\nThe FP8 `wo_a` gate is likewise unmeasured on a real V4 checkpoint. It is\nargued from the code — no positions in means a rotated count of 0 out, and the\nsettle pass is skipped — rather than from a run on FP8 weights.\n\nThat reasoning is not a substitute for an eval, and the V4.1 experience is the\nreason to say so plainly: the failure mode of getting this wrong is fluent\noutput with wrong answers and no error anywhere. A V4 eval, on both the BF16\nand FP8 `wo_a` paths, should gate this leaving draft.\n\n---\n\nAI assistance was used to write this change. I have reviewed every line, ran\nthe tests above myself, and am responsible for it.\n"
    },
    {
      "sha": "3ee2906f47afdfc1d792e04995c31eb6ecf2bdef",
      "pr_number": 57887,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57887",
      "pr_title": "[EPD] Support metadata-only audio inputs",
      "pr_body": "## Summary\n\nSupport metadata-only audio inputs in Python EPD: the encoder publishes audio token counts or feature lengths, allowing the consumer to reconstruct placeholders without repeating audio preprocessing.\n\nCovers Qwen2-Audio, AudioFlamingo3, Ultravox, Qwen2.5-Omni and Qwen3-Omni using the existing parser and EC metadata interfaces. Pure-audio Chat Completions only; no audiovisual or realtime support added.\n\n## Results\n\nMMAU test-mini, 1,000 samples × 2 passes; Qwen3-Omni-30B-A3B-Instruct, BF16, TP=1, Model Runner V2, Mooncake TCP, 1E+1PD, concurrency 64. Prefix/processor caches enabled, content-derived UUIDs and cache hit rates matched. CUDA graphs enabled.\n\n| Metric | Raw audio forwarding | Metadata-only | Change |\n| --- | ---: | ---: | ---: |\n| Throughput | 62.21 req/s | 73.13 req/s | +17.55% |\n| Mean request latency | 980 ms | 853 ms | -12.98% |\n| Accuracy | 75.90% | 75.65% | -0.25 pp |\n\nNo failed requests. The small accuracy difference remains to be investigated; this is not a claim of exact output equivalence.\n\nGPU E2E passed with Example, Mooncake TCP and NIXL (Ultravox: Example only), including multi-audio/repeated-audio requests and AudioFlamingo3 long-audio chunking.\n\n<details>\n<summary>Validation commands</summary>\n\n```bash\nOMP_NUM_THREADS=4 .venv/bin/python -m pytest --confcutdir=tests/models/multimodal/processing tests/models/multimodal/processing/test_common.py -k test_audio_metadata_only_roundtrip -q\n# 5 passed\nOMP_NUM_THREADS=4 .venv/bin/python -m pytest --confcutdir=tests/multimodal tests/multimodal/test_parse.py -k audio_metadata -q\n# 8 passed\nOMP_NUM_THREADS=4 .venv/bin/python -m pytest --confcutdir=tests/v1/ec_connector/unit tests/v1/ec_connector/unit/test_ec_example_connector.py tests/v1/ec_connector/unit/test_epd_proxy_retry.py tests/v1/ec_connector/unit/test_epd_proxy_round_robin.py -q\n# 75 passed\n```\n\nTwo Qwen3-Omni audio-in-video processor test failures also reproduce on unmodified main; audiovisual support is outside this change.\n\n</details>\n\nOpen-PR searches found no duplicate: #55842 targets the Rust frontend and #57275 raw fallback identity/lifecycle. Related: #52409.\n\nAI assistance was used. Draft pending human review and validation.\n"
    },
    {
      "sha": "826e300cde4c512670bf2905cfd03c87451da36c",
      "pr_number": 58212,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58212",
      "pr_title": "[Bugfix] Skip VllmConfig re-validation for with_hf_config submodel views",
      "pr_body": "`with_hf_config()` rebuilds a `VllmConfig` via `replace()` with a derived `model_config` whose architecture list is empty (e.g. a multimodal model's text sub-config), re-running all of `__post_init__` on it. Any check there that resolves the architecture registry then crashes -- e.g. the NIXL DCP assert reading `use_mla`, which broke PD vision model startup over NIXL (see https://github.com/vllm-project/vllm/pull/58071) -- and platform fix-ups re-mutate nested configs that are shared with the already-validated parent.\r\n\r\nMark the derived model_config with `is_submodel_config` and return early from `__post_init__` for such views. Also guard the `architectures[0]` indexing in `_get_transformers_backend_cls` so `use_mla` stays total over this documented `ModelConfig` state (e.g. the CPU platform reads it from the same `__post_init__`).\r\n\r\nKimi-K3 was used to help with this."
    },
    {
      "sha": "d110c2f19c4f47b92b2dbce2ab24f2014589bf0c",
      "pr_number": 52052,
      "pr_url": "https://github.com/vllm-project/vllm/pull/52052",
      "pr_title": "[ROCm] Use silu_and_mul_with_clamp's torch._C op",
      "pr_body": "This is limited to alpha=1.0 and beta=0.0 as a safety consideration due to MiniMax previously avoiding this kernel in other cases.\r\n\r\nDuring DeepSeekV4, day 0 support, forward_cuda was disabled in favor of forward_native for silu_and_mul_with_clamp. This can be reverted for similar accuracy and around a 6% speedup in cases without speculative decode.\r\n\r\nExample Command:\r\nVLLM_ROCM_USE_AITER=1 vllm serve deepseek-ai/DeepSeek-V4-Flash-0731 \\\r\n    --attention_backend ROCM_AITER_UNIFIED_ATTN \\\r\n    --compilation_config '{\"mode\":3,\"cudagraph_mode\":\"FULL_DECODE_ONLY\"}' \\\r\n    --gpu_memory_utilization 0.9 \\\r\n    --host 0.0.0.0 \\\r\n    --max_model_len 4096 \\\r\n    --max_num_seqs 512 \\\r\n    --no_enable_prefix_caching \\\r\n    --port 39817 \\\r\n    --tensor_parallel_size 4 \\\r\n    --no-enable-log-requests \\\r\n    --dtype auto \\\r\n    --kv-cache-dtype fp8 \\\r\n    --max-num-batched-tokens 8192 \\\r\n    --distributed-executor-backend mp \\\r\n    --trust-remote-code \\\r\n    --tokenizer-mode deepseek_v4 \\\r\n    --reasoning-parser deepseek_v4 \\\r\n    --tool-call-parser deepseek_v4 \\\r\n    --enable-auto-tool-choice\r\n\r\nAccuracy for gsm8k with num_fewshot=8 remained in the 0.94 range.\r\n\r\nPerformance (tok/s before->after) for ISL=512, OSL=1024, temperature=0: concurrency=16: 1762.42->1883.16 (6.85%)\r\nconcurrency=128: 9152.36-> 9602.00 (5%)"
    },
    {
      "sha": "1c0eee919db320ea3e2df37f800465c7842a650a",
      "pr_number": 51915,
      "pr_url": "https://github.com/vllm-project/vllm/pull/51915",
      "pr_title": "[ROCm][Model][Bugfix] Enable GLM-5.2-MXFP4 on the deepseek_v32 path and fix sparse attention correctness",
      "pr_body": "## Purpose\r\n\r\nEnables GLM-5.2 (`GlmMoeDsaForCausalLM`) end-to-end on `vllm/models/deepseek_v32/amd/`\r\nfor gfx942/gfx950. Routing is opt-in via `--model-class-overrides`; the registry entry\r\nis unchanged, so the default path for GLM-5.2 and DeepSeek-V3.2 is untouched. \r\n\r\nThe following issues were also fixed as a result of this overall enablement since it surfaced dormant bugs from a prior deepseek_v32 porting work:\r\n\r\n- **`launch_pdl` forwarded to a Triton runtime that doesn't accept it** — the shared\r\n  `common/kernels.py` passed this CUDA-only kwarg unconditionally; ROCm's Triton rejects\r\n  it by name even when `False`, so the engine died at startup with `KeyError`. Now passed\r\n  only when the platform actually supports PDL.\r\n\r\n- **fp4 bmm invoked with mismatched positional args** — the output tensor landed in the\r\n  kernel's `dtype` slot (`Expected 'Optional[int]' for 'dtype' but found Tensor`). Routed\r\n  through the existing `rocm_aiter_ops.batched_gemm_a16wfp4` wrapper rather than calling\r\n  aiter directly, so the argument contract lives in one place.\r\n\r\n- **fp4 MLA bmm silently wrong for non-power-of-two K** — `batched_gemm_a16wfp4` only\r\n  supports power-of-two K, and returns wrong values without erroring otherwise. GLM-5.2's\r\n  `qk_nope_head_dim=192` lands in the unsupported set; DeepSeek-V3.2's 128 does not.\r\n  Guarded on both `qk_nope_head_dim` and `kv_lora_rank` to fall back to the fp8 bmm, with\r\n  a warning. `W_K`/`W_K_scale` are also made contiguous after the transpose, which\r\n  otherwise produced NaN.\r\n\r\n- **Indexer-K cache written in the plain layout while its reader expects the shuffled\r\n  one** — affects DeepSeek-V3.2 identically, not just GLM-5.2. The AMD path writes this\r\n  cache from the shared `fused_norm_rope`, which only implemented\r\n  `[n_blocks, blk, head_dim]`, while the reader selects `SHUFFLE` above block size 1: the\r\n  write kept storing row-major, but the reader expected shuffled, so the indexer scored\r\n  garbage K and output degenerated once a sequence exceeded `index_topk=2048`, with no\r\n  error raised. Refactored so the shared kernel stays platform-agnostic — it writes\r\n  whichever layout the cache reports via a new `uses_shuffled_layout` property — and the\r\n  AMD-specific shuffled write (`[n_blocks, blk/16, head_dim/16, 16, 16]`, used above block\r\n  size 1) lives in the AMD backend. The base class returns `False`, so CUDA is unchanged.\r\n\r\n- **Sparse-MLA block sizes narrowed instead of extended** — the ported backends replaced\r\n  the inherited `get_supported_kernel_block_sizes()` with `[16, 32]`, which made\r\n  `select_common_block_size` silently downgrade a requested 64 to 32 via its\r\n  largest-divisor fallback. Now concatenated with the base list.\r\n\r\n- **Kernel tests gated on `is_cuda()` and running against dims that matched neither\r\n  model** — the tests were skipped entirely on CDNA. The gate is now `supports_fp8()`, so\r\n  the suite runs on gfx950. The hardcoded attention dims were written as if one set served\r\n  both GLM-5.2 and DeepSeek-V3.2, which it did not; they are now taken from each model's\r\n  published config and parametrized over both.\r\n\r\n## Test Plan\r\n\r\nServer:\r\n\r\nVLLM_ROCM_USE_AITER=1 \\\r\nHF_HUB_OFFLINE=1 SAFETENSORS_FAST_GPU=1 \\\r\nvllm serve /path-to-your/GLM-5.2-MXFP4 \\\r\n  --model-class-overrides '{\"GlmMoeDsaForCausalLM\": \"vllm.models.deepseek_v32:DeepseekV32ForCausalLM\"}' \\\r\n  --tensor-parallel-size 8 \\\r\n  --kv-cache-dtype auto \\\r\n  --max-model-len 65536 \\\r\n  --no-enable-prefix-caching \\\r\n  --trust-remote-code \\\r\n  --port 8000\r\n\r\nVariants: --kv-cache-dtype fp8 for the fp8 KV row; --block-size {16,64} for the\r\nblock-size matrix. Drop --model-class-overrides to run the legacy deepseek_v2 path\r\nfor comparison.\r\n\r\nAccuracy:\r\n\r\nlm_eval --model local-completions \\\r\n  --model_args \"model=/models/GLM-5.2-MXFP4,base_url=http://localhost:8000/v1/completions,num_concurrent=32,tokenized_requests=False,max_retries=3,timeout=3600\" \\\r\n  --tasks gsm8k --num_fewshot 5 --gen_kwargs max_gen_toks=2048 --limit 200\r\n\r\nThroughput:\r\n\r\nvllm bench serve --backend vllm --model /shareddata/models/GLM-5.2-MXFP4 \\\r\n  --dataset-name random --random-input-len 4096 --random-output-len 1024 \\\r\n  --num-prompts 200 --max-concurrency 32 --ignore-eos --port 8000 \\\r\n  --save-result --result-filename glm52_v32amd_bench.json\r\n\r\nKernel unit tests:\r\n\r\npytest tests/kernels/test_fused_deepseek_v32_norm_rope.py -v\r\n\r\n## Test Result\r\n\r\n**gsm8k** — GLM-5.2 200 samples, 5-shot, `exact_match`, `deepseek_v32` AMD path\r\n\r\n| KV cache    | Before (flexible / strict) | After (flexible / strict)    |\r\n|-------------|----------------------------|------------------------------|\r\n| auto (bf16) | 0.0000 / 0.0000            | **0.9400 / 0.9400** ± 0.0168 |\r\n| fp8             | 0.0000 / 0.0000 \\*         | **0.9350 / 0.9350** ± 0.0175 |\r\n\r\n\\* Not separately measured. fp8 KV goes through the same absorbed-weight bmm where the\r\nbug was, so it fails identically; only the bf16 run was evaluated pre-fix."
    },
    {
      "sha": "9646f5306475d95de6baf03a81da4203f30928a8",
      "pr_number": 58153,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58153",
      "pr_title": "[Bugfix][ROCm] Use the platform FP8 range in the concat MLA q test",
      "pr_body": "## Purpose\r\n\r\nThree tests in `tests/kernels/test_concat_mla_q.py` fail on MI300X (gfx942):\r\n\r\n```\r\nFAILED test_concat_mla_q_fp8_nope_and_rope[False-0-False]\r\nFAILED test_concat_mla_q_fp8_nope_and_rope[False-0-True]\r\nFAILED test_concat_mla_q_fp8_nope_and_rope[False-64-False]\r\n\r\nAssertionError: Tensor-likes are not equal!\r\nMismatched elements: 40769 / 69632 (58.5%)\r\nGreatest absolute difference: 224.0\r\nGreatest relative difference: 0.5\r\n```\r\n\r\nFailing run:\r\n<https://buildkite.com/vllm/amd-ci/builds/13378/list?jid=01a0c82f-66da-473c-866b-5cd9f6be4bcf&tab=output>\r\n\r\nThe job is the `(MI300) Core Operation Kernels` mirror added by #50519, which is\r\nwhere this file starts running on gfx942. #58136 fixes the other three failures\r\nin the same file, the `enabled=True` `IndexError`; both are needed for the file\r\nto pass on MI300.\r\n\r\nThe expected value hardcodes CUDA's FP8 format, `clamp(-448, 448)` and\r\n`torch.float8_e4m3fn`. `ROCmPlatform.is_fp8_fnuz()` is true for gfx94x, so on\r\nMI300 the op produces `torch.float8_e4m3fnuz`, and vLLM deliberately clamps fnuz\r\nat ±224 rather than the ±240 `torch.finfo` reports, see `get_fp8_min_max()`. 224\r\nis bit pattern `0x7E`, the same mantissa cap `e4m3fn` uses for 448 but one\r\nexponent lower, which keeps the fnuz grid an exact power-of-two rescaling of the\r\nOCP one. The test expects 448 where the op saturates at 224, hence the 0.5\r\nrelative difference.\r\n\r\nUse `get_fp8_min_max()` and `current_platform.fp8_dtype()`, the same helpers the\r\nop itself uses, which is also what `tests/kernels/quant_utils.py` already does\r\nfor its reference implementations. On CUDA and on gfx950 these return exactly\r\n`torch.float8_e4m3fn` and ±448, so the expected value is bit-identical to\r\nbefore.\r\n\r\nAlso assert the produced dtype. Both sides are compared as float, so a wrong\r\noutput dtype would otherwise only surface as a confusing value mismatch.\r\n\r\n## Test Plan\r\n\r\n`pytest tests/kernels/test_concat_mla_q.py` on MI300X (gfx942) and on MI355X\r\n(gfx950).\r\n\r\n## Test Result\r\n\r\nMI300X before: 6 failed, 32 passed. After: 3 failed, 35 passed, the remaining\r\nthree being the #58136 `IndexError`.\r\n\r\nWith #58136 applied as well the file is 38 passed, and the full\r\n`(MI300) Core Operation Kernels` command is green locally: 3253 passed,\r\n526 skipped.\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "111d7d7be2d4cfa439ac0e0cbd2b14a474bfab6e",
      "pr_number": 58006,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58006",
      "pr_title": "[ROCm][Build][The Rock] Bump Triton version to 3.8.x tip-of-tree with source build in The Rock image",
      "pr_body": "\r\n## Purpose\r\nThis PR bumps Triton version to 3.8.x tip-of-tree (last updated Sep 9, 2026) to coincide with coming bump in Dockerfile.rocm_base.  Have currently built this locally to verify.\r\n\r\nThis should fix the remaining Quantization Kernels test group that is recurring in the nightly CI runs.\r\n## Test Plan\r\nLocal build using `--no-cache`.\r\n## Test Result\r\nBuild succeeded.\r\n\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [X ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [X ] The test plan, such as providing test command.\r\n- [X] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "d421a2e8fc0fd17c46c131d5c0884af5cf075fca",
      "pr_number": 57396,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57396",
      "pr_title": "[Perf] Remove CPU-GPU sync in heterogeneous vocabulary speculative decoding",
      "pr_body": "## Purpose\r\n\r\nRemove a CPU-GPU synchronization between target and draft model execution phases during speculative decoding. This occurs specifically when heterogeneous vocabulary is used.\r\n\r\nThe CPU-GPU synchronization point prevents the engine from enqueuing more draft-model CUDA kernels asynchronously during the target model's execution. So the CPU kernel launch rate becomes a bottleneck given a sufficiently small draft model decoding time.\r\n\r\n**NSYS profiling**. Notice the async compute and GPU activity drops to ~0% between target and draft model execution for a decode step.\r\n<img width=\"754\" height=\"138\" alt=\"image\" src=\"https://github.com/user-attachments/assets/204f0afa-c160-4444-8c52-ac7614a501cb\" />\r\n\r\n## Test Plan\r\n\r\n**Correctness test**. These new unit tests ensure that the output of the vocabulary mapping functions return the correct token ids.\r\n```\r\npytest tests/v1/spec_decode/test_vocab_mapping.py -k \"test_map_target_to_draft_ids or test_map_draft_to_target_ids\"\r\n```\r\n\r\n**Performance test**. Run the following script for both this branch and main to compare the performance difference. Draft tokens are all synthetically rejected to avoid non-deterministic behaviour.\r\n```\r\nvllm bench throughput \\\r\n    --model <target_model> \\\r\n\t--speculative-config '{\r\n\t\t\"method\": \"draft_model\",\r\n\t\t\"model\": \"<draft_model>\",\r\n\t\t\"num_speculative_tokens\": 8,\r\n\t\t\"rejection_sample_method\": \"synthetic\",\r\n\t\t\"synthetic_acceptance_length\": 1.0,\r\n\t\t\"parallel_drafting\": <true | false>,\r\n\t\t\"use_heterogeneous_vocab\": true\r\n\t}' \\\r\n\t--num-warmups 1 \\\r\n\t--max-num-seqs 1 \\\r\n\t--random-input-len 128 \\\r\n\t--random-output-len 1024 \\\r\n\t--max-model-len 4096 \\\r\n\t--num-prompts 1\r\n```\r\n\r\n## Test Result\r\n\r\n### Correctness test\r\nThe unit tests passed.\r\n\r\n### Performance test\r\n\r\nThroughput was evaluated on a RTX 6000 Ada Generation GPU and Intel Xeon E5-2640v3 CPU.\r\n\r\n#### Vanilla speculative decoding\r\n\r\n(batch size: 1, speculative tokens: 4, parallel drafting: false, 'use_heterogeneous_vocab': true)\r\n\r\ntarget model | draft model | old decode throughput | new decode throughput | speedup\r\n-- | -- | -- | -- | --\r\nQwen/Qwen3-1.7B | Qwen/Qwen3-0.6B | 21.33 tps | 25.74 tps | +20.7%\r\nQwen/Qwen3-4B | Qwen/Qwen3-0.6B | 24.08 tps | 25.03 tps | +3.9%\r\nQwen/Qwen3-8B | Qwen/Qwen3-0.6B | 18.92 tps | 26.01 tps | +37.5%\r\nQwen/Qwen3-14B | Qwen/Qwen3-0.6B | 15.47 tps | 26.15 tps | +69.0%\r\n\r\n#### Parallel Draft (PARD) speculative decoding\r\n\r\n(batch size: 1, speculative tokens: 8, parallel drafting: true, 'use_heterogeneous_vocab': true)\r\n\r\ntarget model | draft model | old decode throughput | new decode throughput | speedup\r\n-- | -- | -- | -- | --\r\nQwen/Qwen2.5-3B | amd/PARD-Qwen2.5-0.5B | 39.93 tps | 57.02 tps | +42.8%\r\nQwen/Qwen2.5-7B | amd/PARD-Qwen2.5-0.5B | 30.50 tps | 52.31 tps | +71.5%\r\nQwen/Qwen2.5-14B | amd/PARD-Qwen2.5-0.5B | 20.60 tps | 28.78 tps | +39.7%\r\n\r\n### NSYS Profiling\r\n\r\nNotice the removal of this synchronization point improves the GPU and async compute activity, and the throughput for the draft model.\r\n\r\n<img width=\"775\" height=\"138\" alt=\"image\" src=\"https://github.com/user-attachments/assets/9fda3050-6bf7-42bb-a806-1cb02e9fda36\" />\r\n"
    },
    {
      "sha": "4edb55169f69c76bb87ab9f154585917cb01bb17",
      "pr_number": 58193,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58193",
      "pr_title": "[CI] Shard (H200 MIG 18GB) Spec Decode Draft Model across whole-directory replicas",
      "pr_body": "## What\n\nShard the `spec-decode-draft-model` job (Spec Decode Draft Model) with pytest-shard on its one whole directory, `v1/e2e/spec_decode/draft_model/`: 4 replicas on the primary lane and 2 on the AMD mirror. Recent `main` runs spend 34–43m in pytest on the primary lane and ~25m on the mirror, against the ~20m per-job target. No tests move and no filters are added.\n\n## Old\n\n```\ntests/v1/e2e/spec_decode/draft_model/     # 20 cases, one job, one replica per lane\n├── test_async.py                         # 3 cases\n├── test_draft_model.py                   # 16 cases (2 skip: need 2 GPUs)\n└── test_lora.py                          # 1 case\n```\n\n`pytest -v -s v1/e2e/spec_decode/draft_model/`, key `spec-decode-draft-model`, mirror `amd-spec-decode-draft-model`.\n\n## New\n\nSame tree and same key. The command gets `--num-shards=$$BUILDKITE_PARALLEL_JOB_COUNT --shard-id=$$BUILDKITE_PARALLEL_JOB`, and the labels get `Shard %N`.\n\n| key | parallelism | cases per replica (hash assignment) |\n|---|---|---|\n| `spec-decode-draft-model` | 4 | 5 / 3 / 5 / 7 |\n| `amd-spec-decode-draft-model` | 2 (explicit `mirror.amd.parallelism`) | 10 / 10 |\n\n## Notes\n\n- Pattern A (whole-directory hash shards) on the existing named job. A per-file split does not fit because `test_draft_model.py` alone takes ~29m of pytest, and splitting that monolith would add a larger diff with no benefit.\n- Why 4 replicas: the per-case timings put the slowest replica at 22m of pytest with 2 replicas and 19m with 3 (10 of the 20 cases hash into one shard), which is too close to the target given the 34–43m spread between runs. With 4, the slowest replica is ~16m. Every replica gets at least 3 cases, so no shard is empty. The mirror's own timings put its slowest replica at ~14m with 2 replicas, so it overrides the inherited count instead of running 4.\n- Pytest-shard keeps file order inside each shard, so no test runs earlier than it did before. The env changes in these tests all go through `monkeypatch`, so they are reverted after each test.\n- Unchanged: the B200 nightly step and the legacy `test-amd.yaml` copies (optional) still run the directory unsharded.\n\n## Expected runtime and overhead (predictions)\n\nThese predictions use per-case pytest durations from two recent `main` builds (90437, 90426), taken from log timestamps. Per-replica overhead is 2–4m, the wall-minus-pytest range the unsharded job showed in those builds (checkout, image pull, container start). The cause breakdown is unattributed. Measured numbers will follow in a validation comment.\n\n| job / replica | pytest | est. running wall | est. non-pytest overhead | main overhead cause |\n|---|---|---|---|---|\n| primary shard 0 | ~6.7m | ~10m | ~3m | setup (unattributed) |\n| primary shard 1 | ~6.2m | ~9m | ~3m | setup (unattributed) |\n| primary shard 2 | ~13.8m | ~17m | ~3m | setup (unattributed) |\n| primary shard 3 | ~15.8m | ~19m | ~3m | setup (unattributed) |\n| mirror shard 0 | ~13.9m | ~16m | ~2.5m | setup (unattributed) |\n| mirror shard 1 | ~11.8m | ~14m | ~2.5m | setup (unattributed) |\n\n- **Primary lane:** 44.4m wall before (build 90437) → ~55m total worker-minutes after, **~+10 extra** (range +5 to +12, from 3 extra setups). This is inside the ≤20 budget. Slowest replica ~16m pytest.\n- **Mirror lane:** 26.8m wall before → ~30m after, **~+3 extra**. Slowest replica ~14m pytest.\n\n## AI assistance & verification\n\nThis PR was prepared with AI assistance (Claude Code); a human reviewed the diff. No existing PR covers this job.\n\n- `yaml.safe_load` on `spec_decode.yaml`: passes.\n- The coverage guard (`check_area_test_coverage.py --tree tests/v1/e2e/spec_decode`) accepts the shard flags on a whole-directory target for both base and mirror. It reports no new violations; the violations it does report were already there and belong to other jobs.\n- The ci-infra generator reads `amd.get(\"parallelism\", step.parallelism)`, so the mirror's explicit `2` overrides the base `4`.\n- GPU validation: a scoped build will follow as a comment.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n"
    },
    {
      "sha": "4ec0b95c447fe70b92b2d12164c31255bb178e21",
      "pr_number": 57113,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57113",
      "pr_title": "[CI] Split LM Eval TurboQuant KV Cache into per-config jobs",
      "pr_body": "## What\n\n`LM Eval TurboQuant KV Cache` runs four GSM8K accuracy evals — one per kv-cache-dtype config — serially in a single H200 MIG job measuring 39–51m of pytest against a 55m timeout. This replaces it with four content-named jobs, one config each, so the timeline and job names say exactly what ran. Same per-config split as the merged `(H200) LM Eval Large Models` change (#57965).\n\n## Old\n\n```text\ntests/evals/gsm8k/configs/\n└── models-turboquant.txt        # 4 configs, one job: 39–51m pytest, timeout 55m\n```\n\n## New\n\n```text\nlm-eval-turboquant-k8v4    (H200 MIG 18GB) LM Eval TurboQuant k8v4    models-turboquant-k8v4.txt\nlm-eval-turboquant-t4nc    (H200 MIG 18GB) LM Eval TurboQuant t4nc    models-turboquant-t4nc.txt\nlm-eval-turboquant-k3v4nc  (H200 MIG 18GB) LM Eval TurboQuant k3v4nc  models-turboquant-k3v4nc.txt\nlm-eval-turboquant-t3nc    (H200 MIG 18GB) LM Eval TurboQuant t3nc    models-turboquant-t3nc.txt\n```\n\nOld → new key: `lm-eval-turboquant-kv-cache` → the four `lm-eval-turboquant-*` keys. `models-turboquant.txt` is removed; the four YAML configs are untouched, each in exactly one list. Timeout drops 55 → 30 (~1.7× the slowest measured config).\n\n## Notes\n\n- **Same cases, unchanged configs.** The four `Qwen3-4B-TQ-*.yaml` configs are not modified; coverage is 4/4 configs exactly once, verified locally. `models-turboquant.txt`'s only CI consumer was this job.\n- **Per-config jobs over `parallelism: 4`.** An earlier revision of this PR used shard-N list files; revised to named per-config jobs to match #57965 — timeline-readable names and per-config flake isolation (retrying one config no longer re-runs the other three), at the cost of four block/job entries instead of one job with replicas.\n- **Resource note:** the four MIG jobs can run concurrently; each still fits a single MIG slice.\n- No AMD mirror exists on this step and `.buildkite/test-amd.yaml` is untouched; only the TurboQuant lane changes.\n\n## Validation\n\nScoped build [#89258](https://buildkite.com/vllm/ci/builds/89258) measured the four configs individually (12.4–15.9m pytest, 14.4–17.2m wall each, all green) — those per-config durations carry over unchanged since the configs and commands are identical; only the job boundary moved. A fresh scoped build on the revised head is linked in the comments.\n\nChecks run locally: `yaml.safe_load` on the edited pipeline YAML; a script verifying each list file resolves to an existing config and that the four TurboQuant configs are claimed exactly once (matching the old `models-turboquant.txt`); no remaining references to the old key or list file. `pytest --collect-only` is not runnable locally (no GPU/torch env); the scoped build exercises all four jobs.\n\n## Contribution checklist\n\n- Not a duplicate: no open PR touches this job or these configs (searched `turboquant`, `lm eval shard`, and this branch name).\n- This change is AI-assisted (noted per the contribution policy); commit trailers carry the attribution.\n"
    },
    {
      "sha": "5c3b61e9c438a605b52d6d1c2639b2dcb5bbf575",
      "pr_number": 58197,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58197",
      "pr_title": "[Core] Disable JIT warmup in eager mode",
      "pr_body": "## Purpose\r\n\r\nMake `--enforce-eager` also disable JIT kernel warmup. Eager mode already opts out of torch compilation and CUDA graph capture, so paying the up-front JIT compilation cost is unexpected and particularly expensive for short-lived processes.\r\n\r\nAI assistance was used to investigate the startup regression and prepare this change. The submitter reviewed the final diff.\r\n\r\n## Test Plan\r\n\r\n```bash\r\n/home/mgoin/.cache/pre-commit/repoba318kxi/py_env-python3.12/bin/ruff check vllm/config/vllm.py\r\ngit diff --check\r\n```\r\n\r\nNo model evaluation is needed because this only changes eager-mode startup warmup behavior; model execution and outputs are unchanged.\r\n\r\n## Test Result\r\n\r\n```text\r\nAll checks passed!\r\ngit diff --check: passed\r\n```\r\n"
    },
    {
      "sha": "01e15bc94cd29cd1fcbbab484e96acb5b4f6bb14",
      "pr_number": 58189,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58189",
      "pr_title": "[Bugfix] Backport Inductor custom-op pattern matching fix",
      "pr_body": "Backport pytorch/pytorch#196013 for PyTorch 2.15 and earlier in `env_override.py` so Inductor's unsupported-dtype filter only applies to built-in operators during pattern matching. This fixes `auto_functionalized was not removed` with Humming's E8M0 scales.\n\nApply the patch when Inductor imports its pattern matcher, without forcing that import during vLLM initialization.\n\nComplements #58054, which updates Humming. Duplicate searches found no vLLM PR carrying this compiler fix.\n\nValidation on B300 with PyTorch 2.13:\n\n- `chg run -g=1 -- .venv/bin/python -m pytest tests/compile/test_config.py -q`: 66 passed, including both regression cases and all nine tests that failed in CI. Nine B300 capture-size assertions also fail with the compatibility patch disabled.\n- `chg run -g=1 -- .venv/bin/python -m pytest tests/compile/test_config.py -k 'e8m0_custom_op_fullgraph or test_no_compilation' -q`: 3 passed with deferred patching. Fresh-process import comparison against the PR base loads no additional Inductor modules and leaves CUDA uninitialized; importing Inductor before vLLM also works.\n- `chg run -g=1 -- .venv/bin/python -m pytest tests/cuda/test_platform_no_cuda_init.py -q`: 2 passed. The fullgraph test runs in a child process because stock Dynamo initializes CUDA when saving RNG state, even for CPU inputs; importing vLLM with the patch does not initialize CUDA.\n- `chg run -g=1 -- .venv/bin/python -m pytest tests/evals/gsm8k/test_gsm8k_correctness.py --config-list-file=/tmp/vllm-e8m0-eval/configs.txt -s -v`: passed, **38.67% on 1,319 questions**. The list selects the existing `Qwen3-0.6B-MXFP8-humming.yaml` config with `--enforce-eager` removed, using Humming 0.1.16. Compilation and CUDA graph capture completed.\n- `pre-commit run --files vllm/env_override.py tests/compile/test_config.py`: passed.\n\nAI assistance was used for implementation and validation.\n"
    },
    {
      "sha": "ecdd11d0e0a84d73d3c1348d4f37317c5140d32a",
      "pr_number": 58204,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58204",
      "pr_title": "[CI] Build the torch-nightly image on Ubuntu 24.04",
      "pr_body": "## Problem\n\nThe torch-nightly Docker build has been failing since #51360 ([build 90413](https://buildkite.com/vllm/ci/builds/90413#01a0c943-22bd-4c2f-b491-f5259e9b38b1)):\n\n```\nE: Unable to locate package libgnutls30t64\n+ code=100\n[TIMING] snapshot runtime install: 5 s (exit 100)\n```\n\n#51360 added `tools/install_snapshot_runtime.sh`, whose `snapshot_runtime_deps` includes `libgnutls30t64`. That's the 64-bit `time_t` rename — it exists on Ubuntu 24.04 and not on 22.04.\n\nThe list is **correct** for the image everything else builds. `docker/Dockerfile` defaults to `ARG UBUNTU_VERSION=24.04`, and `release-pipeline.yaml` and `docker-bake.hcl` both pass `24.04` explicitly. `.buildkite/image_build/image_build_torch_nightly.sh` was the one place still pinned to `ubuntu22.04`, so the nightly lane was the only build running that layer on jammy.\n\n## This is not a nightly-only problem — the cache was hiding it\n\nRegular CI stayed green throughout, which made this look nightly-specific. It isn't; the layer was simply cached.\n\nBuild **#90310** seeded it on noble right after #51360 landed, and the package installed fine:\n\n```\nGet:3 .../ubuntu noble-updates/main amd64 libgnutls30t64 amd64 3.8.3-1.1ubuntu3.6\nPreparing to unpack .../libgnutls30t64_3.8.3-1.1ubuntu3.6_amd64.deb\n```\n\nEvery later build reported `#92 CACHED` for that step — 73 cached layers in #90376. The nightly build changes the torch wheel, invalidating an earlier layer and cascading into a full rebuild (**0** cached layers in #90413), so it's currently the only lane that actually executes the apt install.\n\n| build | ubuntu | that layer | result |\n|---|---|---|---|\n| #90310 | noble (24.04) | ran, 1st time | passed — seeded the cache |\n| #90376 | — | `#92 CACHED` | passed |\n| #90413 (nightly) | jammy (22.04) | ran, 0 cache hits | **failed** |\n\n## Fix\n\nAlign the nightly base to 24.04 rather than special-casing the dependency list.\n\nThe nightly lane exists to run the normal pipeline against nightly torch, so it should differ from the regular image in **torch alone**. Having it on a different distro undermines the comparison it's there to make — and leaving it would keep masking 24.04-only breakage from the nightly lane and jammy-only breakage from everything else.\n\n`nvidia/cuda:13.0.2-base-ubuntu24.04` exists on Docker Hub (verified against the registry manifest API), so the tag resolves.\n\n## Alternative considered\n\nMaking `install_snapshot_runtime.sh` pick the package by `VERSION_CODENAME` would also unbreak the build, but it entrenches the OS divergence rather than removing it. Happy to switch if you'd rather keep the nightly lane on 22.04 for a reason I'm not seeing.\n\n`bash -n` clean.\n\ncc @khluu @simon-mo"
    },
    {
      "sha": "25965ee583b154c0ce356466d4cf2d25f6815c60",
      "pr_number": 58001,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58001",
      "pr_title": "[Kernel] Remove AllSpark INT8 W8A16 GEMM backend",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\n\r\nThis backend is not important for models now, Ampere performance is not the highest priority, and Marlin/Humming cover this case very well.\r\n\r\nTo be clear, AllSpark only supported dense GEMMs using INT8 weights with channelwise scales on SM80-89, so a very narrow usecase.\r\n\r\n## Test Plan\r\n\r\n## Test Result\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "1b3b88ec2b7457aa030db4d0e7d8aaf04f6d0fb8",
      "pr_number": 57250,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57250",
      "pr_title": "[Core] structured generation mode for DiffusionGemma model (Jev-like)",
      "pr_body": "## Prerequisite PRs\n\nA few PRs that can go in on their own merits were split out:\n\n - https://github.com/vllm-project/vllm/pull/57414: hand out stashed logprobs only on the committing step (bug fix)\n - https://github.com/vllm-project/vllm/pull/57416: give a prefill-only batch the model state's number of logit rows (perf)\n - https://github.com/vllm-project/vllm/pull/57417: honor logprob_token_ids on the converging step (feature parity w/autoregressive)\n - https://github.com/vllm-project/vllm/pull/57462: type cast error if dynamo falls back to eager mid-run (bugfix)\n - https://github.com/vllm-project/vllm/pull/57589: fix multimodal support in DiffusionGemma (bugfix)\n\n## Purpose\n\nDiffusionGemma (and likely other models in vLLM) contains all of the necessary components to build a Jev-like model that provides structured answers with bounded choices. \n\nBy fixing canvas positions in the given structured output format, you can ask the model to provide a token and logprobs (from which a client derives entropy) that can be used to determine the models most likely answer and its confidence in that answer. If entropy does not pass a given threshold, you can perform additional sampling to determine agreement between runs and therefore the actual confidence of the model's answer.\n\nThis PR contains a prototype example server for serving the `/v1/systemone` endpoint. It does not implement a standard endpoint in vLLM. \n\nThe model is sufficiently powerful to work with _any_ structured output that follows one limitation: the token choices _must_ be a single token, so that the canvas itself does not shift. The requesting client can always translate multi-token options into single token options, however (ie: \"moderation_spam\" -> \"A\"). The choices are always laid out in a system prompt and the model correctly diffuses single-token-slot answers (or makes a guess and flags it with uncertainty).\n\nWhy do we require the token IDs in the request? Currently we provide the token slot positions that are unfixed for the model to choose and this allows the two to be matched more accurately. A followup PR will likely add a template syntax. \n\nThe model, using this framework, can answer yes/no (\"noul\" in Jev parlance), scale, or multiple choice questions, and provides its certainty and error bars with the answer.\n\nThe only missing pieces for this to work in vLLM today are in this PR:\n\n - A way to pre-seed the canvas with the fixed tokens\n - A way to send read-only requests that emit the argmax canvas on the converging step and skip the commit forward (logprobs come from that step at temperature 1)\n - A way to limit the number of steps run (we only want the first step)\n - A bugfix for two parallel DiffusionGemma generations that stomp on each other in lockstep: two concurrent requests with logprobs on where one converges in the same step another commits loses its stash and the request fails with HTTP 500 \"list index out of range\".\n\nThis PR currently contains the machinery to make this model work and a sample interposer server is included to demonstrate end-to-end functionality that is sufficient to serve traffic at reasonable scale.\n\n   | field | type | validation | effect |\n   |---|---|---|---|\n   | `diffusion_seed_canvas` | `list[int]`, exactly the request's canvas width | ids in `[0, vocab)`, length checked against `diffusion_canvas_length`, else the served `--diffusion-config` | replaces the random canvas after prefill |\n   | `diffusion_max_steps` | `int >= 1` | clamped to `max_denoising_steps` | denoise steps before the canvas is emitted |\n   | `diffusion_pinned` | `list[int]` of canvas positions | needs a `diffusion_seed_canvas`; positions must lie inside the request's canvas | held at their seed value on every denoise step, and their self-conditioning soft embed is zeroed, so a read past one step denoises the canvas the request seeded instead of one the model rewrote |\n   | `diffusion_read_only` | `bool` (the OpenAI server delivers JSON booleans in `vllm_xargs` as 0/1, which is accepted) | caps `max_tokens` at the canvas, sets `ignore_eos` | emit on the converging step, skip the commit forward, temperature-1 logprobs |\n   | `diffusion_canvas_length` | `int`, 1 to the served `canvas_length` | validated in `verify`; the seed must be this long | the request runs on a canvas this wide: the scheduler schedules this many draft tokens and the sampler tiles the read at this width, so a short schema on a wide server costs the same as on a server of its own width |\n   | `logprob_token_ids` (a request field, not in `vllm_xargs`) | `list[int]`, up to 128 | existing vLLM validation | exact logprobs for those ids at every canvas position. The sampler now honors it on the converging step, so a read asks for its label ids instead of hoping they rank in the top-k (with 26 options only 1-8 did) |\n\nExample request:\n\n```\n{\n  \"model\": \"dgemma\",\n  \"messages\": [\n    {\"role\": \"system\", \"content\": \"Answer a fixed set of questions about the state ... Question urgent: ...\\n  yes\\n  no\\n...\"},\n    {\"role\": \"user\",   \"content\": \"{\\\"ticket\\\": \\\"Since this morning the dashboard shows a blank page after login...\\\"}\"}\n  ],\n  \"chat_template_kwargs\": {\"enable_thinking\": false},\n  \"max_tokens\": 17,\n  \"logprobs\": true,\n  \"top_logprobs\": 20,\n  \"return_tokens_as_token_ids\": true,\n  \"vllm_xargs\": {\n    \"diffusion_seed_canvas\": [100, 45518, 107, 101, 148436, 236787, 71203, 107, 36164, 236787, 9950, 107, 66387, 236787, 236743, 189012, 106, 0, 0, \"... 64 ids total\"],\n    \"diffusion_max_steps\": 1,\n    \"diffusion_read_only\": true\n  }\n}\n```\n\n## Example Queries\n\nThese are _raw_ example queries passed to the model:\n\n```\nAnswer a fixed set of questions about the state the user provides. Each question lists its allowed answers; reply with exactly one label per question.\n\nYou are reviewing a user post on a community forum.\n\nQuestion violates: Does the post break the community rules?\n  yes\n  no\n\nQuestion category: Which rule does it come closest to breaking?\n  A: none\n  B: spam (unsolicited promotion)\n  C: harassment (targets a person or group)\n  D: self_harm\n\nQuestion severity: How severe is it?\n  1: none\n  2: mild\n  3: serious\n  4: critical\n\nReply with one line per question, in this order, formatted as \"id: label\".\n```\n\nAnswer template (@ standing in for a noised slot):\n\n```\n<|channel>thought\\n<channel|>violates: @\\ncategory: @\\nseverity: @\n```\n\nSeeded canvas:\n\n```\n[100, 45518, 107, 101, 51584, 1090, 236787, 58369, 107, 10618, 236787, 13112, 107, 127243, 236787, 236743, 144194, 106, 0, 0, ... 0]\n  pos 7:  violates <- 58369  (' Mell')\n  pos 11: category <- 13112  (' laugh')\n  pos 16: severity <- 144194 ('اهرة')\n```\n\n## Test Plan\n\nRun locally on a DGX spark, with a concurrency sweep to ensure it scales and does not corrupt parallel generations.\n\n```\npytest tests/test_sampling_params.py tests/v1/sample/test_diffusion_gemma_reads.py\n```\n\nRun with: \n\n```\nvllm serve nvidia/diffusiongemma-26B-A4B-it-NVFP4 --diffusion-config '{\"canvas_length\": 32}' --max-logprobs 32 --enable-prefix-caching --async-scheduling --attention-backend TRITON_ATTN --max-num-seqs 32\npython examples/features/structured_diffusion/structured_server.py --upstream http://127.0.0.1:8000 --tokenizer nvidia/diffusiongemma-26B-A4B-it-NVFP4 --canvas 32\n```\n\n## Test Result\n\nHardware: 1 DGX Spark, sole active GPU process.\n\nSetup: read-only single reads, 32-row canvas, three decisions per request, cache-busted state with the schema prefix cached\n\nResults: 1-way 8.7 req/s at 0.12 s, 32-way 54.0 req/s at 0.58 s, about 162 decisions/s.\n\nUsing a sample request/response corpus to determine, from a mock support request, the urgency, tone and routing of an outage report, we get the correct variable number of samples per request (the auto policy re-reads only when the first read is uncertain):\n\nH1: is the first read's entropy per question over the top-20 logprobs (note that reads=4 when H1>0.1)\n\n```\n== samples=auto\noutage      527ms reads=4 urgent=0.88±0.04 bucket=outage(1.00) tone=furious(3.00) H1=urgent:0.87 bucket:0.36 tone:0.08\nbilling     301ms reads=4 urgent=0.00±0.00 bucket=billing(1.00) tone=calm(1.00) H1=urgent:0.65 bucket:0.36 tone:0.28\nfeature     117ms reads=1 urgent=0.00 bucket=feature(1.00) tone=calm(1.00) H1=urgent:0.06 bucket:0.07 tone:0.02\nmixed       296ms reads=4 urgent=0.00±0.00 bucket=billing(1.00) tone=annoyed(2.02) H1=urgent:0.26 bucket:0.13 tone:0.25\n```\n\n<img width=\"2480\" height=\"1120\" alt=\"image-1789615968197\" src=\"https://github.com/user-attachments/assets/c6f52344-0ec2-4d0d-a101-3b5eb17a49cf\" />\n\n## Additional Results\n\n```\n== programming language\n  ok  want=python       got=python       p(want)=1.00±0.00  reads=4  528ms\n  ok  want=rust         got=rust         p(want)=0.99±0.00  reads=4  485ms\n  ok  want=javascript   got=javascript   p(want)=1.00±0.00  reads=4  493ms\n  ok  want=go           got=go           p(want)=0.97±0.01  reads=4  478ms\n  ok  want=c            got=c            p(want)=1.00±0.00  reads=4  497ms\n  ok  want=sql          got=sql          p(want)=1.00±0.00  reads=4  428ms\n  ok  want=bash         got=bash         p(want)=1.00±0.00  reads=4  420ms\n  ok  want=ruby         got=ruby         p(want)=0.99±0.00  reads=4  483ms\n  ok  want=java         got=java         p(want)=1.00±0.00  reads=4  423ms\n  ok  want=haskell      got=haskell      p(want)=0.90±0.02  reads=4  420ms\n  accuracy 10/10\n== human language\n  ok  want=english      got=english      p(want)=1.00±0.00  reads=4  508ms\n  ok  want=french       got=french       p(want)=1.00±0.00  reads=4  494ms\n  ok  want=german       got=german       p(want)=1.00±0.00  reads=4  474ms\n  ok  want=spanish      got=spanish      p(want)=1.00±0.00  reads=4  481ms\n  ok  want=italian      got=italian      p(want)=0.95±0.01  reads=4  482ms\n  MISS want=portuguese   got=french       p(want)=0.47±0.11  reads=4  496ms\n  ok  want=dutch        got=dutch        p(want)=0.98±0.01  reads=4  512ms\n  ok  want=japanese     got=japanese     p(want)=0.98±0.01  reads=4  514ms\n  ok  want=swedish      got=swedish      p(want)=0.93±0.04  reads=4  508ms\n  ok  want=polish       got=polish       p(want)=0.98±0.01  reads=4  504ms\n  accuracy 9/10\n== unit comparison\n  ok  want=first        got=first        p(want)=0.98±0.00  reads=4  503ms\n  ok  want=second       got=second       p(want)=0.69±0.03  reads=4  507ms\n  MISS want=first        got=second       p(want)=0.20±0.03  reads=4  504ms\n  ok  want=equal        got=equal        p(want)=1.00±0.00  reads=4  503ms\n  ok  want=equal        got=equal        p(want)=1.00±0.00  reads=4  491ms\n  ok  want=first        got=first        p(want)=0.86±0.01  reads=4  498ms\n  ok  want=equal        got=equal        p(want)=1.00±0.00  reads=4  418ms\n  ok  want=second       got=second       p(want)=0.99±0.00  reads=4  435ms\n  MISS want=first        got=second       p(want)=0.19±0.02  reads=4  493ms\n  ok  want=second       got=second       p(want)=0.98±0.00  reads=4  493ms\n  ok  want=second       got=second       p(want)=0.80±0.01  reads=4  502ms\n  ok  want=first        got=first        p(want)=0.77±0.01  reads=4  441ms\n  accuracy 10/12\n```\n\nSolve an ASCII maze giving it only N/E/W/S options:\n\n<img width=\"885\" height=\"529\" alt=\"maze\" src=\"https://github.com/user-attachments/assets/51d7711a-13f2-40e1-a5cf-2d768ffaabb6\" />\n\nIt even works with the vision tower!\n\n```\n== color, by square size (8 colors)\n  size 8px        8/8   p(correct) 1.00    426 ms\n  size 16px       8/8   p(correct) 1.00    459 ms\n  size 32px       8/8   p(correct) 1.00    427 ms\n  size 64px       8/8   p(correct) 1.00    452 ms\n  size 128px      8/8   p(correct) 1.00    449 ms\n== count, red 32px squares on a 4x4 grid\n  1 squares       4/4   p(correct) 0.99    495 ms\n  2 squares       4/4   p(correct) 0.98    487 ms\n  3 squares       4/4   p(correct) 0.89    478 ms\n  4 squares       4/4   p(correct) 0.94    492 ms\n== quadrant, 32px square\n  top_left        4/4   p(correct) 0.97    505 ms\n  top_right       4/4   p(correct) 1.00    497 ms\n  bottom_left     4/4   p(correct) 0.84    500 ms\n  bottom_right    3/4   p(correct) 0.51    498 ms\n== presence: is the square red? (half yes, half no)\n  size 8px        8/8   p(correct) 0.94    441 ms\n  size 16px       8/8   p(correct) 1.00    429 ms\n  size 32px       8/8   p(correct) 1.00    407 ms\n  size 64px       8/8   p(correct) 1.00    404 ms\n== contrast: 48px gray square vs plain background, flat/square choice (half each)\n  contrast +2     6/8   p(correct) 0.75    348 ms\n  contrast +4     8/8   p(correct) 0.98    319 ms\n  contrast +8     8/8   p(correct) 0.99    356 ms\n  contrast +16    8/8   p(correct) 0.94    315 ms\n  contrast +32    8/8   p(correct) 1.00    292 ms\n  contrast +64    8/8   p(correct) 1.00    309 ms\n  contrast +127   8/8   p(correct) 1.00    320 ms\n```\n\n---\n<details>\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\n\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\n- [x] The test plan, such as providing test command.\n- [x] The test results, such as pasting the results comparison before and after, or e2e results\n- [x] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\n</details>\n\n\n"
    },
    {
      "sha": "fae5cbfc8b7e957c891ad88881969f3a41de065e",
      "pr_number": 58002,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58002",
      "pr_title": "[Refactor] Remove dead code multiple places",
      "pr_body": "## Purpose\r\n\r\nRemove dead code multiple places\r\n"
    },
    {
      "sha": "276db94fb22f032a933c3c17e6ed377a8fc612fb",
      "pr_number": 55442,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55442",
      "pr_title": "[Bugfix][Model][Spec Decode] Defer disposable GLM MTP head",
      "pr_body": "## Summary\n\n- defer the GLM MTP `ParallelLMHead` that `load_eagle_model()` replaces with the target model's head\n- keep `SharedHead`'s default behavior unchanged for other MTP models\n- verify the quantized placeholder is not allocated\n\nGLM MTP constructs a per-layer `ParallelLMHead` that `load_eagle_model()` replaces with the target model's head. That temporary head caused the load-time memory peak this PR originally addressed. #52501 subsequently initialized unloaded ModelOpt NVFP4 `weight_scale` parameters to a NaN sentinel and added a check that rejects an unoverwritten sentinel during quantization processing. Because the disposable head has no checkpoint scales to load, draft loading now fails inside `get_model()`, before `load_eagle_model()` can replace it.\n\nFixes #57532.\n\n## Validation\n\n- `HF_HUB_OFFLINE=1 PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest tests/v1/spec_decode/test_mtp.py -q --tb=short` (`3 passed`)\n- `pre-commit run --files vllm/model_executor/models/deepseek_mtp.py vllm/models/glm5next/common/mtp.py tests/v1/spec_decode/test_mtp.py` (passed)\n- DGX Spark, 2 nodes / TP=2, GLM-5.3-Flash NVFP4, MTP=3, 8K validation context:\n  - main at validation time (`4c6c1a40b3`) fails while loading the draft: `NVFP4 weight_scale for layer 'parallel_lm_head' was never loaded (still NaN)`\n  - the same build with only #52501 reverted reaches ready state and serves the expected model alias\n  - this PR (`67e55792ff`) reaches ready state, returns HTTP 200 from `/health`, serves `glm-5.3-flash`, and completes a deterministic short request with MTP active (96 draft tokens, 44 accepted)\n\nHardware validation used `67e55792ff`. The identical patch was subsequently rebased onto `44dd18fe0b` as `5517abf8da`; the focused tests and pre-commit passed again after rebase.\n\nThis PR predates #57532; duplicate searches found no competing fix.\n\nAI assistance was used to rebase the change, run validation, and draft this description. Every changed line and the validation results were reviewed by the submitter.\n"
    },
    {
      "sha": "71a239284507e4f9477f8256e04fd28d1aa9a2db",
      "pr_number": 58149,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58149",
      "pr_title": "[Bugfix][V1] Read ModelState max_model_len from model config",
      "pr_body": "## Purpose\n\nFixes #57034.\n\nWhen auto-fit reduces `max_model_len` after model loading, V2's `ModelState` retains the original length while attention metadata builders use the reduced limit. CUDA graph capture can then request more context than those builders support. For example, fitting 1,048,576 tokens down to 980,224 leaves DeepSeek-V4 C128A capture requesting 8,192 compressed entries from a buffer with capacity 7,680.\n\nMake `ModelState.max_model_len` a read-only property backed by `model_config.max_model_len`. All model-state capture paths follow the worker's resolved limit without maintaining another cached copy or extending the runner's update list. Update the existing Mamba test fixture to supply its limit through the config.\n\nAllocation capacities remain distinct from the serving limit: for example, `RopeState` uses its saved length as a physical buffer stride and must retain that value unless its storage is rebuilt.\n\n## Validation\n\n```bash\n.venv/bin/python -m pytest tests/v1/worker/test_gpu_model_runner_v2.py tests/v1/worker/test_mamba_hybrid_model_state.py::test_prepare_attn_forwards_positions -q\n```\n\nResult: 9 passed.\n\n```bash\n.venv/bin/pre-commit run --files vllm/v1/worker/gpu/model_runner.py vllm/v1/worker/gpu/model_states/interface.py tests/v1/worker/test_mamba_hybrid_model_state.py\n```\n\nResult: all applicable hooks passed.\n\nA temporary CPU reproduction using the real `DefaultModelState.prepare_attn` and `DeepseekV4SparseMLAMetadataBuilder` reproduced the original C128A assertion. With this change, capture uses 980,224 tokens and requires 7,658 compressed entries, within the builder's 7,680-entry capacity. The final GPU kernel was stubbed; full DeepSeek model startup and inference were not run.\n\n## Contribution notes\n\nThe duplicate audit found #46794 and #46884, which address V1 InputBatch/block-table sizing. This PR addresses V2 ModelState capture; the update stays on the existing PR for #57034.\n\nAI assistance was used to investigate, implement, and validate this update.\n"
    },
    {
      "sha": "d1f63f2af98540082473cfa4aed3e55eb21dce8b",
      "pr_number": 58030,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58030",
      "pr_title": "[ROCm][CI] Add GELU activation for AiterExperts in the modular-kernel coverage",
      "pr_body": " ## Purpose\r\n\r\n  Part 2 of #54966 plan. `AiterExperts` already declares support for `GELU`, `SITU`, `SWIGLUOAI`, and `SWIGLUOAI_UNINTERLEAVE` (only `SILU` is exercised today), but the modular-kernel harness (`tests/kernels/moe/modular_kernel_tools/common.py` +  `test_modular_kernel_combinations.py`) hardcodes `SILU` for every backend, so none of that support is verified through the actual `AiterExperts.apply()` path\r\n  (shapes, `PrepareAndFinalize` combos, world sizes).\r\n\r\n This adds a real `activation` field to `Config`, threads it through `is_valid()` / `reference_moe_impl` / `make_modular_kernel` / `run_modular_kernel`, and extends the sweep with `GELU` alongside `SILU`. Scoped to:\r\n\r\n  - **AITER-only**: pruned via `is_nyi_config` for now; other backends can opt in later.\r\n  - **Unquantized-only**: FP8 + non-SILU-activation combos are a follow-up (needs scale-tensor interleave handling for some activations).\r\n\r\n  Note `test_rocm_aiter_moe.py` already has direct-kernel activation tests (e.g. `test_aiter_fused_moe_gelu_accuracy`, and `SWIGLUOAI` via the mxfp4  accuracy/determinism tests) — but those call `torch.ops.vllm.rocm_aiter_fused_moe`  directly, bypassing `AiterExperts` and the modular-kernel abstraction entirely. This PR covers the missing integration layer, not a duplicate of that coverage.\r\n\r\n  `SITU` and `SWIGLUOAI`/`SWIGLUOAI_UNINTERLEAVE` are left out of this PR:\r\n  - `SITU`: AITER's SiTUv2 kernel is only implemented behind its MXFP4-quantized    (`per_1x32`) dispatch path. With unquantized bf16 weights      (this PR's scope), AITER falls through to its generic CK codegen instead,      which has no `situv2` instance by design (situv2 is FlyDSL-only per      AITER's own `moe_recipes.py`), causing the JIT failure \r\n      (`gemm_moe_ck2stages_lookup.h` not found). Verified directly against      `aiter.fused_moe()` outside vLLM. Exercising `SITU` for real needs the      same MXFP4 weight-quantization plumbing already noted as a follow-up      above, left for that work.\r\n  - `SWIGLUOAI`/`SWIGLUOAI_UNINTERLEAVE`: AITER currently doesn't support this for bf16 (as default ckpt for gpt-oss are in mxfp4). Unquantized bf16 with no tuned kernel config, `fused_moe()` falls back to the default CK  path (`ck_moe_stage1`/`ck_moe_stage2_fwd`), whose compiled kernel takes only an `activation` enum with no `gate_mode`/interleave parameter — it hardcodes  a separated gate/up layout for Swiglu. AITER's interleave-aware `gate_mode` dispatch only exists for the mxfp4 (`a4w4`) path and a CK-tile path gated on `quant_type == per_1x32`; neither is reached here. (Opened [aiter issue](https://github.com/ROCm/aiter/issues/5760))\r\n\r\n  Since `Config.activation` defaults to `SILU`, this has no effect on non-AITER backends: `AiterExperts` only registers when `has_aiter()` is true, and `is_nyi_config` prunes non-SILU activations paired with any other backend at collection time - no new test cases generated elsewhere.\r\n\r\n  ## Test plan\r\n\r\n  - `VLLM_ROCM_USE_AITER=1 VLLM_ROCM_USE_AITER_MOE=1 pytest tests/kernels/moe/test_modular_kernel_combinations.py -k \"AiterExperts and singlegpu\" -v -s` → 6 passed, GELU + SILU both exercised through `AiterExperts.apply()`.\r\n  - Same file, full suite (all backends, singlegpu + multigpu) → 37 passed, no regressions.\r\n  - `pytest tests/kernels/moe/test_rocm_aiter_moe.py -v` → 32 passed, 2 skipped.\r\n\r\n  ##### Not duplicate work\r\n\r\n  Searched open/merged PRs for `54966`, `AiterExperts activation`. Closest is #57074   (merged) — the FP8/unquantized-scoping step of this same issue, doesn't touch  the activation axis. No other open PR adds activation coverage here.\r\n"
    },
    {
      "sha": "415e11c6f1256e8e007ee88f2350049c51f11a56",
      "pr_number": 55146,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55146",
      "pr_title": "[Bugfix][V1] Honor enable_jit_warmup for V2 kernel warmup",
      "pr_body": "## Purpose\r\n\r\nHonor `kernel_config.enable_jit_warmup` for the V2 scheduler-level kernel warmup.\r\n\r\nCurrently, `Worker.compile_or_warm_up_model()` calls `warmup_kernels()` unconditionally when the V2 model runner is enabled. As a result, setting `enable_jit_warmup=False` does not actually skip this JIT warmup path.\r\n\r\nThis PR gates the V2 `warmup_kernels()` call on the existing `enable_jit_warmup` configuration flag while preserving the current behavior when the flag is enabled.\r\n\r\nThis addresses the `enable_jit_warmup=False` behavior reported in #54866.\r\n\r\nOn an A100 40GB reproduction with a locally cached tiny Llama model, disabling JIT warmup reduced cold engine startup from:\r\n\r\n* Before: 125.865 s\r\n* After: 36.089 s\r\n* Reduction: 71.3%\r\n\r\nAs expected for lazy JIT, the first request became slower because compilation moved to first use:\r\n\r\n* First request: 68.1 ms -> 605.0 ms\r\n* Second request: 49.7 ms -> 47.7 ms\r\n\r\nGreedy output token IDs were identical before and after the change.\r\n\r\n## Test Plan\r\n\r\nAdded a focused regression test for the V2 worker initialization path:\r\n\r\n* `enable_jit_warmup=False` -> `warmup_kernels()` is not called\r\n* `enable_jit_warmup=True` -> `warmup_kernels()` is called exactly once\r\n\r\nAlso ran the complete nearest worker test file and an A100 runtime smoke test with V2 enabled.\r\n\r\nCommands:\r\n\r\n```bash\r\npytest tests/v1/worker/test_gpu_worker.py -q\r\ngit diff --check\r\n```\r\n\r\n## Test Result\r\n\r\n```text\r\n10 passed\r\n```\r\n\r\nPre-commit checks for the modified files passed.\r\n\r\nRuntime smoke test on NVIDIA A100 40GB:\r\n\r\n* V2 engine initialized successfully with `enable_jit_warmup=False`\r\n* Two consecutive greedy generations completed successfully\r\n* Output token IDs were identical\r\n* First request: 76.875 ms\r\n* Second request: 45.129 ms\r\n"
    },
    {
      "sha": "bc162b3f9280c2dc05dfd5aa282d29551f8f6d33",
      "pr_number": 57834,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57834",
      "pr_title": "[MRV2] Release weight offloader on shutdown",
      "pr_body": "## Purpose\r\n\r\nMRV2 leaves its process-global `PrefetchOffloader` alive after in-process shutdown, retaining model layers, pinned CPU weights, and GPU staging buffers. Reset it after device synchronization so the existing teardown can reclaim those resources before another engine starts.\r\n\r\n## Reproducer\r\n\r\nSave as `/tmp/repro_mrv2_offloader_shutdown.py`:\r\n\r\n```python\r\nimport gc\r\nimport weakref\r\nfrom types import SimpleNamespace\r\nfrom unittest.mock import patch\r\n\r\nimport torch\r\nimport torch.nn as nn\r\n\r\nfrom vllm.model_executor.offloader import (\r\n    PrefetchOffloader,\r\n    get_offloader,\r\n    set_offloader,\r\n)\r\nfrom vllm.v1.worker.gpu.model_runner import GPUModelRunner\r\n\r\nmodel = nn.Sequential(nn.Linear(2, 2))\r\nbuffer = torch.ones(1)\r\nrefs = [weakref.ref(model[0]), weakref.ref(model[0].weight), weakref.ref(buffer)]\r\n\r\noffloader = PrefetchOffloader.__new__(PrefetchOffloader)\r\noffloader.module_offloaders = [SimpleNamespace(module=model[0])]\r\noffloader.buffer_pool = SimpleNamespace(_buffers={\"weight\": [buffer]})\r\noffloader.prefetch_step = 1\r\noffloader._hook_module_forward(0, model[0])\r\nset_offloader(offloader)\r\n\r\nrunner = GPUModelRunner.__new__(GPUModelRunner)\r\nrunner.model = model\r\nrunner.speculator = None\r\nrunner.cudagraph_manager = None\r\nrunner.fast_prefill = None\r\nrunner.vllm_config = SimpleNamespace()\r\ndel model, buffer, offloader\r\n\r\nwith (\r\n    patch(\"vllm.v1.worker.gpu.model_runner.torch.accelerator.synchronize\"),\r\n    patch(\"vllm.v1.worker.gpu.model_runner.torch.accelerator.empty_cache\"),\r\n    patch(\"vllm.v1.worker.gpu.model_runner.free_before_shutdown\"),\r\n):\r\n    runner.shutdown()\r\n\r\ngc.collect()\r\nprint(type(get_offloader()).__name__)\r\nprint([ref() is not None for ref in refs])\r\n```\r\n\r\nRun:\r\n\r\n```bash\r\n.venv/bin/python /tmp/repro_mrv2_offloader_shutdown.py\r\n```\r\n\r\n### Main\r\n\r\n```text\r\nPrefetchOffloader\r\n[True, True, True]\r\n```\r\n\r\n### This branch\r\n\r\n```text\r\nNoopOffloader\r\n[False, False, False]\r\n```\r\n## Test Plan\r\n\r\nCPU and static checks:\r\n\r\n```text\r\n.venv/bin/python /tmp/repro_mrv2_offloader_shutdown.py\r\n.venv/bin/python /tmp/check_mrv2_offloader_shutdown_idempotence.py\r\n.venv/bin/python -m pytest tests/model_executor/offloader/test_prefetch.py -q --tb=short\r\n.venv/bin/python -m pytest tests/models/test_language_model_cache_is_weak.py tests/v1/engine/test_llm_engine_finalizer_is_weak.py -q --tb=short\r\n.venv/bin/pre-commit run --files vllm/v1/worker/gpu/model_runner.py\r\n.venv/bin/pre-commit run mypy-3.12 --files vllm/v1/worker/gpu/model_runner.py --hook-stage manual\r\n```\r\n\r\nResults: deterministic main-versus-branch and idempotence checks passed; 12 CPU tests passed; all file-scoped hooks and Python 3.12 mypy passed.\r\n\r\n```text\r\nCUDA_VISIBLE_DEVICES=1 .venv/bin/python /tmp/repro_mrv2_offloader_shutdown_e2e.py\r\nPATH=.venv/bin:$PATH CUDA_VISIBLE_DEVICES=1 VLLM_USE_V2_MODEL_RUNNER=1 .venv/bin/python -m pytest -q tests/basic_correctness/test_cpu_offload.py::test_mrv2_weight_offloading\r\nPATH=.venv/bin:$PATH CUDA_VISIBLE_DEVICES=1 VLLM_USE_V2_MODEL_RUNNER=1 .venv/bin/python -m pytest -q tests/basic_correctness/test_cpu_offload.py::test_cpu_offload\r\nPATH=.venv/bin:$PATH CUDA_VISIBLE_DEVICES=1 VLLM_USE_V2_MODEL_RUNNER=1 .venv/bin/python -m pytest -q tests/basic_correctness/test_prefetch_offload.py\r\n```\r\n\r\n```text\r\nALLOCATED_BEFORE_SHUTDOWN_BYTES=2760996352\r\nOFFLOADER_AFTER_SHUTDOWN=vllm.model_executor.offloader.base.NoopOffloader\r\nALLOCATED_AFTER_SHUTDOWN_BYTES=33555456\r\nWEAKREF_RELEASED_OFFLOADER=True\r\nWEAKREF_RELEASED_MODULE=True\r\nWEAKREF_RELEASED_PARAMETER=True\r\nWEAKREF_RELEASED_STAGING_TENSOR=True\r\nPREFETCH_BASELINE_OUTPUTS_IDENTICAL=True\r\nMRV2_OFFLOADER_SHUTDOWN_E2E=PASS\r\n```\r\n\r\nPytest results: MRV2 UVA/Prefetch offloading `2 passed`; CPU-offload output parity `4 passed`; Prefetch-versus-baseline output parity `1 passed`. The H200 image required `.venv/bin` on `PATH` so FlashInfer could find `ninja`.\r\n\r\n## AI assistance disclosure\r\n\r\nOpenAI Codex (GPT-5) assisted with root-cause analysis, and implementation."
    },
    {
      "sha": "f944491db914880dc5f12c0b53c1aac113014a4b",
      "pr_number": 58179,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58179",
      "pr_title": "[Bugfix] batch_invariant: keep non-AllReduce collectives enabled on NCCL >= 2.31",
      "pr_body": "## Purpose\n\nUnder `VLLM_BATCH_INVARIANT=1`, `override_envs_for_invariance()` sets `NCCL_ALGO=allreduce:tree` and `NCCL_PROTO=Simple`. On NCCL 2.31 that pair leaves every collective other than AllReduce with no algorithm, and any TP>1 or PP>1 engine fails at warmup:\n\n```\nNo algorithm/protocol nor symKernelId available for function Broadcast with datatype ncclInt8.\nNCCL_ALGO was set to allreduce:tree. NCCL_PROTO was set to Simple.\ntorch.distributed.DistBackendError: NCCL error ... invalid usage\n```\n\n(TP fails on the pynccl `ncclAllGather` in the logits gather, PP on `torch.distributed.broadcast` in `pp_utils.py`.)\n\nNCCL 2.31 zero-fills the per-collective algorithm table whenever `NCCL_ALGO` is set and re-enables only the collectives the string names (`src/tuning/cost_model.cc`, `ncclTuningCostModelInit`). The zeroed rows are consulted only for collectives NCCL marks \"forced\", and the prefix-less `NCCL_PROTO=Simple` forces all of them. NCCL 2.30 left unnamed collectives enabled, which this setting relied on. PyPI `nvidia-nccl-cu12` is already 2.31.2, so this bites as soon as the torch pin moves.\n\n## Change\n\n`NCCL_ALGO=\"ring,tree;allreduce:tree\"`: re-enable Ring and Tree for every collective, then pin AllReduce to Tree. Same selection as before on NCCL 2.30 (Ring for Broadcast/AllGather/ReduceScatter, Tree for AllReduce), so determinism is unaffected.\n\n## Test Plan\n\n2-GPU `torch.distributed` probe, one collective per process, under the exact env set of `override_envs_for_invariance()`:\n\n| NCCL | env | allreduce | broadcast | allgather | reducescatter |\n|---|---|---|---|---|---|\n| 2.31.2 | current (`allreduce:tree`) | OK | FAIL | FAIL | FAIL |\n| 2.31.2 | this PR | OK | OK | OK | OK |\n| 2.30.7 | current | OK | OK | OK | OK |\n| 2.30.7 | this PR | OK | OK | OK | OK |\n\nIdentical on H100-class (sm90), GB200 (sm100) and GB300 (sm103). Leave-one-out over the env set confirms `NCCL_ALGO` + `NCCL_PROTO` is the necessary and sufficient pair.\n\n## Test Result\n\nEnd to end with `vllm serve` (Llama-3-8B bf16, `--enforce-eager`, `VLLM_BATCH_INVARIANT=1`) on GB200 with NCCL 2.31.2: TP=2 and PP=2 both die at warmup on the current code and both come up and answer `/v1/completions` with this change (same node, same model, patch as the only difference).\n\nSide note, not changed here: NCCL rejects `NCCL_NTHREADS=1` (`must be a multiple of 32`) and ignores it.\n"
    },
    {
      "sha": "c024cfdb02e32ef5c804d31b28846003894276aa",
      "pr_number": 57386,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57386",
      "pr_title": "[Fast Start] Support data parallelism in the weight cache daemon ",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\n\r\nThe daemon rejected `--data-parallel-size > 1`, so DP + EP decode fleets  could not use ipc_cache. With EP the expert shard a GPU holds depends on its DP rank, so the daemon must reproduce the engine's placement.\r\n\r\n## Test Plan\r\n\r\n## Test Result\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "ca831d1c55714aa10acd826389674ce8f6a4f872",
      "pr_number": 57435,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57435",
      "pr_title": "[ROCm][DSv4.1][Perf] Fuse the inverse RoPE into the sparse decode reduce",
      "pr_body": "## Purpose\r\n\r\nThe ROCm sparse decode writes the attention output twice. The reduce kernel\r\ncombines the split-K partials and stores `[T, H, 512]`, and then\r\n`_inverse_rope_gptj_kernel` reads that whole tensor back, rotates the trailing\r\n64 rope lanes and writes it out again — a full round trip per layer, for an\r\nepilogue on data the reduce already held in registers.\r\n\r\nFrom a 10-step decode trace on MI355X (TP4, rank 0):\r\n\r\n| kernel | total | calls | share of kernel time |\r\n|---|---|---|---|\r\n| `_sparse_attn_decode_gfx950_partial_kernel` | 4707.3 us | 430 | 3.24% |\r\n| `_sparse_attn_decode_reduce_kernel` | 2214.2 us | 430 | 1.52% |\r\n| `_inverse_rope_gptj_kernel` | 1965.5 us | 430 | 1.35% |\r\n\r\n430 = 43 layers x 10 steps: every layer pays it, sliding-window and MTP layers\r\nincluded.\r\n\r\nFlashMLA's mega-attention kernel already folds this rotation and the FP8 cast\r\ninto its epilogue on SM100 — `flash_mla_mega_attn.py` describes exactly that —\r\nand the layer interface for it is platform-generic (`_alloc_attn_out` /\r\n`_o_proj`, `accepts_unnormed_unroped_query`). The ROCm Triton path just never\r\ntook it.\r\n\r\n## What changes\r\n\r\nThe reduce gained an optional inverse-RoPE epilogue. Reshaping the combined row\r\ninto adjacent pairs puts the rope lanes in the last 32 of 256 pairs, so giving\r\nthe NoPE pairs cos=1 and sin=0 lets one expression cover the whole row with no\r\nbranch and no second pass:\r\n\r\n```python\r\neven, odd = tl.split(tl.reshape(out, (BLOCK_H, COMB_DIM // 2, 2)))\r\nout = tl.reshape(tl.join(even * cos + odd * sin, odd * cos - even * sin), ...)\r\n```\r\n\r\nWhich rows still owe a rotation depends on the batch: the reduce rotates the\r\ndecode rows it produces, prefill rows have no such epilogue, and a decode path\r\nthat does not fuse (for instance the opt-in HIP kernel in #57282) rotates\r\nnothing. `rocm_sparse_attn_decode` reports how many leading rows it rotated and\r\n`forward_mqa` pays the standalone pass for the rest, in place. On a decode-only\r\nstep that leaves nothing to do, so the kernel does not launch at all rather\r\nthan being replaced by a copy.\r\n\r\nThat bookkeeping belongs in the eager attention segment specifically, not in\r\n`_o_proj`. An earlier revision of this PR handed the row count to `_o_proj`,\r\nwhich runs inside the compiled region; since vLLM compiles with\r\n`skip_all_guards_unsafe`, the Python int froze at its trace-time value and rows\r\nended up rotated zero or two times. GSM8K fell to 0.087 against a 0.902\r\nbaseline with fluent output and no error of any kind. The lane arithmetic was\r\ncorrect throughout, so no kernel-level test caught it -- only the eval did.\r\n\r\nTwo side effects worth noting: the rotation now happens on the fp32\r\naccumulator rather than the already-rounded bf16 output, dropping a rounding\r\nstep, and the NoPE lanes come through bit-exact (x1.0 + 0.0).\r\n\r\nExpected effect: ~196 us and 43 launches per step removed, ~1.35% of decode\r\nGPU time on DeepSeek-V4.1-Flash.\r\n\r\n## Not a duplicate\r\n\r\n- #54894 fuses inverse RoPE with group quantization via AITER's\r\n  `inverse_rope_group_quant` — different model (`deepseek_v4`), different\r\n  mechanism, and it keeps the rotation as its own pass. This removes the pass.\r\n  The two are compatible in direction: if the V4.1 ROCm path later adopts an\r\n  FP8 `wo_a`, the natural next step is emitting the FP8 cast from this same\r\n  epilogue, which is what the SM100 kernel does.\r\n- #57282 adds an opt-in HIP decode kernel for a specific batch range and falls\r\n  through to this Triton path otherwise; the two compose, and the rotated-row\r\n  count above is what keeps them honest.\r\n- #56720 and #56342 touch other kernels in the same file, not the reduce.\r\n\r\n## Test results\r\n\r\nUnit tests, on the branch, MI355X:\r\n\r\n```\r\n$ pytest tests/kernels/attention/test_rocm_triton_attn_dsv4.py \\\r\n         tests/kernels/attention/test_dsv41_rocm_fused_inv_rope.py -q\r\n69 passed in 38.64s\r\n```\r\n\r\nKernel equivalence, MI355X: running the real decode kernels fused, then\r\nunfused followed by `_fused_inverse_rope_gptj`, on identical inputs. NoPE lanes\r\nare bit-exact and rope lanes agree to bf16 rounding at every shape tried --\r\n`(queries, heads)` of (2, 3), (1, 128), (8, 128), (32, 128):\r\n\r\n| | max abs diff |\r\n|---|---|\r\n| NoPE lanes | 0.0 (32 queries: 4.9e-4) |\r\n| rope lanes | <= 2.0e-3, against \\|rope\\| up to 0.29 |\r\n\r\nDecode capture, DeepSeek-V4.1-Flash TP4 on MI355X, 10 engine steps at\r\nconcurrency 1, MTP spec decode. Per-step kernel time, mean over the four TP\r\nranks:\r\n\r\n| kernel | main | this PR |\r\n|---|---|---|\r\n| `_inverse_rope_gptj_kernel` | 201.6 us | 0.0 us (430 launches -> 0) |\r\n| `_sparse_attn_decode_reduce_kernel` | 218.7 us | 227.0 us |\r\n\r\n<img width=\"3338\" height=\"1314\" alt=\"image\" src=\"https://github.com/user-attachments/assets/084dd8aa-0b25-421e-8757-e445a5402307\" />\r\n\r\nThe pass disappears on decode-only steps and the reduce absorbs it for 8.3 us,\r\nso 193 us per step net, against the 196 us the trace analysis predicted.\r\n\r\nAccuracy, GSM8K 5-shot, greedy, concurrency 32 score remains the same\r\n\r\n---\r\n\r\nAI assistance was used to write this change. I have reviewed every line, ran\r\nthe tests and evals above myself, and am responsible for it.\r\n"
    },
    {
      "sha": "72675c4706e4c158a3e363b261370d06ad02c87f",
      "pr_number": 58136,
      "pr_url": "https://github.com/vllm-project/vllm/pull/58136",
      "pr_title": "[Bugfix][ROCm] Dispatch the QuantFP8 CUDA fallback on the class",
      "pr_body": "## Purpose\r\n\r\nThree tests in `tests/kernels/test_concat_mla_q.py` fail in the\r\n`(MI355) Core Operation Kernels` job:\r\n\r\n```\r\nFAILED test_concat_mla_q_fp8_nope_and_rope[True-0-False] - IndexError: tuple index out of range\r\nFAILED test_concat_mla_q_fp8_nope_and_rope[True-0-True] - IndexError: tuple index out of range\r\nFAILED test_concat_mla_q_fp8_nope_and_rope[True-64-False] - IndexError: tuple index out of range\r\n```\r\n\r\nFailing run:\r\nhttps://buildkite.com/vllm/ci/builds/90376/list?jid=01a0c7db-41fd-4aeb-9b8d-8509eee8af45&tab=output\r\n\r\n`QuantFP8.forward_hip` falls back to the CUDA implementation through\r\n`self.forward_cuda(...)`. `_DecodeConcatQuantFP8` overrides `forward_cuda` with\r\na different signature, taking the two halves of the decode query, so the\r\nvirtual call re-enters the subclass with every argument shifted by one and\r\n`decode_q_pe` ends up holding the 0-d scale tensor.\r\n\r\nThe path is not new, only newly tested: AITER returns before the fallback is\r\nreached, and #57458 added the first test that exercises it with AITER off.\r\nFixed by dispatching the fallback on the class instead of on `self`, which is\r\nwhat the existing comment already intends.\r\n\r\n## Test Plan\r\n\r\n`pytest tests/kernels/test_concat_mla_q.py` on MI355X (gfx950), both with AITER\r\noff, as the job runs it, and with `VLLM_ROCM_USE_AITER=1`.\r\n\r\n## Test Result\r\n\r\nBefore: 3 failed, 35 passed. After: 38 passed. With AITER enabled the file\r\npasses either way, so the fast path is unchanged.\r\n\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "74370a0e3044c16b6ba58f9c144b02ab733cd73a",
      "pr_number": 43462,
      "pr_url": "https://github.com/vllm-project/vllm/pull/43462",
      "pr_title": "[Bugfix] hadacore_transform: respect inplace parameter to fix garbage outputs with QuIP transforms",
      "pr_body": "## Purpose\r\n\r\nFix garbage outputs from QuIP transforms on Blackwell GPUs under `torch.compile` + CUDA graph capture. Initially observed on RTX 5090 (SM120); since reproduced on B200 (SM100) with the v0.21.0 prebuilt wheel + torch 2.11.0. Addresses the runtime side of vllm-project/llm-compressor#2006.\r\n\r\n## Background\r\n\r\nThe `hadacore_transform` op is declared with a mutable schema:\r\n\r\n```cpp\r\nops.def(\"hadacore_transform(Tensor! x, bool inplace) -> Tensor\");\r\n```\r\n\r\nTwo interacting issues:\r\n\r\n**1. Kernel ignores the `inplace` parameter (latent bug).** In `csrc/libtorch_stable/quantization/hadamard/hadacore/hadamard_transform_cuda.cu`, the kernel always writes to `x.data_ptr()` regardless of `inplace`:\r\n\r\n```cpp\r\ntorch::stable::Tensor out = inplace ? x : torch::stable::empty_like(x);\r\nhadacore::run_fht<SCALAR_TYPE>(x.data_ptr(), x.data_ptr(), x.numel(), ...);\r\n// `inplace=False` callers receive `out`, but `out` was never written to\r\nreturn torch::stable::reshape(out, res_shape);\r\n```\r\n\r\nThe conditional copy-back cannot fire because `out` is only aliased to `x` when `inplace=True`. The path is currently unused in vLLM but is publicly exposed.\r\n\r\n**2. Mutable schema causes inductor to elide protective clones.** In `CompressedTensorsLinearTransformMethod.apply()`, the `.clone()` introduced in #26849 for compile robustness:\r\n\r\n```python\r\nx[:, start:start+length] = self.output_transform(\r\n    x[:, start:start+length].clone(), part_id=part_id\r\n)\r\n```\r\n\r\nInductor analyzes the `Tensor!` schema, decides the clone is redundant (the op will mutate anyway), and elides it from the FX graph (verified via `TORCH_LOGS=\"output_code\"`). The kernel ends up receiving a slice view aliasing `x`, producing garbage outputs under `torch.compile` + cudagraph. Initially observed on SM120; later also reproduced on B200 (SM100) with the v0.21.0 prebuilt wheel + torch 2.11.0 (QuIP variants: WikiText-2 PPL ~1.4e6; coherent with `enforce_eager=True`). @mgoin's `gsm8k_eval` in #28748 (0.697) used a combination that does not trigger the inductor elision: whether the bug manifests depends on the (torch version x compile/cudagraph) configuration, not the hardware.\r\n\r\n| Test | Hardware | Pre-fix | Post-fix |\r\n|---|---|---|---|\r\n| W4A16 + QuIP, compile | RTX 5090 | ❌ Garbage | ✅ Coherent |\r\n| NVFP4A16 + QuIP, compile | RTX 5090 | ❌ Garbage | ✅ Coherent |\r\n| NVFP4A16 only, compile | RTX 5090 | ✅ Coherent | ✅ Byte-identical |\r\n| NVFP4A16 + QuIP, cudagraph, v0.21.0 wheel + torch 2.11 | B200 | ❌ Garbage (PPL ~1.4e6) | N/A |\r\n| Same, `enforce_eager=True` | B200 | ✅ Coherent (6.84) | N/A |\r\n| Kernel sanity (`inplace=True` vs `False`) | B200 | N/A | ✅ max diff 0.0 |\r\n| `gsm8k_eval` @mgoin SpinQuant model | B200 | 0.697 (#28748) | 0.697 |\r\n\r\n## Changes\r\n\r\nKernel writes to `out.data_ptr()` instead of `x.data_ptr()` (respects `inplace`); call sites in `module.py` pass `inplace=False` to opt into the non-aliasing path. The existing `.clone()` from #26849 is retained as a defensive layer.\r\n\r\n## Reproduction\r\n\r\nThe bug manifests on any compressed-tensors checkpoint using online Hadamard transforms (QuIP) under vLLM's default `torch.compile` + cudagraph execution. The root cause is in the kernel, independent of the calibration path. It also blocks the MXFP4 W4A4 + MR-GPTQ path (where rotations contribute ~+3 pts PPL recovery vs GPTQ alone on Llama-3.1-8B) and any future recipe relying on online Hadamard rotations.\r\n\r\n### Step 1: produce a QuIP-transformed checkpoint\r\n\r\n```bash\r\ngit clone https://github.com/vllm-project/llm-compressor\r\ncd llm-compressor\r\npip install -e .\r\n\r\n# QuIP + GPTQ + NVFP4A16 example from vllm-project/llm-compressor#2751 (~10 min):\r\ngh pr checkout 2751\r\npython examples/transform/nvfp4_quip_gptq_example.py\r\n# produces Llama-3.1-8B-quip-gptq-nvfp4a16\r\n```\r\n\r\nThe attached logs were produced with an equivalent minimal datafree variant (`examples/transform/quip_example.py` adapted to the NVFP4A16 scheme, on Llama-3.1-8B-Instruct, producing `Llama-3.1-8B-Instruct-quip-nvfp4a16`). Both recipes trigger the same bug.\r\n\r\n### Step 2: reproduce the failure on `main`\r\n\r\n```bash\r\n# vLLM main branch build (per-commit nightly wheel; the version string embeds the main commit)\r\npip install vllm==0.22.1rc1.dev255+g4dcd10eb0 --extra-index-url https://wheels.vllm.ai/nightly\r\n\r\nVLLM_USE_FLASHINFER_SAMPLER=0 python validate_nvfp4_quip.py <checkpoint_dir>\r\n```\r\n\r\n(`VLLM_USE_FLASHINFER_SAMPLER=0` works around an unrelated FlashInfer sampler init failure on SM120 with this build; it does not affect the transform path.)\r\n\r\n`validate_nvfp4_quip.py` is a minimal 5-prompt sanity script (greedy, `max_tokens=80`, vLLM defaults, no `enforce_eager`):\r\n\r\n<details>\r\n<summary>validate_nvfp4_quip.py</summary>\r\n\r\n```python\r\nimport sys\r\n\r\nfrom vllm import LLM, SamplingParams\r\n\r\nCHECKPOINT_DIR = (\r\n    sys.argv[1] if len(sys.argv) > 1 else \"Llama-3.1-8B-Instruct-quip-nvfp4a16\"\r\n)\r\n\r\nPROMPTS = [\r\n    \"The capital of France is\",\r\n    \"Quantum mechanics is the branch of physics that\",\r\n    \"Once upon a time, in a distant kingdom,\",\r\n    \"def fibonacci(n):\\n    \",\r\n    \"Translate to French: 'Hello, how are you today?'\\nFrench:\",\r\n]\r\n\r\nSAMPLING = SamplingParams(max_tokens=80, temperature=0.0)\r\n\r\n\r\ndef main() -> None:\r\n    print(f\"Loading checkpoint: {CHECKPOINT_DIR}\")\r\n    model = LLM(model=CHECKPOINT_DIR, dtype=\"auto\")\r\n\r\n    print(f\"\\nRunning {len(PROMPTS)} sanity generations...\\n\")\r\n    outputs = model.generate(PROMPTS, SAMPLING)\r\n\r\n    for i, output in enumerate(outputs):\r\n        print(f\"=== Prompt {i + 1}/{len(PROMPTS)} ===\")\r\n        print(f\"PROMPT: {output.prompt!r}\")\r\n        print(f\"OUTPUT: {output.outputs[0].text!r}\")\r\n        print()\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()\r\n```\r\n\r\n</details>\r\n\r\n**Failure on `main` @ `4dcd10eb0` (RTX PRO 6000 / SM120, torch 2.11.0, engine `v0.22.1rc1.dev255+g4dcd10eb0`):**\r\n\r\n<details>\r\n<summary>Garbage output (NVFP4A16 + QuIP) on main</summary>\r\n\r\n```text\r\n=== Prompt 1/5 ===\r\nPROMPT: 'The capital of France is'\r\nOUTPUT: 'inousMainFrameublangينهift sweepsăngalseroGaplugpletσί้าวorneềnatomliteatoreCtrls...'\r\n```\r\n\r\nAll 5 prompts garbage. Full log (engine version visible in the log): `validate_quip_nvfp4a16_MAIN_GARBAGE.log` (attached).\r\n\r\n</details>\r\n\r\nNotably, on this main build, dynamo itself now flags the aliasing this PR fixes:\r\n\r\n```text\r\nUserWarning: _C::hadacore_transform (with implementation in ???): The output of this custom\r\noperator (1) must not also be an input to this custom operator and (2) may not alias any inputs\r\nto this custom operator or other returns. [...] This is deprecated and will become an error in\r\nPyTorch 2.12.\r\n```\r\n\r\nSame failure also reproduced on the `v0.22.1` release (`g0decac0d9`), and previously on `v0.21.0`: RTX 5090 (`validate_quip_nvfp4a16_GARBAGE.log`, `validate_quip_w4a16_GARBAGE.log`, attached) and B200 SM100 with the prebuilt wheel + torch 2.11.0 (WikiText-2 PPL ~1.4e6; coherent 6.84 with `enforce_eager=True`). The trigger is the (torch version x compile/cudagraph) combination, not the hardware alone.\r\n\r\n## Test Plan\r\n\r\nSame command, with this PR's commit cherry-picked onto the **same main commit** (the fix touches `csrc/`, so a full build is required):\r\n\r\n```bash\r\ngit clone https://github.com/vllm-project/vllm && cd vllm\r\ngit checkout 4dcd10eb0  # same main commit as the failing run above\r\ngit fetch https://github.com/Yatimai/vllm.git fix/hadacore-respect-inplace\r\ngit cherry-pick 9867bb81d658787e31c7fce0290cce173e40c742\r\nTORCH_CUDA_ARCH_LIST=12.0 pip install -e . --no-build-isolation\r\n\r\nVLLM_USE_FLASHINFER_SAMPLER=0 python validate_nvfp4_quip.py <checkpoint_dir>\r\n```\r\n\r\nOnly delta between the two runs is this PR's 3-line change (same GPU, same checkpoint, same command, same base commit).\r\n\r\n## Test Result\r\n\r\n**On `main` @ `4dcd10eb0` + this fix (engine `v0.22.1rc1.dev256+g1ea902a7c`, same GPU / checkpoint / command):**\r\n\r\n<details>\r\n<summary>Coherent output (NVFP4A16 + QuIP) on main + fix</summary>\r\n\r\n```text\r\n=== Prompt 1/5 ===\r\nPROMPT: 'The capital of France is'\r\nOUTPUT: ' Paris, and it is also the most visited city in the world. Paris is known for its beautiful architecture, art museums, and fashion...'\r\n```\r\n\r\nAll 5 prompts coherent (incl. correct fibonacci code and French translation). Full log: `validate_quip_nvfp4a16_BRANCH_OK.log` (attached).\r\n\r\n</details>\r\n\r\nEarlier validation on v0.21-era builds (RTX 5090): `validate_cppFIX_nvfp4a16_OK.log`, and the W4A16 variant `validate_cppFIX_w4a16_OK.log` (attached).\r\n\r\n**No-regression checks:**\r\n\r\n- NVFP4A16 *without* QuIP, compile on: byte-identical outputs pre/post fix (`validate_cppFIX_noquip_OK.log`)\r\n- Kernel sanity `inplace=True` vs `inplace=False` on B200 (torch 2.11.0+cu128, sm_100): max diff = 0.0 across `had_size` 128/256/1024 (`kernel_sanity_b200_OK.log`)\r\n- `gsm8k_eval` on @mgoin's SpinQuant model (B200): 0.697, matching #28748 (`gsm8k_b200_0.697_OK.log`)\r\n\r\n**Attached logs:**\r\n\r\n<!-- drop the 2 new .log files (MAIN_GARBAGE + BRANCH_OK) on the line below -->\r\n[validate_quip_nvfp4a16_MAIN_GARBAGE.log](https://github.com/user-attachments/files/28688616/validate_quip_nvfp4a16_MAIN_GARBAGE.log)\r\n[validate_quip_nvfp4a16_BRANCH_OK.log](https://github.com/user-attachments/files/28688618/validate_quip_nvfp4a16_BRANCH_OK.log)\r\n[validate_quip_nvfp4a16_GARBAGE.log](https://github.com/user-attachments/files/28668111/validate_quip_nvfp4a16_GARBAGE.log)\r\n[validate_quip_w4a16_GARBAGE.log](https://github.com/user-attachments/files/28668115/validate_quip_w4a16_GARBAGE.log)\r\n[validate_cppFIX_nvfp4a16_OK.log](https://github.com/user-attachments/files/28668119/validate_cppFIX_nvfp4a16_OK.log)\r\n[validate_cppFIX_w4a16_OK.log](https://github.com/user-attachments/files/28668124/validate_cppFIX_w4a16_OK.log)\r\n[validate_cppFIX_noquip_OK.log](https://github.com/user-attachments/files/28668131/validate_cppFIX_noquip_OK.log)\r\n[kernel_sanity_b200_OK.log](https://github.com/user-attachments/files/28668136/kernel_sanity_b200_OK.log)\r\n[gsm8k_b200_0.697_OK.log](https://github.com/user-attachments/files/28668145/gsm8k_b200_0.697_OK.log)\r\n\r\n## Related\r\n\r\n- vllm-project/llm-compressor#2006: original symptom report (@mratsim)\r\n- #26849: prior `.clone()` fix (@kylesayrs), insufficient when inductor elides the clone\r\n- #28748: build coverage for Blackwell (@mgoin)\r\n- #28747: B200 build issue (@eldarkurtic)\r\n- #22486: original online Hadamard rotations support (@kylesayrs)\r\n\r\n"
    },
    {
      "sha": "a6131b0e656e18eeb3797b8856aa3dbc69efd8e5",
      "pr_number": 57914,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57914",
      "pr_title": "[Bugfix][Engram] Fall back when /dev/shm is absent before sharing tables",
      "pr_body": "Follow-up to #57651. `can_share_engram_tables` called `shutil.disk_usage(\"/dev/shm\")` unguarded, so a host without `/dev/shm` raised `FileNotFoundError` at model construction instead of falling back to per-rank tables. This checks `os.path.isdir(SHM_PATH)` first and reuses `SHM_PATH` from `shm_broadcast` for the remaining `/dev/shm` references. No duplicate PR covers this. AI assistance (Claude Code) was used.\r\n\r\nAlso flattens `DPSharedEngramStorage._allocate` while touching it: the nested try/except, the `stage` tracker and the manual `finalizer()`/`mapping.close()` rollback become one try/except around the mmap+register step. On a peer failure the locals are dropped (`del tensor, owner, mapping`), so the owner's finalizer unregisters the pages and the mapping unmaps in the same order as before. Error messages carry the exception text instead of a stage prefix. No behavior change.\r\n\r\nTest: `pytest tests/distributed/test_engram_dp_shard.py tests/test_config.py -k \"shm or shared_memory or engram\"` -> passed\r\nTest: `pytest tests/distributed/test_engram_dp_shard.py -k shared_across_dp_replicas` (4x GB200, covers the create/open/mmap/register/pinned failure injections and the mapping lifetime checks) -> 2 passed on the flattened `_allocate`\r\n\r\n### Model evaluation\r\n\r\nEvaluated with `deepseek-ai/DeepSeek-V4.1-Flash` at revision `dba1be0a40aa45a94ad051997016db3960a90277` on one 4x GB200 node using DEP4 (`TP1 x DP4`, expert parallel) and DSpark with 5 speculative tokens and adaptive verification. MQA GSM8K 5-shot ran through the Completions API with `temperature=0`, `max_tokens=1024`, concurrency 32, and all 1,319 examples. It achieved `0.9325` flexible exact match and `0.9325` strict exact match with 0 request errors in 107.46 seconds. `dp_shared_memory=True` initialized successfully, and both Engram tables were shared across all four DP replicas (`94.42 GiB` per table/layer).\r\n"
    },
    {
      "sha": "066ee911975133dc148433ed9e65061b4587c969",
      "pr_number": 56579,
      "pr_url": "https://github.com/vllm-project/vllm/pull/56579",
      "pr_title": "[Bugfix][Attention] Avoid NaN in the Triton softcap for large attention logits",
      "pr_body": "## Purpose\r\n\r\nFixes #56578 \r\n\r\n`apply_softcap` computed `x * tanh(S / x)` as `x * (exp(S/x) - exp(-S/x)) / (exp(S/x) + exp(-S/x))`. Both exponentials overflow to `inf` when `|S / x| > ~88`, so the ratio becomes `inf / inf = NaN` and the whole attention row is poisoned.\r\n\r\nThis is reachable for Gemma-2 style models that set `attn_logit_softcapping = 50`: a pre-softcap score above 4400 overflows. These models have very large attention logits, which is why they use softcapping. Verified on a GTX 1650 Ti with the real helper:\r\n\r\n```text\r\ninput  : [1.0, 100.0, 1000.0, 3000.0, 5000.0, 10000.0, -1.0, -10000.0, 4400.0]\r\nbefore : [0.9999, 48.2, 50.0, 50.0, NaN, NaN, -0.9999, NaN, inf]\r\nafter  : [0.9999, 48.2, 50.0, 50.0, 50.0, 50.0, -0.9999, -50.0, 50.0]\r\n```\r\n\r\nThe fix computes the same value as `1 - 2 / (exp(2 * S / x) + 1)`, which saturates cleanly at `+/-1` for either sign. The helper is shared by the unified, DiffKV, and INT4 attention kernels.\r\n\r\n## Test Plan\r\n\r\n```bash\r\npytest tests/kernels/attention/test_triton_unified_attention.py -k softcap_does_not_overflow_on_large_scores\r\npytest tests/kernels/attention/test_triton_unified_attention.py -k \"50.0 and not fp8 and not q_dtype1\"\r\n```\r\n\r\n## Test Result\r\n\r\n- New test fails on main without the fix: `NaN` for the large scores.\r\n- With the fix: the new test passes.\r\n- 336 softcap tests pass. The `fp8` cases fail only on SM75, where Triton reports `fp8e4nv` is unsupported."
    }
  ]
}