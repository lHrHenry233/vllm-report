{
  "date": "2026-09-21",
  "repo": "vllm-project/vllm",
  "fetched_at": "2026-09-22T19:06:15.862302+08:00",
  "commits": [
    {
      "sha": "8c0825090df8b7939a92ff22b02bc7d03cb3605a",
      "pr_number": 57389,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57389",
      "pr_title": "[Bugfix][NIXL] Fix DCP pulls across MLA cache regions",
      "pr_body": "When prefill and decode use different cache-group layouts, as with a HiSparse decoder, NIXL matches their KV blocks by memory region. That receive path currently assumes a single source rank and fails when a DCP prefiller supplies multiple ranks.\n\nCreate a read for each source rank using the existing region mapping. For each region, reuse `_apply_dcp_prefix_caching` to select the pages owned by that rank and skip the decoder's cached prefix. Bound the reads by the sequence length and zero any remaining allocation padding. The ordinary DCP path stays unchanged. Requires unshared MLA regions with matching logical/kernel page sizes.\n\nIndependent of #55398's GPU-landing feature; #57077 covers DCP1 only.\n\n### Validation\n\n```bash\n.venv/bin/python -m pytest tests/v1/kv_connector/unit/test_nixl_connector_hma.py -m cpu_test -q\npre-commit run --files vllm/distributed/kv_transfer/kv_connector/v1/nixl/pull_worker.py vllm/distributed/kv_transfer/kv_connector/v1/nixl/base_worker.py tests/v1/kv_connector/unit/test_nixl_connector_hma.py\n```\n\n- 97 CPU tests passed (CpuPlatform wrapper); all 6 regressions fail on main. Pre-commit passed.\n- 1,888 valid DCP mappings match the previous implementation.\n- H200 E2E passed on `8efdab56` (Sep 21): PCP8/DCP8/EP8 → TP8 HiSparse, 512/384/128 GiB budgets, MTP3. Exact 8K/32K retrieval; 48/48 P/D and 48/48 decoder-local answers correct; 6,144 NIXL transfers, zero failures; both CPU restores and all four benchmark points passed. Predates main merge `3bf7b0fa`; the three fix files are unchanged by that merge.\n\nAI assistance: OpenAI Codex. Draft for human review.\n"
    },
    {
      "sha": "6b858751f6e99c4f33913657dc0c2afd34ff13e6",
      "pr_number": 57958,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57958",
      "pr_title": "[docs] Fix legacy hf CLI references (vllm)",
      "pr_body": "\r\nHey, maintainer of `huggingface_hub` library here. Just a small cleanup PR if you could review it @hmellor please? :pray: \r\n\r\nA few references are no longer the recommended `huggingface_hub` usage in 1.x: `huggingface-cli` was replaced by `hf`, and the `[cli]` extra was dropped in 1.0 (so `huggingface-hub[cli]` already resolves with a \"does not provide the extra\" warning today). The surrounding code blocks already use `hf download` / `hf auth login`.\r\n\r\n- `docs/usage/troubleshooting.md`: link text `huggingface-cli` → `` `hf` CLI ``\r\n- `docs/models/supported_models.md`: dead anchor `#huggingface-cli-download` → `#hf-download`\r\n- `docker/Dockerfile.rocm`, `docker/Dockerfile.rock`, `docker/Dockerfile.rocm_gfx1250` and `docs/getting_started/installation/gpu.rocm.inc.md`: `huggingface-hub[cli]` → `huggingface-hub`\r\n\r\nDocs and install lines only, no code change.\r\n\r\nFor context: https://github.com/huggingface/huggingface_hub/pull/4955\r\n\r\n🤖 Generated with [Claude Code](https://claude.com/claude-code)"
    },
    {
      "sha": "dc479a2d839d5c474b36ac521dc5a72294bcb186",
      "pr_number": 53743,
      "pr_url": "https://github.com/vllm-project/vllm/pull/53743",
      "pr_title": "[Bugfix] Fix external LB DP rank handling when replicas share nodes",
      "pr_body": "stack two fixes together\r\n1# Fix ZeroDivisionError with external LB when DP size exceeds node count in multi-node setups\r\n2# Fix external LB rank inference on shared nodes https://github.com/vllm-project/vllm/pull/54694\r\n\r\n## Purpose\r\n\r\n`ParallelConfig.nnodes_within_dp` reports how many nodes a single DP replica\r\nspans, computed as `nnodes // (data_parallel_size // data_parallel_size_local)`.\r\n\r\nThe formula assumes `data_parallel_size_local` describes DP ranks per node. That\r\nholds for internal LB, where it is inferred — and the inference already guards\r\nthe same floor-rounding hazard (`arg_utils.py:2144`):\r\n\r\n```python\r\nself.data_parallel_size_local = max(local_world_size // world_size_within_dp, 1)\r\n```\r\n\r\nExternal LB instead pins it to 1 for its one-engine-per-process model\r\n(`arg_utils.py:2183`), so the denominator becomes the full DP size and the ratio\r\nfloors to **0** whenever replicas outnumber nodes.\r\n\r\n## Where it divides by zero\r\n\r\nTwo properties in `ParallelConfig` use the value as a divisor:\r\n\r\n```python\r\n# vllm/config/parallel.py:742\r\ndef node_rank_within_dp(self) -> int:\r\n    return self.node_rank % self.nnodes_within_dp\r\n\r\n# vllm/config/parallel.py:755\r\ndef local_world_size(self) -> int:\r\n    return self.world_size // self.nnodes_within_dp\r\n```\r\n\r\n`MultiprocExecutor._get_parallel_sizes` reaches both before any worker is\r\nspawned (`vllm/v1/executor/multiproc_executor.py:279`):\r\n\r\n```python\r\nassert self.world_size % self.parallel_config.nnodes_within_dp == 0, (...)\r\nself.local_world_size = self.parallel_config.local_world_size\r\n```\r\n\r\nThe assert raises `ZeroDivisionError` rather than `AssertionError`; under\r\n`python -O` it is stripped and the next line raises instead. Startup aborts in\r\nthe executor, before `Worker.init_device()` runs, so the fix belongs in\r\n`nnodes_within_dp` itself rather than in each caller."
    },
    {
      "sha": "e34685dfc0c9f46ae58650fe7b08cc89b615a5fe",
      "pr_number": 52362,
      "pr_url": "https://github.com/vllm-project/vllm/pull/52362",
      "pr_title": "[ROCm][DSv4] Enable DSpark adaptive verification",
      "pr_body": "## Purpose\r\n\r\nEnable DSpark confidence-scheduled adaptive verification for DeepSeek-V4 on ROCm. Developed with contributions from [@larryli2-amd](https://github.com/larryli2-amd), who added explicit missing-confidence-head error handling and provided the MI350X performance validation.\r\n\r\nThis change:\r\n\r\n- adds AMD confidence-head construction, inference, checkpoint remapping, and safe fallback when a checkpoint has no confidence head\r\n- enables variable-length FULL graph support in the DeepSeek-V4 ROCm sparse MLA and sparse SWA metadata builders when adaptive verification is enabled, while retaining their existing support classification otherwise\r\n- extends the shared DeepSeek indexer flattening capability to the ROCm DeepSeek-V4 path, deriving adaptive request ownership from device query boundaries\r\n- adds focused confidence-loading and graph-replay regression coverage\r\n\r\nAdaptive verification can redistribute a fixed draft-token budget between requests on device, so CPU request boundaries can differ from final device boundaries. The flattened path builds request ownership, context lengths, and block-table rows from device decode lengths. It reuses the shared flattening selection introduced by [#52795](https://github.com/vllm-project/vllm/pull/52795) and keeps the ROCm capability scoped to the DeepSeek-V4 builder. NVIDIA and unrelated ROCm indexers are unchanged.\r\n\r\nThe ROCm sparse MLA/SWA declarations are similarly scoped. Their ragged metadata is device-boundary-derived and copied into persistent buffers before FULL graph replay.\r\n\r\nAI assistance was used in preparing this PR.\r\n\r\n## Test plan\r\n\r\n- changed-file pre-commit, including Ruff and mypy for Python 3.10–3.13;\r\n- focused AMD confidence-head, ROCm adaptive metadata, native `next_n`, and adaptive-verification tests;\r\n- complete GSM8K 5-shot evaluation for no speculation, fixed K=7, and adaptive K<=7;\r\n- SPEED-Bench throughput comparison on 8x MI350X at TP=8 and client concurrency 4/8/16/32/64/128/256.\r\n- Rocm vllm nightly ShareGPT throughput comparison on 8x MI355X at TP=8 and client concurrency 4/8/16/32/64/128/256.\r\n\r\n## Test results\r\n\r\n### Source and focused tests\r\n\r\n- rebased PR head `ea586873d5210d9e1624c42333be1b02c276c832` onto upstream `cd10ed6f9f6b37a8ace9cf380007e66fe12ec0c3`;\r\n- changed-file pre-commit passed, including Ruff and mypy for Python 3.10–3.13;\r\n- the two PR-specific test files passed (`10 passed`) and the shared adaptive-verification suite passed (`13 passed`).\r\n\r\n### Performance validation — 8x MI350X\r\n\r\nPerformance validation contributed by [@larryli2-amd](https://github.com/larryli2-amd) used 8x AMD Instinct MI350X at TP=8 with `deepseek-ai/DeepSeek-V4-Flash-DSpark`.\r\n\r\nThis PR changes only vLLM; it does not modify or vendor AITER source code. The benchmark environment used a locally rebuilt, pinned AITER wheel installed into the derived image with `--no-deps`, leaving the base PyTorch and Triton installations unchanged:\r\n\r\n- base image: `vllm/vllm-openai-rocm@sha256:72e90adf360ccffb231a098a15eadfb808719c029c6cd43f6386095064552b7e`;\r\n- AITER version: `0.1.21.post2.dev2+g804648bcb`;\r\n- AITER source: `804648bcbf87646ddef4b7add10da9c3b322567c`;\r\n- AITER wheel SHA256: `30360d15819a9c8e3ec8e4ae665683abf4c5358d5be1f49a9aa2f7962c0032db`;\r\n- PyTorch: `2.12.0+rocm10.0.0`.\r\n\r\nThe pinned wheel was used because the stock AITER post1 environment had a known GPU-fault issue in this setup.\r\n\r\nThe server used Model Runner V2, breakable CUDA graphs, async scheduling, prefix caching, FP8 KV cache, `max_num_seqs=256`, `max_num_batched_tokens=16384`, and `FULL_AND_PIECEWISE` capture through 2048. Adaptive DSpark used K<=7 and an 8192-token profiling context.\r\n\r\nThe benchmark used a frozen 880-request SPEED-Bench Throughput-1K request set at temperature 1, top-p 1, and maximum output length 2048. Concurrency ran in descending order from 256 to 4. Each point used an independent cold server boot, two 64-request warmups, matching client/server generated-token counts, zero preemptions, and no GPU faults.\r\n\r\n<img width=\"2400\" height=\"1200\" alt=\"MI350X DSpark adaptive-verification throughput results\" src=\"https://github.com/user-attachments/assets/fc56d7e4-98d0-4fed-b6d2-42741a6df05f\" />\r\n\r\n<img width=\"705\" height=\"322\" alt=\"MI350X DSpark adaptive-verification comparison\" src=\"https://github.com/user-attachments/assets/c473ac8e-f8df-4142-bd38-178fa4d2a994\" />\r\n\r\nCompared with fixed-length verification, confidence-scheduled DSpark delivered comparable performance at low concurrency and higher throughput at high concurrency. The complete serving command and environment details are preserved in the [reproduction comment](https://github.com/vllm-project/vllm/pull/52362#issuecomment-5675603735).\r\n\r\n### ROCm nightly validation — 8x MI355X\r\n\r\nThe main rerun used upstream `cd10ed6f9f6b37a8ace9cf380007e66fe12ec0c3`, PR head `ea586873d5210d9e1624c42333be1b02c276c832`, and stock-AITER image `vllm/vllm-openai-rocm:nightly-rocm100-cd10ed6f9f6b37a8ace9cf380007e66fe12ec0c3`.\r\n\r\nThe server used TP=8, FP8 KV, `max_num_seqs=256`, `max_num_batched_tokens=4096`, `max_model_len=4096`, synchronous scheduling, prefix caching disabled, and `FULL_AND_PIECEWISE` capture through 2048. Both DSpark arms used probabilistic sampling. `AITER_USE_CK_MOE_SORTING=1` avoids a stock AITER post1 sorting fault; AITER MLA/OPUS remained enabled.\r\n\r\nShareGPT output throughput, tokens/s:\r\n\r\n| Concurrency | No speculation | Fixed K=7 | Adaptive K<=7 | Adaptive vs fixed K=7 | Adaptive vs no-spec |\r\n|---:|---:|---:|---:|---:|---:|\r\n| 4 | 381.53 | 970.33 | 902.37 | -7.00% | +136.51% |\r\n| 8 | 729.20 | 1,762.59 | 1,616.77 | -8.27% | +121.72% |\r\n| 16 | 1,342.83 | 2,909.52 | 2,773.69 | -4.67% | +106.56% |\r\n| 32 | 2,392.99 | 4,756.32 | 4,501.11 | -5.37% | +88.10% |\r\n| 64 | 4,094.75 | 6,712.35 | 7,144.12 | +6.43% | +74.47% |\r\n| 128 | 6,340.78 | 8,778.87 | 10,356.48 | +17.97% | +63.33% |\r\n| 256 | 8,878.57 | 7,852.42 | 13,324.67 | +69.69% | +50.08% |\r\n\r\nEach cell used a fresh server, one readiness request, a 64-request c32 warmup, a 64-request cell-concurrency warmup, and 880 measured raw-ShareGPT requests with forced output length 512. All 21 cells completed 880/880 requests with the expected 450,560 generated tokens, no preemptions, and no GPU faults.\r\n\r\nAt c256, adaptive K<=7 reached 13,324.67 tok/s: 69.69% above fixed K=7 and 50.08% above no speculation. Adaptive trades 4.7–8.3% against fixed K=7 at c4–c32 and crosses ahead at c64.\r\n\r\nThe complete three-arm, 1,319-sample GSM8K 5-shot gate also passed:\r\n\r\n| Mode | Flexible exact match | Strict exact match |\r\n|---|---:|---:|\r\n| No speculation | `0.9484 +/- 0.0061` | `0.9484 +/- 0.0061` |\r\n| Fixed K=7 | `0.9431 +/- 0.0064` | `0.9431 +/- 0.0064` |\r\n| Adaptive K<=7 | `0.9492 +/- 0.0060` | `0.9492 +/- 0.0060` |\r\n\r\nThe score range was `0.0061`, within one reported standard error.\r\n\r\nReproduction environment and base command:\r\n\r\n```bash\r\nexport AITER_USE_CK_MOE_SORTING=1\r\nexport HIP_VISIBLE_DEVICES=0,1,2,3,4,5,6,7\r\nexport VLLM_ROCM_USE_AITER=1\r\nexport VLLM_USE_BREAKABLE_CUDAGRAPH=1\r\nexport VLLM_USE_V2_MODEL_RUNNER=1\r\n\r\nvllm serve deepseek-ai/DeepSeek-V4-Flash-0731 \\\r\n  --no-async_scheduling \\\r\n  --block_size 256 \\\r\n  --compilation_config '{\"mode\":3,\"cudagraph_mode\":\"FULL_AND_PIECEWISE\",\"max_cudagraph_capture_size\":2048}' \\\r\n  --distributed_executor_backend mp \\\r\n  --enable_auto_tool_choice \\\r\n  --no-enable_prefix_caching \\\r\n  --gpu_memory_utilization 0.9 \\\r\n  --host 0.0.0.0 \\\r\n  --kv_cache_dtype fp8 \\\r\n  --max_model_len 4096 \\\r\n  --max_num_batched_tokens 4096 \\\r\n  --max_num_seqs 256 \\\r\n  --port 8000 \\\r\n  --reasoning_config '{\"reasoning_parser\":\"deepseek_v4\",\"reasoning_start_str\":\"\",\"reasoning_end_str\":\"\"}' \\\r\n  --reasoning_parser deepseek_v4 \\\r\n  --tensor_parallel_size 8 \\\r\n  --tokenizer_mode deepseek_v4 \\\r\n  --tool_call_parser deepseek_v4 \\\r\n  --trust_remote_code\r\n```\r\n\r\nFixed K=7 appends:\r\n\r\n```bash\r\n--speculative_config '{\"method\":\"dspark\",\"num_speculative_tokens\":7,\"draft_sample_method\":\"probabilistic\",\"enable_adaptive_verification\":false}'\r\n```\r\n\r\nAdaptive K<=7 changes the final field to `\"enable_adaptive_verification\":true`.\r\n\r\nFor each concurrency, start a fresh server, run the two 64-request warmups described above, then run:\r\n\r\n```bash\r\nvllm bench serve \\\r\n  --backend vllm \\\r\n  --dataset-name sharegpt \\\r\n  --dataset-path \"$SHAREGPT_PATH\" \\\r\n  --disable-shuffle \\\r\n  --ignore-eos \\\r\n  --request-rate inf \\\r\n  --max-concurrency \"$CONCURRENCY\" \\\r\n  --num-prompts 880 \\\r\n  --sharegpt-output-len 512 \\\r\n  --temperature 1.0 \\\r\n  --seed 20260902 \\\r\n  --model deepseek-ai/DeepSeek-V4-Flash-0731 \\\r\n  --tokenizer-mode deepseek_v4 \\\r\n  --trust-remote-code\r\n```\r\n\r\nThe GSM8K gate used:\r\n\r\n```bash\r\nlm_eval \\\r\n  --model local-completions \\\r\n  --model_args model=deepseek-ai/DeepSeek-V4-Flash-0731,base_url=http://localhost:8000/v1/completions,num_concurrent=16,tokenizer_backend=none,tokenized_requests=False \\\r\n  --tasks gsm8k \\\r\n  --num_fewshot 5 \\\r\n  --batch_size 1 \\\r\n  --seed 42\r\n```\r\n\r\n## Current ROCm qualification\r\n\r\nOn current ROCm main/nightly, DeepSeek-V4 defaults to Model Runner V1, while DSpark requires Model Runner V2. Set `VLLM_USE_V2_MODEL_RUNNER=1` when launching this feature.\r\n\r\nROCm breakable CUDA graphs were disabled by default for every architecture in [#53155](https://github.com/vllm-project/vllm/pull/53155). DeepSeek V4 adaptive throughput regresses without that execution mode, so the validated launch configuration also sets `VLLM_USE_BREAKABLE_CUDAGRAPH=1`."
    },
    {
      "sha": "21aa17c1289f9c376c941b5a5e3d140590d4cae1",
      "pr_number": 57948,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57948",
      "pr_title": "[Bugfix][Frontend] Accept diarized transcription responses in run-batch",
      "pr_body": "## Summary\r\n\r\n- reuse the canonical `TranscriptionResponseVariant` in the batch response union\r\n- accept successful diarized transcription results instead of misclassifying them as streaming\r\n- keep the transcription wrapper return annotation synchronized with the canonical response union\r\n\r\n## Why\r\n\r\n`vllm run-batch` currently turns a successful non-streaming `diarized_json`\r\ntranscription into a false HTTP 400 with `Request must not be sent in stream mode`.\r\nThe batch launcher duplicated only the plain and verbose transcription response\r\ntypes, omitting the diarized response from its success classification.\r\n\r\nFixes #57944.\r\n\r\n## Testing\r\n\r\n```text\r\nruff check vllm/entrypoints/launchers/run_batch.py tests/entrypoints/launchers/test_run_batch.py\r\n# All checks passed!\r\n\r\nruff format --check vllm/entrypoints/launchers/run_batch.py tests/entrypoints/launchers/test_run_batch.py\r\n# 2 files already formatted\r\n\r\n# Local Python smoke check using TranscriptionResponseDiarized and\r\n# TypeAdapter(AllResponse) for direct and JSON response forms:\r\n# direct and JSON diarized responses accepted\r\n```"
    },
    {
      "sha": "887cf91e30545373f90eb967c981b456c9cbbf8d",
      "pr_number": 57498,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57498",
      "pr_title": "[Pooling] Fix normalization of chunked long-text embeddings",
      "pr_body": "## Purpose\r\n\r\nAveraging normalized chunks can produce a non-unit long-text embedding. Re-normalize multi-chunk aggregates when `use_activation=True`, preserving single-chunk and raw outputs.\r\n\r\n## Reproducer\r\n\r\nThe temporary CPU reproducer passes two equal-weight, orthogonal normalized\r\nchunk embeddings through `_post_process_chunked`. It also checks a one-chunk\r\nBF16 prompt and the `use_activation=False` path.\r\n\r\n```bash\r\nenv PYTHONPATH=<main-worktree> .venv/bin/python /tmp/chunked_norm_cpu_reproducer.py\r\nenv PYTHONPATH=$PWD .venv/bin/python /tmp/chunked_norm_cpu_reproducer.py\r\n```\r\n\r\n## Output on main / on branch\r\n\r\nMain:\r\n\r\n```text\r\nmulti=[0.5, 0.5] norm=0.707107\r\nsingle_exact=True value=[0.6015625, 0.80078125]\r\nraw=[0.5, 0.5]\r\n```\r\n\r\nBranch:\r\n\r\n```text\r\nmulti=[0.7071067690849304, 0.7071067690849304] norm=1.000000\r\nsingle_exact=True value=[0.6015625, 0.80078125]\r\nraw=[0.5, 0.5]\r\n```\r\n\r\n## H100 multi-model evaluation\r\n\r\nThe  end-to-end sweep used BF16, `max_model_len=128`, and long inputs of 1,192-1,682 tokens. It covered encoder mean pooling, encoder CLS pooling, and decoder last-token pooling.\r\n\r\n| Model | Architecture / pooling | Old multi-chunk norms | New multi-chunk norms | Short max abs delta across independent servers | Minimum cosine with old output | Retrieval ranking |\r\n| --- | --- | ---: | ---: | ---: | ---: | --- |\r\n| `intfloat/e5-small-v2` | encoder / MEAN | 0.997116-0.997419 | 1.000000 | 7.45e-9 | 1.000000 | cosine and IP unchanged |\r\n| `BAAI/bge-small-en-v1.5` | encoder / CLS | 0.986880-0.993246 | 1.000000 | 1.49e-8 | 1.000000 | cosine and IP unchanged |\r\n| `Qwen/Qwen3-Embedding-0.6B` | decoder / LAST | 0.784638-0.821176 | 1.000000 | 7.45e-9 | 0.999994 | cosine unchanged; IP changed |\r\n\r\nFor E5 and BGE, the new long output matched an explicitly normalized old weighted mean within `2.98e-8`. Qwen's maximum BF16 difference was `4.73e-4` with the same cosine ranking. Its IP ranking changed from `[0, 1, 2, 4, 3]`\r\nto `[0, 1, 4, 2, 3]`, demonstrating why this should be documented as an intentional correctness-related output change rather than a refactor.\r\n\r\nShort prompts do not execute the new normalization operation. The direct BF16 aggregation check is bitwise exact; independent H100 server launches differed by at most `1.49e-8`, consistent with normal numerical variation.\r\n\r\n### Compatibility note\r\n\r\nOnly normalized outputs formed from multiple chunks change. Their direction is preserved while their magnitude becomes approximately 1. Cosine retrieval is therefore expected to remain stable, but inner-product scores and rankings can change. Deployments using inner-product indexes should not mix old and new long-document embeddings; re-embed affected long documents when upgrading.\r\n\r\n## Test plan\r\n\r\n```bash\r\n.venv/bin/python -m pytest tests/entrypoints/pooling/embed/test_io_processor.py -q\r\n# 46 passed\r\n\r\nenv UV_CACHE_DIR=/tmp/vllm-uv-cache .venv/bin/pre-commit run \\\r\n  --files vllm/entrypoints/pooling/embed/io_processor.py\r\n# Passed\r\n\r\nenv UV_CACHE_DIR=/tmp/vllm-uv-cache .venv/bin/pre-commit run \\\r\n  --all-files --hook-stage manual\r\n# Passed, including dependency locks and mypy for Python 3.10-3.13\r\n```\r\n\r\nThe deterministic temporary CPU reproducer and the three-model H100 E2E sweep also passed with the outputs summarized above.\r\n\r\n\r\n## AI assistance disclosure\r\n\r\nOpenAI Codex (GPT-5) assisted with drafting the implementation and temporary validation scripts. \r\n"
    },
    {
      "sha": "c8824a1e42bbd674751f808d657d58024c68fb0f",
      "pr_number": 57913,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57913",
      "pr_title": "[MM] Move `supports_multimodal_inputs` and cache out of registry",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\n\r\nFurther reduce reliance on `MM_REGISTRY` by decoupling `supports_multimodal_inputs` check and MM cache construction from it.\r\n\r\nAlso organize the cache implementations into separate files, renaming accordingly.\r\n\r\n## Test Plan\r\n\r\n## Test Result\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "7488b5780f585fc0a00a6ab28b6a2477696574d1",
      "pr_number": 57869,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57869",
      "pr_title": "[ROCm][Tests] Reduce host memory when downcasting FP32 HF references",
      "pr_body": "- Load eligible native FP32 causal HF references directly onto the current ROCm device when requesting FP16/BF16.\r\n- Preserve staged loading for same-dtype or unknown-dtype checkpoints and non-ROCm platforms.\r\n- Exclude explicit revisions, additional loader options, remote model code, quantized models, and ZeRO-3.\r\n- Add the inferred device map to a new kwargs dictionary while retaining existing dtype normalization and runner cleanup.\r\n\r\nThe [AMD-CI 13265 Language Extended Generation group](https://buildkite.com/vllm/amd-ci/builds/13265/list?sid=01a0be0c-75bb-4cb2-97ce-bc0fd92212d2&tab=output) lost container heartbeat during HF PowerMoE loading, motivating an investigation of reference-loading memory. No introducing regression PR or CI OOM termination has been established, so this change is a measured host-memory mitigation rather than a claimed fix for that external failure. Controlled PowerMoE and HyperCLOVAX comparisons reduced peak RSS from 20.26 to 15.96 GiB and 83.74 to 58.80 GiB while preserving generated outputs, sampled weights, and device placement. Direct loading increased memory for an already-BF16 Qwen checkpoint, which is why the optimization requires a positively declared FP32 source and stays ROCm-only. \r\n\r\nInvestigation, implementation, validation, and this description were AI-assisted.\r\n"
    },
    {
      "sha": "8b98b7d0b465920740cfa1acf5dde1b6a57d8d52",
      "pr_number": 57922,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57922",
      "pr_title": "[Frontend] Add streaming parity tests and docs for derender",
      "pr_body": " ## Purpose\r\n\r\nPR3 for RFC #47161, following #47301 and #50550. Adds streaming parity CI and docs for the streaming derender endpoints.\r\n\r\n**Parity tests.** `test_derender_parity.py` now covers `stream=true`. It streams `/v1/chat/completions` with `return_token_ids=True` and sends each chunk's tokens through the chunked `/v1/chat/completions/derender` with the same boundaries. The assembled content, reasoning, tool calls and `finish_reason` have to match. Tool call IDs compare by format and neither side may change an ID mid stream.\r\n\r\nCases are plain, reasoning, auto tool call and reasoning + forced tool call. The last one also runs with multi-token chunks (`stream_interval=4`, shaped like spec decode) and with the finish reason on a trailing chunk with no tokens. Derender gets the engine's finish reason rather than the chat one, so the `tool_calls` rewrite is tested.\r\n\r\nThe stream helpers in `test_derender_stream.py` move to a shared `utils.py` so both files use them.\r\n\r\n**Docs.** `derenderer.md` gets the streaming request formats, the client loop contract (echo `stream_state`, one choice per chunk, `n > 1` as N streams, forward the usage chunk) and a streaming example. It also notes that streaming drops\r\nlogprobs for now. It picks up the two docs items from #42729 too: the render server's parser and chat template flags have to match `vllm serve` and completions don't parse. `protocol.py` only gains snippet markers.\r\n\r\n**Not in this PR.** The batch parity test still feeds the coupled `finish_reason` into derender. Fixing that and checking batch tool call ID format would fail on `main` until #47931 and the batch tool call ID fix land, so it follows them. The\r\nlogprobs line in the docs changes when #55029 lands.\r\n\r\n**Why the tool cases look the way they do.** In #50550's CI run, batch `test_parity_tool_call` was skipped because the model hit `max_tokens` inside the `<tool_call>` JSON. Streamed tool call arguments can be partial JSON, so they compare as raw strings. The streaming tool cases use `max_tokens=512` because replay is O(n³).\r\n\r\n\r\n## Test Plan\r\n`pytest -s -v tests/entrypoints/scale_out/derender/test_derender_stream.py`\r\n`pytest -s -v tests/entrypoints/scale_out/derender/test_derender_parity.py`\r\n\r\n## Test Result\r\n\r\nAll tests pass.\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [x] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [x] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n"
    },
    {
      "sha": "606d124b377e6462fe0f9076df4d50c88e4dcf48",
      "pr_number": 57234,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57234",
      "pr_title": "[Bugfix][Multimodal] Tolerate malformed EXIF in Molmo 2 image preprocessing",
      "pr_body": "## Purpose\n\nFollow-up to #56576 (issue #56527), which made `MultiModalHasher` tolerate a\nmalformed EXIF header. Molmo 2 keeps its own copy of the EXIF normalization and\nthat copy is **not** tolerant, so for that model the merged fix moved the\nfailure one step later instead of removing it.\n\n`vllm/multimodal/image.py` suppresses the read:\n\n```python\ndef normalize_image(image: Image.Image) -> Image.Image:\n    \"\"\"Normalize EXIF orientation so the pixel data matches visual display.\"\"\"\n    with contextlib.suppress(Exception):\n        image = ImageOps.exif_transpose(image)\n    return image\n```\n\n`vllm/model_executor/models/molmo2.py` does not:\n\n```python\ndef exif_transpose(images: ImageInput | None) -> ImageInput | None:\n    ...\n    elif images is not None and isinstance(images, Image):\n        images = ImageOps.exif_transpose(images)   # bare\n    return images\n```\n\nThe fix reuses `normalize_image` rather than adding a second\n`contextlib.suppress`, so there is only one place that decides how a bad EXIF\nblock is handled. `vllm/multimodal/parse.py` already imports it the same way.\n\n## Why the second read is the one that raises\n\nThis is the non-obvious part, and it is why the bug survived #56576. Measured on\nPillow 12.3.0 with the payload from #56576's test:\n\n| sequence | result |\n| --- | --- |\n| `Image.open(...)` then `ImageOps.exif_transpose(im)` | no raise |\n| `ImageMediaIO.load_bytes(...)` then `ImageOps.exif_transpose(im)` | `SyntaxError: not a TIFF file (header b'XXXX\\x00\\x00\\x00\\x08' not valid)` |\n| the same, second call on the same object | no raise |\n\n`ImageMediaIO.load_bytes` (`vllm/multimodal/media/image.py:90-92`) runs\n`normalize_image(image)` -> `image.load()` -> `_convert_image_mode(image)`. The\nsuppressed transpose plus `load()` is what primes Pillow's lazy EXIF state, and\n`.convert()` carries that state forward — so the *next* read raises, and only\nthe next one (Pillow caches the failure afterwards). That last row is why the\nregression test loads a fresh image for every case; reusing one would make the\ntest pass on `main` by coincidence.\n\n## Reachability\n\n`POST /v1/chat/completions` against a Molmo 2 model with an `image_url` whose\nJPEG carries a malformed APP1/Exif segment:\n\n1. `ImageMediaIO.load_bytes` primes the EXIF state (above).\n2. Hashing runs next and is now tolerant, thanks to #56576.\n3. `Molmo2MultiModalProcessor._get_prompt_updates` ->\n   `get_image_replacement_molmo2` (`molmo2.py:2218`) -> the bare\n   `exif_transpose` -> `SyntaxError`.\n\n`SyntaxError` is neither `VLLMClientError` nor `GracefulHTTPError`, so\n`AsyncLLM.generate`'s generic handler wraps it as\n`raise EngineGenerateError() from e` (`vllm/v1/engine/async_llm.py:764`) — a 500\nwith an empty `message`, matching what #56527 reported for the hasher.\n\n## Is the class fully fixed?\n\nYes. `grep -rn \"exif_transpose\\|getexif\" --include=*.py vllm/` on current `main`\nreturns four sites:\n\n| site | state |\n| --- | --- |\n| `vllm/multimodal/image.py:24` | suppressed |\n| `vllm/multimodal/hasher.py:74` | tolerant since #56576 |\n| `vllm/model_executor/models/molmo2.py:1302` | **fixed here** |\n| `vllm/model_executor/models/molmo2.py:1299` | recursive call into the same function |\n\nMolmo 2 was the only remaining bare read, so no follow-up is needed.\n\n## Test\n\n**No test ships with this PR** — removed at the reviewer's request\n(review of 2026-09-17: \"Thanks but let's remove the tests\"). The diff is now\nthe two-line source change only.\n\nFor the record, the regression test that had been in this PR was verified on\n**fork CI run\n[35144842256](https://github.com/Hotragn/vllm/actions/runs/35144842256)**, with\nBEFORE run against `main` (`30b847c1fa`):\n\nBEFORE — **1 failed**, and the failure is the defect itself rather than an\nincidental assertion:\n\n```\ntests/models/multimodal/processing/test_molmo2.py:135: in test_exif_transpose_tolerates_malformed_exif\n    assert exif_transpose(_served_image()) is not None\nvllm/model_executor/models/molmo2.py:1302: in exif_transpose\n    images = ImageOps.exif_transpose(images)\nPIL/ImageOps.py:703: in exif_transpose\n    image_exif = image.getexif()\n...\nE  SyntaxError: not a TIFF file (header b'XXXX' not valid)\n```\n\nAFTER — the whole file: **6 passed**. Whole-suite\n`pytest tests/models/multimodal/processing` on both sides: the same two\npre-existing collection errors (`test_llava_next.py`, `test_llava_onevision.py`),\nidentical on `main` and on the branch.\n\nLocal lint on the remaining change, all clean: `ruff check` / `ruff format`\n(0.14.0), `typos` (1.43.5), `check_spdx_header`, `check_forbidden_imports`,\n`check_torch_cuda`, `check_init_lazy_imports`.\n\npytest cannot run on my local machine (Windows-on-ARM), hence the fork runner;\nthe Pillow measurements in the table above were made locally with Pillow alone.\n\n## Notes\n\nAI assistance was used to research and draft this change. I have reviewed every\nchanged line and verified the change as described above.\n\n"
    },
    {
      "sha": "db7f1f671419535b85fa73e29ff061e287e39459",
      "pr_number": 57889,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57889",
      "pr_title": "[CI][XPU] Deselect Ray UT in XPU V1 test Job",
      "pr_body": "Deselects only the `[ray]` parametrization for the XPU V1 test job  tests/v1/kv_connector/unit/test_nixl_connector.py::test_abort_timeout_on_prefiller\r\n"
    },
    {
      "sha": "7268f6e38ebbb3d61700f16c638c6eba0c88bb0c",
      "pr_number": 57833,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57833",
      "pr_title": "[Security] Prefer fresh multimodal payloads over a stale receiver cache",
      "pr_body": "## Summary\n\nThe engine-side multimodal processor cache treated a hash hit as authoritative even when the frontend had resent a full payload. After independent LRU eviction, that lets a later request reuse the same cache identity with a different item: the engine keeps the request's placeholder layout but substitutes the stale tensor, and embedding merge then kills EngineCore. This change always stores and returns the resent payload, so a later cache-only lookup cannot revive the old item.\n\n## Changes\n\n- `vllm/multimodal/cache.py`: on `MultiModalReceiverCache.get_and_update_item`, if tensor data is present, drop any existing entry then cache and return that payload. Cache-only lookups (`data=None`) are unchanged, including `MultiModalCacheMissError`.\n- `tests/multimodal/test_cache.py`: cover item replacement and the `get_and_update_features` engine entry.\n\n## Codepath coverage\n\n- Default LRU IPC cache used by EngineCore `preprocess_add_request` (`/v1/chat/completions` and other generate paths that attach `mm_features`).\n- Worker SHM cache is a different class and already uses the caller's address payload rather than substituting a hash hit.\n- Processor-only (no IPC) keeps prompt updates with the cached item in one process, so this drift does not apply.\n\n## Duplicate-work check\n\n- Open PRs searched for receiver-cache substitution, processor-cache eviction, and embedding-count mismatch.\n- https://github.com/vllm-project/vllm/pull/57696 rejects encoder-cache hits with mismatched embedding counts. It does not stop the processor cache from substituting a stale tensor before encode.\n- https://github.com/vllm-project/vllm/pull/46747 (merged) recovers the opposite drift (frontend hit, engine miss) via `MultiModalCacheMissError`. It still returns a cached item whenever the engine has a hash hit, including when a payload was resent.\n\n## Tests\n\n- `test_receiver_cache_replaces_stale_item_when_payload_resent`: resent larger item replaces the cached one; a later `data=None` lookup returns the new item.\n- `test_receiver_cache_features_keep_resent_payload`: same control through `get_and_update_features`.\n\n```bash\n.venv/bin/python -m pytest tests/multimodal/test_cache.py -v -k \"receiver_cache_replaces or receiver_cache_features or mm_cache_miss or oversized\"\n```\n\nPassed: `test_oversized_item_is_served_uncached`, `test_mm_cache_miss_raises_and_recovers`, `test_receiver_cache_replaces_stale_item_when_payload_resent`, `test_receiver_cache_features_keep_resent_payload`, `test_mm_cache_miss_batches_all_drifted_hashes`.\n\n## AI assistance\n\nThis PR was developed with AI assistance.\n\nMade with [Cursor](https://cursor.com)"
    },
    {
      "sha": "82daf9f5756e1868be0aa751afaec4726beca12a",
      "pr_number": 57876,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57876",
      "pr_title": "[CI][ROCm] Add ten AMD parity groups",
      "pr_body": "Add ten AMD mirrors with matching legacy `test-amd.yaml` jobs: Scale-out EC, Mooncake EC TCP, Kimi Linear disaggregation, Sharded RDT, single-GPU hidden-state extraction, DeepSeek V4 fused kernels, watermarking, watermark feature combinations, Qwen3.8 FP8 accuracy, and MoE refactor integration.\n\nPlace each group in its existing hardware/topic section, add ROCm evaluation configs, and allow 15 minutes of timeout headroom. The hidden-state job selects non-distributed cases; existing two-GPU coverage keeps the distributed cases. NVIDIA commands remain unchanged.\n\nThese mirror parent keys do not overlap with #50519, #50922, or #56679. The disaggregation coverage is also distinct from #53925's communication/Mamba/context-attention work.\n\nThe DeepSeek V4 RoPE-only query test allows ROCm BF16 rounding with `rtol=1e-2, atol=1e-6` on rotary dimensions. CUDA, unrotated dimensions, and padding remain exact. Pre-commit and a 32-seed FMA rounding audit on MI250 passed; full fused-kernel validation on MI355 is pending (the local build lacks the compiled operation).\n\nValidation: `pre-commit run`, both AMD pipeline generators, and full rendered key/dependency checks with MiniJinja 2.3.1 passed. The legacy Qwen3-8 label avoids a period in its generated block key; model paths and commands are unchanged. [AMD CI #13310 bootstrap](https://buildkite.com/vllm/amd-ci/builds/13310) successfully uploaded the pipeline on `b39f7c836`. Incoming main features are preserved. AI assistance: OpenAI Codex.\n\n### Newly proposed jobs\n\nMirrors inherit their NVIDIA parent’s `optional` setting; all ten `test-amd.yaml` jobs remain optional.\n\n- (MI300) Scale-out EC Connector E2E — 2 GPUs.\n- (MI300) Mooncake EC TCP E2E — 2 GPUs.\n- (MI355) Kimi-Linear-48B-A3B Disaggregated DP EP — 4 GPUs.\n- (MI355) Sharded RDT Weight Transfer — 1 GPU.\n- (MI355) Extract Hidden States Integration Single GPU — 1 GPU.\n- (MI355) DeepSeek V4 Fused Kernels — 1 GPU.\n- (MI355) LM Eval Watermarking — 1 GPU.\n- (MI355) LM Eval Watermark Feature Combination — 2 GPUs.\n- (MI355) Qwen3.8-Flash-Next-FP8 Accuracy Eval — 4 GPUs.\n- (MI355) MoE Refactor Integration Shard %N — 2 GPUs.\n"
    },
    {
      "sha": "15859bb3a1d81a709b64eff2a1d1f38a958a362b",
      "pr_number": 57545,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57545",
      "pr_title": "[Test] Organize structured output utility tests",
      "pr_body": "## Background\n\nFollow up on [#48115](https://github.com/vllm-project/vllm/pull/48115), which suggested organizing `test_utils.py` tests by the function they exercise and the PR that introduced them.\n\nThis is a test-only refactor. Assertions, fixtures, coverage, and behavior are unchanged.\n\n## Changes\n\n- Grouped `test_unsupported_json_features_by_type` and `test_supported_json_features` under `TestHasXgrammarUnsupportedJsonFeatures`.\n- Grouped `test_property_names_constrains_keys` and `test_pattern_properties_constrains_keys_and_values` under `TestIsGrammarAcceptString.TestPR42904Support`, corresponding to [#42904](https://github.com/vllm-project/vllm/pull/42904).\n- Kept the existing choice grammar regressions under `TestIsGrammarAcceptString.TestPR48115Regressions`, corresponding to [#48115](https://github.com/vllm-project/vllm/pull/48115).\n\nThe original feature-gate tests were introduced in [#12388](https://github.com/vllm-project/vllm/pull/12388). The parametrized `test_supported_json_features` currently contains cases from both #12388 and #42904, so it remains grouped at the function level rather than being assigned to a single PR.\n\nIf reviewers have more detailed historical information or prefer a different classification, I am happy to adjust the grouping accordingly.\n\n## Test Plan\n\n- 68 tests passed\n- pre-commit passed\n"
    },
    {
      "sha": "04c1f4a407962a37d01b573f3a2aba91dd2ed3a9",
      "pr_number": 57906,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57906",
      "pr_title": "[Bugfix][ROCm][DSv4.1] Disable SWA bounded replay on ROCm",
      "pr_body": "## Purpose\n\nDisable SWA bounded replay on ROCm, where it currently faults the GPU.\n\nSWA bounded replay (#56227, on by default) keeps the sliding-window KV out of prefix caching and rebuilds it after a prefix hit by padding the replayed tokens' slots in the prefix-cacheable groups. Correctness depends on the consuming prefill kernel clamping the window; that clamp landed in the FlashInfer and FlashMLA paths.\n\nThe ROCm sparse SWA metadata builders (`DeepseekV4ROCMAiterSparseSWAMetadataBuilder` in both `deepseek_v4/amd/rocm.py` and `deepseek_v41/amd/rocm.py`) only gained the `replay_start` kwarg and forward it to the base builder. Nothing on the ROCm prefill path clamps the window, so the first prefix hit that carries a replay start walks off the padded slots and raises `HSA_STATUS_ERROR_MEMORY_FAULT`, which takes down the worker and then the engine core:\n\n```\nMemory access fault by GPU node-6 (Agent handle: 0x5621a810) on address 0x7ba8c13ff000. Reason: Unknown.\nRuntimeError: Worker failed with error 'CUDA error: an illegal memory access was encountered'\nRuntimeError: Engine core process EngineCore died unexpectedly.\n```\n\nThis adds a ROCm guard alongside the existing model-runner-V2 and prefill-context-parallel guards in `DeepseekV4Attention`. Prefix caching itself is unaffected — the sliding-window cache simply takes part in it again, which is the pre-#56227 behavior. The guard should be removed once the ROCm prefill kernels clamp the window too.\n\n## Test Plan\n\nDeepSeek-V4.1-Flash on MI355X (gfx950), ROCm 10.0, torch 2.12.0+rocm10.0.0, Triton 3.8.0, TP=4, `--max-model-len 1048576`, `--max-num-batched-tokens 16384`, DSpark speculative decoding, `--moe-backend aiter`.\n\nSingle prompt per request at `max_tokens=1`, sweeping input length across the prefill-chunk boundary:\n\n```\nvllm serve deepseek-ai/DeepSeek-V4.1-Flash --tensor-parallel-size 4 \\\n    --language-model-only --tokenizer-mode deepseek_v41 \\\n    --moe-backend aiter --gpu-memory-utilization 0.9 \\\n    --max-model-len 1048576 --max-num-batched-tokens 16384 \\\n    --speculative-config '{\"method\":\"dspark\",\"num_speculative_tokens\":5,...}'\n```\n\n## Test Result\n\nBaseline (`swa_bounded_replay` on, the current default):\n\n| Input tokens | Result |\n| --- | --- |\n| 4,096 | OK |\n| 32,768 | `HSA_STATUS_ERROR_MEMORY_FAULT`, engine core dies |\n\nA prompt that fits in one prefill chunk passes; the fault appears once the prompt spans more than one chunk. Every fault immediately follows the first launch of `_pad_replayed_slots_kernel`. Setting `AMD_SERIALIZE_KERNEL=3` makes the same 32,768-token request pass, which is consistent with reading past the padded slots rather than with a bad constant. `TRITON_HIP_USE_ASYNC_COPY=0` only moves the threshold (32,768 passes, 131,072 still faults), so this is not a Triton async-copy issue.\n\nWith this change, on the same build and the same four GPUs:\n\n| Input tokens | Result |\n| --- | --- |\n| 32,768 | OK (1.4s) |\n| 131,072 | OK (5.5s) |\n| 262,144 | OK (10.4s) |\n| 314,510 | OK (4.3s) |\n\nZero memory faults, and `_pad_replayed_slots_kernel` is never launched.\n\nEnd to end, a 16-configuration MI355X AgentX sweep (TP=2 and TP=4, concurrency 1-128) failed every point before this change and runs clean after it, with the accuracy arm reporting gsm8k `em_strict` 0.9750 / `em_flexible` 0.9742 over 1319 effective samples.\n\nNo new test is added: the two sibling guards in this function (model runner V2, prefill context parallelism) are likewise untested, and exercising this one requires a ROCm device with the model loaded.\n"
    },
    {
      "sha": "0b7f11a1eece3dc9c689f1b5ab90d60c03b10793",
      "pr_number": 45635,
      "pr_url": "https://github.com/vllm-project/vllm/pull/45635",
      "pr_title": "[AuxOutput] Add block-keyed storage for routed-expert outputs",
      "pr_body": "# AuxOutput Connector: block-keyed storage for routed-expert outputs\n\n## Summary\n\nThis PR introduces the first **AuxOutput Connector** implementation for vLLM.\nThe first supported auxiliary output is routed-expert output (R3): the expert IDs selected for\nevery token executed by an MoE model.\n\nR3 cannot remain a request-local GPU buffer when prefix caching and async\nscheduling are enabled:\n\n- a prefix-cache hit skips model execution, so cached tokens produce no new R3;\n- physical GPU/KV slots are reused and are not stable object identities;\n- speculative decoding can execute rows that are later rejected;\n- one result may contain cached blocks, new blocks, and a partial tail.\n\nThe connector stores immutable R3 blocks using KV-compatible block hashes. KV\nand R3 for the same prefix can therefore be reused together. This PR provides a\nbounded local SHM backend; distributed storage and more auxiliary output types are\nfollow-ups.\n\n## Why an AuxOutput Connector?\n\nThe model capture hook only obtains a tensor from the current forward pass. A\nseparate lifecycle layer must decide:\n\n- which logical tokens are accepted;\n- which blocks are reusable across requests;\n- when temporary GPU/CPU buffers can be released;\n- where immutable blocks are stored; and\n- how cached and new blocks form one ordered result.\n\nThese responsibilities also apply to future DSA and logprobs auxiliary outputs. The\nconnector keeps them out of the model runner and gives storage backends one\ncommon object lifecycle. It is not a second KV Connector or a generic Scheduler\nsidecar.\n\n## Architecture\n\n```text\n+-------------------------------------------------------------------+\n| API / OutputProcessor                                             |\n| Current: return complete R3                                       |\n| Follow-up: return ordered auxiliary output keys                           |\n+-------------------------------+-----------------------------------+\n                                |\n                                v\n+-------------------------------------------------------------------+\n| EngineCore                                                        |\n| Scheduler -> AuxOutputSchedulerConnector                           |\n|              - logical request progress                           |\n|              - KV-compatible hashes                              |\n|              - terminal events                                   |\n+-------------------------------+-----------------------------------+\n                                | per-step metadata\n                                v\n+-------------------------------------------------------------------+\n| GPU Worker                                                        |\n| Model Runner -> router capture -> async GPU-to-CPU copy            |\n|              -> AuxOutputWorkerConnector                           |\n|                 - accepted-row assembly                           |\n|                 - block publication/materialization               |\n+-------------------------------+-----------------------------------+\n                                |\n                  +-------------+-------------+\n                  |                           |\n                  v                           v\n     +--------------------------+  +--------------------------+\n     | Current                  |  | Follow-up                |\n     | Bounded local SHM        |  | Mooncake distributed     |\n     | background publication   |  | immutable objects        |\n     +--------------------------+  +--------------------------+\n```\n\nThe Scheduler connector owns logical progress and hashes, not payloads. The\nWorker connector owns accepted R3 rows and publication. `BlockObjectStore` owns opaque\nimmutable blocks and retention, but knows nothing about tokens or speculative\ndecoding.\n\n## Auxiliary output types\n\nAn execution auxiliary output is an auxiliary model output that is aligned to a stable\nlogical coordinate and may need to outlive the worker step that produced it.\n\n| Auxiliary output | Coordinate | Reuse | Status |\n| --- | --- | --- | --- |\n| Routed experts (R3) | Executed token | Prefix block | This PR |\n| DSA routing/index data | Executed token | Prefix block | Follow-up |\n| Logprobs/prompt logprobs | Predicted token | Prefix block | Follow-up |\n| Multimodal auxiliary data | Request/object | Request only | Exploratory |\n| Sampling candidate IDs | Sampling step | Request only | Exploratory |\n\nPrefix-block auxiliary outputs are content-addressed and reusable with a matching\nprefix. Request-only auxiliary outputs use the same storage contract but do not\nparticipate in prefix hits. Reward values, tool results, finish reasons, and\nScheduler status remain normal request/control-plane metadata.\n\n## Current PR scope\n\nThis PR implements:\n\n- Model Runner V2 R3 capture;\n- block-keyed SHM publication and prefix materialization;\n- async scheduling, chunked prefill, speculative rejection, and preemption;\n- background publication, bounded capacity, and KV-aligned retention;\n- terminal-only R3 delivery for streaming output;\n- fail-closed checks for unsupported configurations.\n\nIt does not implement Mooncake, public auxiliary output keys, additional auxiliary output\nfields, PD disaggregation, or new parallel topologies.\n\nWhen disabled, no auxiliary output capture buffer, Store, per-step metadata, or R3 copy\nis created on the normal serving path.\n\nThe existing `--enable-return-routed-experts` flag and `routed_experts` response\nfield are unchanged. The new optional configuration is exposed as\n`--aux-output-config` (`AuxOutputConfig` in Python).\n\n## Follow-ups\n\n```text\nThis PR\n  R3 lifecycle + local SHM\n        |\n        v\nUnified object/profile contract + ordered keys\n        |\n        v\nDirect Mooncake backend\n        |\n        v\nJoint KV + mandatory-auxiliary output prefix readiness\n        |\n        v\nDSA, logprobs, and additional topology support\n```\n\nThe main planned work is:\n\n1. define a backend-independent object/profile contract and ordered key output;\n2. add a direct Mooncake backend for external trainer/JAX consumers;\n3. admit remote prefix hits only when KV and mandatory auxiliary outputs are ready;\n4. add DSA and logprobs through small field adapters, not new lifecycle code;\n5. validate CPU KV offload, PD, PP/DCP/PCP, and multi-writer deployments before\n   enabling them.\n\n## Validation\n\nNaming-update validation: **96 passed, 1 skipped** across configuration, store,\ncapture, Scheduler, output, and Ray tests. An AST comparison verified that all\n24 renamed files preserve the original logic, allowing renamed identifiers,\nmessages, and sorted imports. Additional smoke checks passed for the existing\nR3 flag, the new JSON/Python configuration, compilation hashes, serialization,\nobject keys, and R3 bytes. The latest naming-only update was also exercised on GB200; results and\nlimitations are below.\n\n```bash\nVLLM_TARGET_DEVICE=cpu .venv/bin/python -m pytest -q \\\n  tests/config/test_aux_output_config.py \\\n  tests/distributed/aux_output_connector/test_store.py \\\n  tests/model_executor/test_routed_experts_capture.py \\\n  tests/v1/executor/test_ray_utils.py tests/v1/core/test_scheduler.py \\\n  tests/v1/core/test_async_scheduler.py tests/v1/engine/test_output_processor.py \\\n  -k 'aux_output or routed_experts or pending_output or store or worker or detach_zero_copy'\npre-commit run\n```\n\n### GB200 rename A/B (2026-09-08)\n\nCompared `85f100032e` (before rename) with `f99d53b521` (after rename) on the\nsame two GB200 nodes (8 GPUs), using the locally cached DeepSeek-V4-Flash-Base\nFP8 weights. Both arms enable R3, MRV2, MP, TP1/DP8/EP8, async scheduling and\nprefix caching. They use identical native libraries/dependencies; Python source\ninjection was verified by per-file SHA checks.\n\nWorkload: 64 concurrent requests, each with 262,144 input tokens and one output\ntoken; synthetic token IDs, with output constrained to token 42. GPU memory\nutilization is 0.90, max batched tokens 16,384, block size 256, and batch-invariant\nmode is off. This is a serving/R3 regression test, not a model-quality evaluation.\n\n| Measurement | Before rename | After rename |\n| --- | ---: | ---: |\n| Total throughput (input + output tokens/s) | 82,741.42 | 82,568.30 |\n| Total duration | 202.768 s | 203.193 s |\n| Preemptions | 0 | 0 |\n| Prefix-hit tokens in the timed workload | 0 | 0 |\n\nThroughput changed by **−0.21%** in this single A/B: no obvious regression, not a\nstatistical significance claim. Startup and warmup are excluded. Both arms still\nreported the same first-long-request `CombineTopkSwaIndicesKernel.kernel` JIT\nwarning, so this is not a strictly all-kernels-warm measurement.\n\nCorrectness checks:\n\n- All 128 long responses passed token-count, R3 row-count, shape and dtype checks.\n- Separate cold/hot tests cover eight 1K-prompt/16-decode requests and eight\n  64K-prompt/1-decode requests per version. The latter exercises multi-step\n  chunked prefill. All requests' reused-prefix R3 SHA hashes match exactly:\n  768 rows/request for the short case and 65,280 rows/request for the long case.\n- All eight short cold requests have identical full R3 hashes across versions.\n\n**Limitation: long-context full-output bitwise equivalence did not pass.** None\nof the 64 full 256K R3 hashes matched across the two server runs. In the saved\nrequest-0 sample, differences begin at row index 2051 and affect 55.38% of\nelements. The same candidate version also differs when comparing the identical\nfirst 64K input prefix in 256K versus 64K cold requests (first difference at\n2051; 46.44% of elements). Recomputed decode R3 can differ between cold/hot runs\nas well, already before the rename. These observations do not establish the\nunderlying cause or prove long-output equivalence; they must not be presented\nas a full R3 correctness pass.\n\nThis run validates the response structure, exact cached-block reuse and short\ncross-version outputs. It does not add new forced-preemption, speculative\ndecoding or free-generation quality coverage. Slurm job `24176` completed with\nexit code 0; test containers and node allocations were released.\n\nDevelopment validation covers focused unit tests, async scheduling, forced\npreemption, chunked prefill, speculative rejection, cold/hot prefix reuse,\nstreaming aggregation, SHM capacity/LRU behavior, pre-commit, and GB200/H200\nthroughput experiments reported in the PR discussion.\n\nThe latest update also moves R3 GPU-to-CPU transfer onto the existing async\noutput copy stream, avoiding a synchronous copy on the model execution critical\npath.\n\nAI assistance was used for implementation, testing, performance experiments,\nand review. The submitting human reviewed the accepted changes and validation.\n"
    },
    {
      "sha": "0aee727ff6131a1b647941186ceef8f4777bdac2",
      "pr_number": 57903,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57903",
      "pr_title": "[CI][ROCm] Increase timeout for MI300 Distributed DP Extended",
      "pr_body": "## Purpose\r\nThe `:amd: (MI300) Distributed DP Extended` job was killed by its 60-minute step timeout in build [13265](https://buildkite.com/vllm/amd-ci/builds/13265/list?sid=01a0be0c-75dd-4284-91e6-0eb7ee1a0f75&tab=output),\r\nwith tests still making normal progress and no assertion failure. Same step also timed out in build 13193.\r\n\r\nOver builds 12366-13265 (Aug 25 - Sep 20) the step runs at p50 41 min and p95 50 min, and its slowest passing run took 59.9 min. With the limit set at 60 minutes there is no headroom left above the normal spread of runtimes, so a slow run gets\r\ncancelled.\r\nRaise `timeout_in_minutes: 60 -> 75`, which covers the slowest observed run with 25% headroom."
    },
    {
      "sha": "8902dbb1770b7b1780bf3d978f16cd208702472a",
      "pr_number": 57877,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57877",
      "pr_title": "[CI][ROCm] Mirror remaining portable suites without duplicate coverage",
      "pr_body": "Declare seven AMD mirrors while giving existing coverage a single owner:\n\n| Group | AMD target | Coverage change |\n| --- | --- | --- |\n| Granite compatibility | MI355 | Remove Granite from the general hybrid suite. |\n| Multimodal beam search | MI355 | Exclude the selected cases from general samplers. |\n| E2E Core Large Memory | MI355 | Move KV-sharing and both Mamba MRV2 cases out of general E2E. |\n| MLA kernels | MI355 | Migrate the standalone job; remove duplicate MI300 and broad-attention selections. |\n| Attention DiffKV | MI355 | Add a ROCm PyTorch reference and remove broad-suite overlap. |\n| GLM5Next | MI355 | Exercise the AMD KDA kernel against the existing reference. |\n| Compiler startup | MI355 | Use spawn on ROCm/XPU instead of unsafe fork; preserve artifact-count assertions. |\n\nAll seven dynamic mirrors inherit their required NVIDIA parent status. Matching legacy `test-amd.yaml` jobs remain optional. Add AMD timeout headroom. NVIDIA definitions remain unchanged.\n\nThese parent jobs do not overlap with #50519, #50922 or #56679, or the separate ten-group proposal. #55611 covers different MLA prefill tests. **Coordinate with #57509:** its proposed CUDA-only startup guard would disable the ROCm coverage here; preserve ROCm eligibility when reconciling them.\n\nAI assistance was used. Draft pending full ROCm CI validation and human review."
    },
    {
      "sha": "b8cf2753825d05d7f41eb4747480b78934953aac",
      "pr_number": 54535,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54535",
      "pr_title": "[AMD][Minimax-M3][perf] Enable packed LBHNC AITER QK-norm fusion for MiniMax-M3 on ROCm",
      "pr_body": "\r\n## Purpose\r\nKeep MiniMax-M3's ROCm sparse path on AITER's fused QK-norm (`fused_qknorm_idxrqknorm`).\r\n\r\nDepends on:\r\n- [ROCm/aiter#5143](https://github.com/ROCm/aiter/pull/5143) (fused QK-norm, including unit-scale e4m3 `index_q`; stacked on [aiter#4787](https://github.com/ROCm/aiter/pull/4787))\r\n\r\nWhat this PR does:\r\n- Call AITER fused QK-norm for sparse-PA insert (full and skip-index), with fallback to vLLM's fused kernel.\r\n- Support packed LBHNC SHUFFLE K/V page spans.\r\n- Pass fp8 index cache / `index_q` through as `index_cache_dtype=\"fp8\"` so the AITER indexer can score e4m3 without a separate cast. If AITER cannot emit fp8 `index_q`, the wrapper returns false and the existing vLLM fused kernel is used.\r\n\r\n## Test Plan\r\nTest1:\r\n```bash\r\npytest tests/kernels/attention/test_minimax_m3.py -k \"aiter_consolidated_qknorm or aiter_sparse_pa_rejects\"\r\n```\r\nTest2:\r\n```bash\r\nvllm serve amd/MiniMax-M3-MXFP4 \r\n--port 8888 \r\n--tensor-parallel-size 4 \r\n--compilation-config '{\"cudagraph_mode\":\"FULL_DECODE_ONLY\"}' \r\n--trust-remote-code \r\n--block-size 128 \r\n--enable-chunked-prefill \r\n--no-enable-prefix-caching \r\n--language-model-only \r\n--max-num-batched-tokens 65536 \r\n--max-model-len 139776 \r\n--attention-backend ROCM_AITER_FA \r\n--moe-backend aiter \r\n--kv-cache-dtype fp8 \r\n--reasoning-parser minimax_m3\r\n```\r\n\r\n## Test Result\r\nTest1:\r\n17 passed (gfx950), including packed SHUFFLE, fp8 `index_q` mapping, and fallback when the fp8-IQ capability marker is missing.\r\n\r\nTest2:\r\n| Conc | Baseline TPUT (tok/s) | QKNorm TPUT (tok/s) | TPUT % uplift | Baseline P90 TPOT | QKNorm P90 TPOT | TPOT % uplift | Baseline P90 ITL | QKNorm P90 ITL | ITL % uplift |\r\n|------|------------------------------------:|-------------------------------:|--------------:|--------------------------------:|---------------------------:|--------------:|-------------------------------:|--------------------------:|-------------:|\r\n| 4    | 4391.71                            | 4651.97                        | 5.9%        | 7.89                           | 7.44                      | 5.7%        | 7.32                          | 6.87                      | 6.1%       |\r\n| 32    | 15691.37                            | 16120.43                        | 2.7%        | 18.53                           | 18.07                      | 2.5%        | 12.42                          | 12.05                      | 3.0%       |\r\n\r\nAccuracy score:\r\n|Tasks|Version|     Filter     |n-shot|  Metric   |   |Value |   |Stderr|\r\n|-----|------:|----------------|-----:|-----------|---|-----:|---|-----:|\r\n|gsm8k|      3|flexible-extract|     5|exact_match|↑  |0.9568|±  |0.0056|\r\n|     |       |strict-match    |     5|exact_match|↑  |0.9575|±  |0.0056|\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [x] The test plan, such as providing test command.\r\n- [x] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "f05b88751ec74b66ee27eb38f65fdfdda6161239",
      "pr_number": 57870,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57870",
      "pr_title": "[CI][ROCm] Make Python-only installation failures blocking",
      "pr_body": "- Make AMD Python-only Installation failures block the pipeline.\r\n- Remove its explicit `soft_fail: true` override so the job inherits the hard-fail default.\r\n- Preserve the job's installation commands, scheduling, dependencies, and timeout.\r\n\r\nThe audit prompted by [AMD CI build 13265](https://buildkite.com/vllm/amd-ci/builds/13265) found that Python-only Installation explicitly allowed its AMD job to fail softly. An installation failure could therefore leave the pipeline passing. The parent step already uses the default hard-fail policy. \r\n\r\nThe original open-PR audit found no matching change for this job. Implementation, investigation, validation, and this description were AI-assisted.\r\n"
    },
    {
      "sha": "76ffb0d37488988ec158cab609d5569e9f9ca34c",
      "pr_number": 57651,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57651",
      "pr_title": "[Model][Engram] Share host tables across co-located DP replicas by default",
      "pr_body": "## Purpose\n\n`EngramConfig.dp_shared_memory` (#56512) keeps one copy of the CPU-offloaded Engram tables per node instead of one per DP replica, with no per-step Engram DP collectives, but it had to be requested by hand and failed startup whenever its requirements were not met. This PR turns it on by default wherever it can work and falls back otherwise:\n\n- `dp_shared_memory` defaults to unset and resolves to enabled when `cpu_offload` is on, `data_parallel_size > 1` and elastic EP is off. An explicit `true` keeps the existing strict validation.\n- Before the layers are built, `can_share_engram_tables` falls back to the existing DP head sharding (same host footprint, one table per node) when the node-local Engram DP group has size 1 (replicas not co-located) or `/dev/shm` is smaller than the full tables: 188.8 GiB for DeepSeek-V4.1-Flash, against Docker's default 64 MiB. Both cases log a `warning_once` pointing at `--shm-size` / `--ipc=host`. The check compares against the tmpfs total, which is identical on every rank of a node, so no collective is needed to keep the decision consistent.\n- The `load_format` allowlist is dropped: `dummy` (`fill_`) and `sharded_state` (`copy_`) write identical bytes into the shared mapping on every rank, so the restriction guarded nothing.\n\nDuplicate-work check (2026-09-19): [#57195](https://github.com/vllm-project/vllm/pull/57195) shares host lookups for SP token owners, [#56757](https://github.com/vllm-project/vllm/pull/56757) backs tables with disk files, [#57491](https://github.com/vllm-project/vllm/pull/57491) keeps tables in host memory on ROCm. None changes the `dp_shared_memory` default or adds a `/dev/shm` fallback.\n\n## Test plan\n\n```bash\n.venv/bin/python -m pytest tests/distributed/test_engram_dp_shard.py \\\n  tests/test_config.py -q -k engram\n.venv/bin/pre-commit run --files vllm/config/engram.py vllm/config/vllm.py \\\n  vllm/models/deepseek_v41/nvidia/engram.py vllm/models/deepseek_v41/nvidia/model.py \\\n  tests/test_config.py tests/distributed/test_engram_dp_shard.py\n.venv/bin/pre-commit run mypy-3.12 --hook-stage manual --files vllm/config/engram.py \\\n  vllm/config/vllm.py vllm/models/deepseek_v41/nvidia/engram.py \\\n  vllm/models/deepseek_v41/nvidia/model.py\ngit diff --check\n```\n\n## Test result\n\n- 68 passed on 4xGB200, including the four-process shared-memory storage tests, the new default-resolution cases (offloaded DP on; no offload, DP=1 and elastic EP off) and the new `/dev/shm`-too-small fallback test.\n- Pre-commit, mypy-3.12 and `git diff --check` passed.\n\n## Model evaluation\n\nDeepSeek-V4.1-Flash (`dba1be0a`), one GB200 node, DEP4 (TP1 x DP4, EP), DSpark 5 tokens with adaptive verification, **no `--engram-config` flag**. GSM8K 5-shot via MQA, completions API, `temperature=0`, `max_tokens=1024`, n=1319.\n\n| Metric | Value |\n| --- | --- |\n| exact_match/flexible-extract | 0.931 |\n| exact_match/strict-match | 0.931 |\n| num_request_errors | 0 |\n\nThis matches the 0.9303 recorded for the same model on main (2026-09-13). The engine log confirms the default took the shared path on its own: `Resolved Engram configuration: EngramConfig(cpu_offload=True, embedding_across_dp=False, dp_shared_memory=True)`, `Engram node-local DP group size: 4 (TP=1)`, and both tables reported `94.42 GiB shared across DP replicas` with no fallback warning. On the node, `/dev/shm` held 190 GiB and `free` reported 191 GiB shared, one copy of the tables rather than four private pinned copies.\n\nAI assistance was used for implementation, testing, and this draft (Claude Code). Human review of every changed line and independent execution of relevant tests are required before submission.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n"
    },
    {
      "sha": "0ff0477ff955154f37db48791260cdf27fa35e60",
      "pr_number": 57526,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57526",
      "pr_title": "[Perf][ROCm] Add a ROCm path for Hy4 and compile the backbone",
      "pr_body": "## Summary\r\n\r\n- Adds `vllm/models/hy_v4/amd/`, so ROCm no longer runs the NVIDIA backbone directly, matching the layout every other model in `vllm/models/` already has.\r\n- Gives the ROCm backbone `@support_torch_compile`. Without it, vLLM silently resolved `cudagraph_mode` to `NONE` and served the whole 78-layer model eager.\r\n- Restores cudagraph capture on ROCm: **13x lower decode latency** (311.7 → 23.8 ms TPOT) in the default cudagraph mode, plus a regression test so this cannot silently return.\r\n\r\n## Motivation\r\n`hy_v4` was the only model under `vllm/models/` without an `amd/` path, so ROCm ran the NVIDIA backbone directly. This adds one, matching `minimax_m3`, `kimi_k3` and others, and gives subsequent ROCm kernel work somewhere to land. `nvidia/model.py` is untouched; the draft head has no ROCm implementation yet and still resolves to `nvidia/mtp.py`.\r\nPrior to this PR the shared NVIDIA backbone had no `@support_torch_compile`, which on ROCm showed up in the server startup logs as:\r\n```\r\nWARNING [vllm.py] `torch.compile` is turned on, but the model tencent/Hy4-preview-FP8\r\n                  does not support it.\r\nWARNING [compilation.py] Cudagraph mode PIECEWISE requires piecewise capture, but the\r\n                  loaded model provides neither a compiled submodule nor breakable\r\n                  CUDA graphs. Overriding to NONE.\r\nWARNING [model_runner.py] Skipping encoder and decoder CUDA graph capture.\r\n```\r\nBoth cudagraph paths were affected: explicit `PIECEWISE` had no compiled submodule to attach to and was overridden to `NONE`, while the default `FULL_AND_PIECEWISE` fell through `without_piecewise()` to `FULL_DECODE_ONLY`, which dies at capture on gfx942. \r\n\r\n\r\n## Results\r\n\r\n8×MI325X (gfx942), TP8, `tencent/Hy4-preview-FP8`,\r\nImage: `vllm/vllm-openai-rocm:nightly@sha256:f169e8df365b5112a1e6500543e13d8161374277c22c94d8ed60817324c101e1`\r\n| cudagraph mode | TPOT | output tok/s | speedup |\r\n|---|---|---|---|\r\n| `NONE` -> eager fallback, before this PR | 311.7 ms | 3.2 | 1.0x |\r\n| `PIECEWISE` -> explicit | 80.6 ms | 12.4 | 3.9x |\r\n| `FULL_AND_PIECEWISE` -> default | **23.8 ms** | **42.1** | **13.1x** |\r\n\r\nAfter the fix the default resolves to `FULL_AND_PIECEWISE` and captures both passes (57 FULL + 106 PIECEWISE) with no downgrade.\r\n\r\n\r\n## Correctness\r\n\r\n- `tests/models/test_hyv4_rocm.py`: 6/6 passed. They run a toy Hy4 (2 layers, dummy weights), so they check wiring rather than model quality \r\n- The model produces coherent output on ordinary reasoning and arithmetic prompts, with correct answers and clean stop behaviour.\r\n\r\n| max_gen_toks | GPQA-Diamond (flexible-extract) | correct | blanks     | blank acc | non-blank acc    |\r\n|--------------|---------------------------------|---------|------------|-----------|------------------|\r\n| 32k          | 79.80% ± 2.86                   | 158/198 | 26 (13.1%) | 0/26 (0%) | 91.86% (158/172) |\r\n| 64k          | 85.35% ± 2.52                   | 169/198 | 5 (2.5%)   | 0/5  (0%) | 87.56% (169/193) |\r\n\r\nIn the table above A \"blank\" is a response where the model exhausted its generation budget mid-reasoning and never emitted a final answer choice, so the harness scores it wrong; raising max_gen_toks from 32k to 64k cuts these from 13.1% to 2.5% and accounts for the entire +5.6-point gain, since accuracy on responses that actually completed is statistically unchanged (91.9% vs 87.6%, overlapping at ±2.9/±2.5).\r\n\r\nThe new test runs `VLLM_COMPILE` + `PIECEWISE` and asserts the backbone reaches the compiler and that graphs are actually captured. Existing ROCm coverage pins compilation mode to `NONE`, so it could not observe this.\r\n\r\n\r\n<details>\r\n<summary><b>Serving command</b></summary>\r\n\r\n```bash\r\nVLLM_ROCM_USE_AITER=1 \\\r\nVLLM_ROCM_QUICK_REDUCE_QUANTIZATION=INT4 \\\r\nHF_HUB_CACHE=/data/hf_home \\\r\nvllm serve tencent/Hy4-preview-FP8 \\\r\n  --trust-remote-code \\\r\n  --tensor-parallel-size 8 \\\r\n  --port 8000 \\\r\n  --kv-cache-dtype fp8 \\\r\n  --max-model-len 65536 \\\r\n  --reasoning-parser hy_v4\r\n```\r\n\r\nThe `PIECEWISE` row adds `--compilation-config '{\"cudagraph_mode\":\"PIECEWISE\"}'` to the same command."
    },
    {
      "sha": "67513c8b67e2439934ce7c0ba5a18c2e859f5c53",
      "pr_number": 57874,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57874",
      "pr_title": "[Bugfix][DSV4.1] Restrict mHC overlap to full CUDA graphs",
      "pr_body": "## Problem and fix\n\nDSV4.1's small-batch eager warmup selected overlap, while breakable PIECEWISE capture selected Mega mHC. That could leave Mega mHC's stream-local barriers uninitialized until capture, causing startup to fail.\n\nUse overlap only during FULL CUDA graph capture, still bounded by the existing 16-token cutoff and hardware requirements. Eager execution/warmup and PIECEWISE execution retain native mHC dispatch (Mega mHC where supported). Eager warmup therefore initializes Mega mHC on the capture stream. FULL replay retains the captured overlap kernels without executing the Python check per token.\n\nThe selection checks actual stream capture and excludes PIECEWISE. This works with V2's existing FULL-capture context, which advertises NONE while the runner explicitly wraps the model in `torch.cuda.graph`. CUDA graph runner/context code, AMD model code, and shared model-state code are unchanged. There is no separate warmup helper or cache. Mega mHC eligibility remains centralized in its implementation module.\n\n## Validation\n\n```bash\n.venv/bin/python -m pytest tests/kernels/test_mhc_kernels.py \\\n  -k decoder_mixes_match_torch -q -rs\n\n.venv/bin/pre-commit run --files \\\n  vllm/models/deepseek_v41/nvidia/model.py \\\n  vllm/models/deepseek_v41/nvidia/ops/mhc.py \\\n  vllm/models/deepseek_v41/nvidia/ops/mega_mhc.py \\\n  tests/kernels/test_mhc_kernels.py\n```\n\n- **20 passed**, 285 deselected on GB200. Decoder/reference parity covers broadcast, pipeline, residual, and Engram entry paths under disabled overlap, eager execution, FULL capture/replay, PIECEWISE, and larger batches. The FULL cases verify overlap is called during capture, never eager warmup.\n- Pre-commit passed.\n- Fresh-process real-weight GB200 TP4 FULL_AND_PIECEWISE startup/capture passed on all four ranks with capture sizes [1,2,4,8]. Both measured exact8192-input/1024-output C1 requests completed, with no monitored inference-time JIT warnings. Mean TPOT 4.553 ms, median ITL 4.385 ms, P99 ITL 10.368 ms. These two requests are a serving smoke check, not a matched performance comparison or a tail-latency improvement claim.\n- Earlier revision's GSM8K 128-example smoke: 126/128 (98.44%) flexible extraction, strict 106/128. Checkpoint revision `dba1be0a40aa45a94ad051997016db3960a90277`, TP4, FlashInfer FP8 KV, MXFP4 sparse indexer, greedy, five-shot, thinking disabled. This predates the FULL-only-overlap revision and is not a new accuracy evaluation of it. Eager small-batch execution now uses the existing native path, so cross-path bitwise equivalence is not claimed.\n\n## Related work and AI assistance\n\nOpen-PR checks found no equivalent fix for the overlap/Mega mHC warmup mismatch exposed by #57603. #57843 warms standalone DeepGEMM prenorm projection specializations; it does not initialize Mega mHC's stream-local barriers. This PR changes DSV4.1 dispatch policy instead.\n\nOpenAI Codex assisted with implementation and validation. The human submitter reviewed/tested an earlier revision and requested this FULL-only overlap policy; the revised validation above was performed by the agent.\n"
    },
    {
      "sha": "97dc6b19d2fe92b794b49e684677a2e1a8b2c540",
      "pr_number": 57885,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57885",
      "pr_title": "[Perf][Attention] Avoid redundant sparse attention metadata operations",
      "pr_body": "## Purpose\n\nSparse attention metadata preparation launches unnecessary GPU work on every decode step. Write SWA token validity directly into its persistent buffer, and compute compressed prefill sequence lengths only when the batch contains prefills.\n\nFor the tested DSV4.1 TP4 configuration, this eliminates four validity copies and one unused division per decode step. No new kernels, flags, or dispatch thresholds are introduced.\n\n## Validation\n\nReal-weight DeepSeek-V4.1-Flash on 4x GB200, TP4, no speculative decoding, FlashInfer sparse attention, FULL_AND_PIECEWISE CUDA graphs, concurrency 1, exact 8192-input/1024-output tokens; eight requests per arm in the same loaded server:\n\n| Metric | Baseline | Candidate |\n| --- | ---: | ---: |\n| Mean TPOT | 4.467 ms | 4.457 ms |\n| Median ITL | 4.383 ms | 4.365 ms |\n| Median request TPOT | 4.414 ms | 4.422 ms |\n\nThis is a small reduction in typical token interval; mean TPOT is effectively flat. No material end-to-end speedup or tail-latency improvement is claimed. Serving measurements and evaluation were collected before rebasing this unchanged two-file patch onto current main, with the independent breakable-graph warmup fix present in both arms.\n\nGSM8K five-shot greedy smoke: 126/128 (98.4375%) flexible extraction, matching the prior smoke score; strict extraction 107/128. This is a smoke evaluation, not a full accuracy result. Independent serving outputs were not bitwise identical (3/8 exact text matches).\n\n```bash\n.venv/bin/python -m pytest \\\n  tests/v1/attention/test_deepseek_v4_swa_visible.py \\\n  tests/v1/attention/test_dspark_noncausal_sparse_mla.py \\\n  tests/v1/attention/test_indexer_deepseek_v4_slot_mapping.py \\\n  tests/v1/worker/test_gpu_input_batch_v2.py \\\n  tests/v1/worker/test_gpu_model_runner_v2.py -q -rs\n\n.venv/bin/pre-commit run --files \\\n  vllm/v1/attention/backends/mla/indexer.py \\\n  vllm/v1/attention/backends/mla/sparse_swa.py\n```\n\nTests: 101 passed, 8 expected skips for unsupported attention backend/cache combinations. The same test results and pre-commit pass were confirmed after rebasing onto current main.\n\n## Duplicate check and AI assistance\n\nOpen-PR searches for validity, compressed sequence lengths, and metadata preparation found no equivalent change. #57756 concerns ROCm-specific metadata fusion; #57458 concerns GLM sparse MLA preparation. Neither modifies these operations. Constant-offset reuse overlapping #35561 was excluded.\n\nAI assistance: OpenAI Codex helped implement and validate this change.\n\nEvaluation command (against the candidate server):\n\n```bash\n.venv/bin/python -m lm_eval run --model local-chat-completions \\\n  --model_args 'model=dsv41-gsm8k,base_url=http://127.0.0.1:8003/v1/chat/completions,num_concurrent=2,timeout=600,max_retries=2,max_gen_toks=1024,max_length=16384,seed=123' \\\n  --tasks gsm8k --num_fewshot 5 --batch_size 1 \\\n  --apply_chat_template --fewshot_as_multiturn \\\n  --gen_kwargs max_gen_toks=1024 temperature=0.0 do_sample=False \"chat_template_kwargs={'thinking': False}\" \\\n  --seed 123,123,123,123 --log_samples --output_path gsm8k --limit 128\n```\n"
    },
    {
      "sha": "3c84ad13ed3f9fcd816bf1d15acb83c28cfa02f9",
      "pr_number": 48416,
      "pr_url": "https://github.com/vllm-project/vllm/pull/48416",
      "pr_title": "[Bugfix] Fix xgrammar feature gate bypass when JSON Schema type is a list",
      "pr_body": "## Purpose\n\nJSON Schema allows `type` to be either a string or a list. Nullable type lists are also used in [OpenAI's Structured Outputs examples](https://openai.com/index/introducing-structured-outputs-in-the-api/):\n\n```json\n{\"type\": [\"string\", \"null\"]}\n```\n\n`has_xgrammar_unsupported_json_features` previously only recognized scalar types. As a result, schemas such as:\n\n```json\n{\n  \"type\": [\"string\", \"null\"],\n  \"pattern\": \"^[0-9-]+$\",\n  \"maxLength\": 10\n}\n```\n\nbypassed the existing unsupported-feature check. In the default `auto` mode, the request stayed on xgrammar, whose compiled grammar can accept strings longer than `maxLength`.\n\nThis PR normalizes scalar and list-valued types before applying the existing checks. Unsupported schemas now fall back to another backend, while supported list-type schemas remain on xgrammar.\n\n## Tests\n\n- Added numeric, string, array, and object list-type regressions.\n- Added supported list-type coverage.\n- Verified `auto` backend fallback.\n- `79 passed` with xgrammar 0.2.1, 0.2.3, and 0.2.7.\n- Pre-commit checks passed.\n\n---\n\n*AI-assisted: the implementation and tests were drafted with Codex. I reviewed the changes and verified the behavior locally.*\n"
    },
    {
      "sha": "9598487bc1829504c8af81d6007ab6b9deeaa15a",
      "pr_number": 53758,
      "pr_url": "https://github.com/vllm-project/vllm/pull/53758",
      "pr_title": "[Bugfix][Model] Mistral3: align placeholder grid with public processor",
      "pr_body": "## Purpose\n\nFollow-up to #52874.\n\nThat PR fixed Mistral3 placeholder grids for processor size overrides, but the new grid calculation matches a direct call to `hf_processor.image_processor(...)`. The public `PixtralProcessor(...)` path also applies `spatial_merge_size`, so it rounds image dimensions to the merged 28-pixel stride rather than the vision encoder's 14-pixel patch size.\n\nFor dimensions that are not already aligned, vLLM can therefore still produce a different placeholder sequence from Transformers. For example:\n\n- A 29×29 image produces 4 image tokens through the public HF processor, while current vLLM predicts 1.\n- A 1540×1700 image with `longest_edge=1000` produces 1,188 image tokens through HF, while current vLLM predicts 1,152.\n\nThis change computes the grid using `patch_size * spatial_merge_size` and updates the regression test to compare the exact placeholder sequence from the public `AutoProcessor` API.\n\nThis was likely missed in #52874 because its test called the lower-level image processor directly. The test and implementation agreed with each other, but bypassed the wrapper behavior used in normal processing.\n\nThis does not duplicate an open PR; repository searches for the same Mistral3 placeholder-grid behavior returned no matches.\n\n## Test Plan\n\n```bash\n.venv/bin/python -m pytest -q \\\n  tests/models/multimodal/processing/test_mistral3.py \\\n  -k processor_size_override\n\npre-commit run --files \\\n  vllm/model_executor/models/mistral3.py \\\n  tests/models/multimodal/processing/test_mistral3.py\n```\n\n## Test Result\n\n- DGX Spark: all 20 parameterized Mistral3 public-processor parity cases passed.\n- The 29×29 and 1540×1700 boundary cases above now match the exact HF image/break/end placeholder sequence.\n- Ruff formatting and lint checks passed.\n- No model-quality evaluation was run because the patch does not change model weights or inference numerics. The model-facing change is placeholder construction, which is covered by exact parity against the public HF processor.\n\nAI assistance from OpenAI Codex was used for investigation, implementation, and testing. The commits include `Assisted-by` and DCO `Signed-off-by` trailers.\n\n---\n<details>\n<summary>Essential Elements of an Effective PR Description Checklist</summary>\n\n- [x] Purpose and relationship to the previous PR\n- [x] Test commands\n- [x] Before/after results and HF parity evidence\n- [x] No documentation update required\n\n</details>\n"
    },
    {
      "sha": "59f15272802077724c4ed483ea8ad393725554e4",
      "pr_number": 57769,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57769",
      "pr_title": "[Bugfix] Fix Whisper engine crash on audio clips longer than 30s",
      "pr_body": "## Purpose\n\nOffline `LLM.generate` with Whisper and an audio clip longer than 30 s kills the engine on main and on v0.29.0:\n\n```\nRuntimeError: The size of tensor a (1750) must match the size of tensor b (1500) at non-singleton dimension 0\n```\n\nEvery later request then fails with `EngineDeadError`. v0.26.0 accepts the same input and truncates the clip to 30 s, which is what the HF feature extractor does by default.\n\nThe cause is the same as in #56912 (Qwen2-Audio). Since #53275 the multimodal processor passes `truncation=False` to the HF processor so that the placeholder text is not cut. `WhisperProcessor` forwards the same kwarg to `WhisperFeatureExtractor`, which then stops truncating audio to its 30 s window. A 35 s clip becomes 3500 frames, which is 1750 positions after the conv, and the encoder has 1500.\n\nThe fix is the same line as in #56912: `WhisperMultiModalProcessor._get_hf_mm_inputs` passes `truncation=True`. Clips of up to 30 s are processed exactly as before.\n\n`/v1/audio/transcriptions` and `/v1/audio/translations` are not affected, because the server splits audio into chunks of at most 30 s before it reaches the processor, and Whisper does not serve the other endpoints. Only offline use with a longer clip hits this.\n\nI did not find an existing issue or PR for this (searched for whisper truncation, whisper 30s, and the error message). Of the other audio models I could load, GLM-ASR and Qwen3-ASR handle long clips correctly and Qwen2-Audio is already fixed. I have not checked the rest.\n\nRepro:\n\n```python\nimport numpy as np\nfrom vllm import LLM, SamplingParams\n\nsr = 16000\nt = np.arange(sr * 35) / sr  # 35 s, longer than Whisper's 30 s window\naudio = (0.1 * np.sin(2 * np.pi * 440 * t)).astype(np.float32)\n\nllm = LLM(\"openai/whisper-small\", max_model_len=448, limit_mm_per_prompt={\"audio\": 1})\nout = llm.generate(\n    {\n        \"prompt\": \"<|startoftranscript|><|en|><|transcribe|><|notimestamps|>\",\n        \"multi_modal_data\": {\"audio\": (audio, sr)},\n    },\n    SamplingParams(temperature=0, max_tokens=50),\n)\nprint(out[0].outputs[0].text)\n```\n\n## Test Plan\n\n```bash\npytest tests/models/multimodal/processing/test_whisper.py\npytest tests/models/multimodal/processing/test_common.py -k whisper\npytest tests/models/multimodal/processing/test_tensor_schema.py -k whisper\n```\n\nThe new test runs the processor on 5 s, 30 s and 35 s clips and checks that `input_features` always has the feature extractor's 3000 frames. It needs no GPU.\n\nEnd to end on one RTX 6000 Ada: the repro above, and a second script that sends 25 s, 35 s, 65 s and 25 s clips of real speech (FLEURS `cmn_hans_cn`, concatenated) to one `LLM` in that order, with `temperature=0`.\n\nI did not run `tests/models/multimodal/generation/test_whisper.py`. It asks for `gpu_memory_utilization=0.9` and my GPU is shared.\n\n## Test Result\n\nNew test at e378275a8:\n\n| | 5 s | 30 s | 35 s |\n| --- | --- | --- | --- |\n| main | pass | pass | fail, `assert 3500 == 3000` |\n| this PR | pass | pass | pass |\n\n`test_common.py -k whisper`: 6 passed. `test_tensor_schema.py -k whisper`: 2 passed.\n\nRepro: v0.29.0 and main (e378275a8) die with the error above, this PR prints a transcript.\n\nReal speech, `openai/whisper-small`:\n\n| | 25 s | 35 s | 65 s | 25 s again |\n| --- | --- | --- | --- | --- |\n| v0.26.0 | ok | ok | ok | ok |\n| v0.29.0 | ok | `EngineDeadError` | `EngineDeadError` | `EngineDeadError` |\n| main (e378275a8) | ok | `EngineDeadError` | `EngineDeadError` | `EngineDeadError` |\n| this PR | ok | ok | ok | ok |\n\nWith this PR every transcript is identical to the v0.26.0 one, and the 35 s and 65 s transcripts are identical to each other, as expected when both are cut to the first 30 s. `openai/whisper-large-v3-turbo` with this PR also returns all four.\n\nAI assistance: Claude helped find the affected model, write the test and run the comparisons. I reviewed the change and the results.\n"
    },
    {
      "sha": "8ec00ec2bc2d294b742f7814041d3a3043e0a90e",
      "pr_number": 57868,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57868",
      "pr_title": "[Tests] Release HF embedding weights after prompt extraction",
      "pr_body": "- Compute reference prompt embeddings under `torch.no_grad()` in the language generation comparison test.\r\n- Prevent retained prompt values from keeping HF embedding weights alive after runner exit.\r\n- Preserve embedding values, MiniCPM scaling, and existing generation/logprob assertions.\r\n\r\nThe investigation of [AMD-CI 13265 Language Extended Generation](https://buildkite.com/vllm/amd-ci/builds/13265/list?sid=01a0be0c-75bb-4cb2-97ce-bc0fd92212d2&tab=output) exposed an independently reproducible reference-model lifetime issue. Although Transformers loads models in evaluation mode and disables gradients inside generation, the test's later embedding lookup still builds an autograd graph. A controlled Qwen probe showed those references retained embedding weights after HF shutdown, with direct placement pinning roughly 16.47 GB of reserved GPU storage. Disabling gradients at the inference-value producer removes that ownership without changing embedding or generation results, and applies equally across platforms. \r\n\r\nInvestigation, implementation, validation, and this description were AI-assisted.\r\n"
    },
    {
      "sha": "6654bcbfca86a8c17c8a377f6813c7a86e64cd6d",
      "pr_number": 57277,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57277",
      "pr_title": "[MRV2][XPU] use xpu sample kernel in mrv2 sampler",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\nThis PR adds MRV2 sampler support for XPU using the fused top-k/top-p sampling kernel.\r\n\r\n## Test Plan\r\nset topp=0.9\r\n```bash\r\n  ZE_AFFINITY_MASK=0,1 vllm bench latency \\\r\n    --model Qwen/Qwen3-0.6B \\\r\n    --enforce-eager \\\r\n    --batch-size 32 \\\r\n    --input-len 128 \\\r\n    --output-len 256 \\\r\n    --num-iters-warmup 3 \\\r\n    --num-iters 100\r\n```\r\n\r\n**bs = 32** \r\n- w/o xpu sampler   Avg latency: 4.4615468850506295 seconds\r\n- w/ xpu sampler     Avg latency: 3.824720986480097 seconds\r\n\r\n**bs = 1**  \r\n- w/o xpu sampler   Avg latency: 3.9164883875501255 seconds\r\n- w/ xpu sampler     Avg latency: 3.484434666330162 seconds\r\n\r\n\r\n## Test Result\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "76ee20c1ec5a9d6e094ecaecd46d71a5d591d05c",
      "pr_number": 57864,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57864",
      "pr_title": "[ROCm][Tests] Avoid pooling cleanup waits against live shared engines",
      "pr_body": "- Skip the ROCm wait for GPU usage below 10% in the three pooling cleanup fixtures.\r\n- Retain garbage collection and accelerator cache clearing on every platform.\r\n- Preserve runner-owned startup/shutdown waits and the existing non-ROCm cleanup behavior.\r\n\r\nPR [#57371](https://github.com/vllm-project/vllm/pull/57371) added function-scoped memory waits beneath longer-lived pooling model fixtures. The [CI 90039 group](https://buildkite.com/vllm/ci/builds/90039/list?sid=01a0bd6c-73f3-4a23-9ed7-f28bd586585b&tab=output) and [AMD-CI 13265 group](https://buildkite.com/vllm/amd-ci/builds/13265/list?sid=01a0be0c-75c1-4cce-ab39-5d3f5c8c903a&tab=output) repeatedly spent 240 seconds waiting while their shared ColQwen engines remained alive. Independent git bisects of both CI commit ranges identified that PR. Local HIP observations show usage remains above 79% between shared tests and drops to 0.25% when the module owner shuts down. The ROCm guard removes this invalid between-test wait while preserving synchronization after resources are released.\r\n\r\nInvestigation, implementation, validation, and this description were AI-assisted.\r\n"
    },
    {
      "sha": "a15dbbd63daaba913b3302a858ad7e20e6dcc219",
      "pr_number": 57855,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57855",
      "pr_title": "[XPU][CI] fallback to 7-args of moe_align_block_size",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\nAdd temporary XPU compatibility for `moe_align_block_size` by using the existing 7-argument XPU op schema. Explicitly reject unsupported `scatter_idx` usage to avoid returning uninitialized data.\r\n\r\n## Test Plan\r\n\r\n## Test Result\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "3df4ae153eb385e27b52f26c81f8edb9e20b9984",
      "pr_number": 57783,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57783",
      "pr_title": "[Core][KDA] Generalize Mamba prefill checkpoint builder and exporter",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\n\r\n\r\n  Generalize internal prefill checkpoint metadata and state export so that future\r\n  KDA and Mamba2 backends can share the same scheduling and metadata logic.\r\n\r\n## Test Plan\r\n\r\n## Test Result\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "ff0c8854946b75dc0fe160756e8adff10a8da053",
      "pr_number": 57589,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57589",
      "pr_title": "[Bugfix] DiffusionGemma: fix multimodal support",
      "pr_body": "## Purpose\r\n\r\nDiffusionGemma reuses `Gemma4ForConditionalGeneration._process_image_input` for its image path. That method reads `self._enable_mm_lora`, which Gemma4's constructor sets. DiffusionGemma's doesn't, so any request with an image raises `AttributeError` in the encoder runner and takes the engine down.\r\n\r\nSet the flag in DiffusionGemma's constructor the same way Gemma4 does.\r\n\r\nRequired for #57250 to support multimodal inputs.\r\n\r\n## Test plan\r\n\r\nHardware: 1 DGX Spark.\r\n\r\nSend an image.\r\n\r\n```\r\nvllm serve nvidia/diffusiongemma-26B-A4B-it-NVFP4 --max-model-len 4096 --diffusion-config '{\"canvas_length\": 64}'\r\n```\r\n\r\n```\r\ncurl -s localhost:8000/v1/chat/completions -H 'content-type: application/json' -d '{\r\n  \"model\": \"nvidia/diffusiongemma-26B-A4B-it-NVFP4\", \"max_tokens\": 32,\r\n  \"messages\": [{\"role\": \"user\", \"content\": [\r\n    {\"type\": \"image_url\", \"image_url\": {\"url\": \"https://upload.wikimedia.org/wikipedia/commons/thumb/4/47/PNG_transparency_demonstration_1.png/280px-PNG_transparency_demonstration_1.png\"}},\r\n    {\"type\": \"text\", \"text\": \"What is in the image?\"}]}]}'\r\n```\r\n\r\n## Test results\r\n\r\nWithout patch: 500 error (AttributeError)\r\n\r\nWith patch: a successful response\r\n\r\n---\r\n\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [x] The test plan, such as providing test command.\r\n- [x] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n"
    },
    {
      "sha": "9a70c233cd5d3ef3b14d79c582a6f9ddfa03cf80",
      "pr_number": 57643,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57643",
      "pr_title": "[Perf][DSV4.1] Fuse TP all-reduce with mHC input preparation",
      "pr_body": "## Purpose\n\nFuse TP all-reduce, mHC post mixing, delayed collapse, and RMSNorm at DSV4.1 decoder boundaries. This replaces three launches with one, preserves the BF16 boundaries, and retains coefficient overlap. The kernel reuses the existing MNNVL workspace without an additional persistent communication allocation.\n\nEnabled by default for eligible SM100 TP4 forwards with 1–16 tokens, including speculative decoding. The selector requires hidden size 5120, hc_mult=4, EP disabled, and a multicast workspace; it does not restrict DP/PP size, LoRA, or attention backend. The existing overlap-support checks still apply, including disabling overlap for ubatching. Larger batches and graph boundaries use the existing operations. Sixteen is the largest size with a validated gain, not a measured crossover.\n\nBuilds on #57603, now merged into main, which overlaps coefficient computation but does not fuse collectives. This differs from #51244's terminal mHC/head fusion. Open-PR searches for `mhc allreduce` and `mhc fusion` found no duplicate of this decoder-boundary collective fusion.\n\nThe independent startup warmup fixes have been extracted: DeepGEMM mHC projection coverage is in #57843, and cached FlashInfer MXFP8 tactics are in #57842. Neither is included in this fusion PR.\n\n## Communication choice and token cutoff\n\nKeep multicast and the 16-token cutoff. A controlled GB200 TP4 experiment replaced multicast stores with explicit stores to each of the four peers, retaining the same math, Lamport synchronization, cleanup, and PDL. Push did not consistently improve performance:\n\n| Tokens | Multicast kernel | Push kernel | Multicast model forward | Push model forward |\n| --- | ---: | ---: | ---: | ---: |\n| 1 | 14.348 µs | 14.634 µs | 4.054 ms | 4.060 ms |\n| 8 | 14.395 µs | 14.972 µs | 5.724 ms | 5.728 ms |\n| 16 | 13.910 µs | 15.795 µs | 7.133 ms | 7.130 ms |\n\nKernel timings use cold-L2 CUDA-graph replay, five samples of 50 replays, and the maximum rank time. Model timings use the same synthetic-weight model instance, KV8704, identical tactics, and three samples of 20 replays; they exclude the output head and serving. Multicast and push model outputs were byte-identical on every rank at all nine tested sizes (1–128).\n\nExtending fusion to 24, 32, 48, 64, 96, and 128 tokens regressed model-forward time in this screen, despite isolated kernel gains; coefficient overlap remained disabled above 16. Some nonstandard sizes used default tactics because exact cache buckets were absent. A follow-up using normal serving autotune buckets confirmed the gain at 1 and 16 tokens (4.667→4.060 ms and 7.783→7.127 ms). Near-boundary runs at 17 and 20 tokens failed the unfused baseline's native-top-k correctness check, so no timings from those shapes are accepted. This supports retaining 16 conservatively; it does not establish the exact crossover. No new model accuracy evaluation was run for the experimental variants.\n\nMulticast requires a supported NVLink/NVSwitch configuration and successful group memory setup; GPU generation alone does not establish support. Explicit peer stores could broaden support to Hopper-or-newer systems with suitable peer-memory access without multicast. That would require separate allocation/dispatch support and hardware validation: this PR remains SM100 TP4, and the experimental push variant reused the existing workspace. Push is a possible portability fallback, not a demonstrated GB200 performance improvement.\n\n## Latest model-forward comparison against main\n\nMeasured candidate `dd63c1fa00` (runtime source identical to the subsequently merged PR head `7be123b000`) versus main `d2983f2f16` fetched at benchmark start, including merged overlap and the latest DSpark block-table fix in both arms. GB200 ×4, TP4/DP1, FlashInfer FP8 attention, MXFP4 sparse-logits indexer, auto MoE, KV8704, max model length 1M. Matched synthetic weights/KV and 210 cached tactics with normal hybrid token buckets. Five samples ×20 full-forward CUDA-graph replays after warmup, maximum rank time per sample; median reported below.\n\n| Mode | Batch | Component / tokens | Main | Candidate | Reduction |\n| --- | ---: | --- | ---: | ---: | ---: |\n| No DSpark | 1 | main / 1 | 4.651 ms | 4.055 ms | 12.8% |\n| No DSpark | 8 | main / 8 | 6.236 ms | 5.668 ms | 9.1% |\n| No DSpark | 16 | main / 16 | 7.806 ms | 7.127 ms | 8.7% |\n| DSpark | 1 | main / 6 | 5.867 ms | 5.280 ms | 10.0% |\n| DSpark | 1 | draft / 5 | 0.353 ms | 0.344 ms | 2.4% |\n\nDSpark uses fixed draft length5. These are model-forward measurements, excluding the output head, sampling, scheduling, HTTP and acceptance effects. The draft implementation is unchanged, so its small difference is not attributed directly to fusion. All accepted cases pass eager/graph and changed/restored-input replay checks with zero error. Initialization manifests and source/final tactic hashes match between arms.\n\nAn additional candidate DSpark B2 target case failed the harness native-top-k score check at layer2; no timing from that shape is accepted, and main B2 was not measured. This run does not establish B2 correctness or new model accuracy. Untimed setup permits compilation and strict JIT checking resumes before validation/timing; neither arm includes the extracted startup fixes #57842/#57843. The comparison measures warmed execution.\n\n## Test Result\n\nGB200 ×4, DeepSeek-V4.1-Flash revision `dba1be0a40aa45a94ad051997016db3960a90277`, TP4/DP1, FlashInfer FP8 attention, MXFP4 sparse-logits indexer, auto MoE, no DSpark. Baseline disables only this fusion before model construction; it includes #57603. Both arms use the same checkpoint, configuration and FlashInfer autotuning cache (all 168 entries reused, checksum unchanged).\n\nThe latest polling/RMSNorm follow-up loads peer payloads together and distributes norm partials for local shared-memory reads. PDL completion stays early for multi-token forwards and moves to the end for one token. Compared with the previous fused kernel, a same-instance synthetic KV8704 forward comparison measured B1 4.558/4.588→4.044/4.044 ms, B8 6.014/6.012→5.969/5.979 ms, and B16 7.787/7.782→7.794/7.822 ms. B1 baseline varied between runs (4.184 ms in the prior controlled run), so the largest paired percentage is not a robust gain estimate. B16 is 0.1–0.5% slower in the final screen. Output bytes match on every rank for all three sizes; the final collective test also covers signed-zero sentinel collisions.\n\nThe serving and GSM8K measurements below are from the initial fused implementation (`fa59bb3883`). They have not been rerun after this output-equivalent follow-up.\n\n8K input / 1K output serving, concurrency 1, three requests per run, forced output length:\n\n| Run | Mean TPOT | Median ITL | P99 ITL |\n| --- | ---: | ---: | ---: |\n| Baseline | 4.947 ms | 4.951 ms | 5.034 ms |\n| Candidate, initial | 5.361 ms | 4.510 ms | 16.240 ms |\n| Candidate, repeat after GSM8K | 4.578 ms | 4.506 ms | 7.676 ms |\n\nThe warmed repeat reduces mean TPOT by 7.5%, but the initial candidate had stalls and tail latency remains higher. These short serving runs do not establish a tail-latency improvement. All requests completed, with 8192 input and 1024 output tokens each.\n\nA separate fixed-forward screen measured B8 6.565→6.028 ms (−8.2%) and B16 8.421→7.785 ms (−7.6%). B32 exercises fallback: 11.382→11.353 ms with identical output. These are three-sample synthetic-weight/cache measurements at KV8704, excluding the output head and serving.\n\nFull GSM8K, 1319 questions, five-shot chat, thinking disabled, greedy generation, seed 123, maximum 1024 generated tokens, concurrency 8:\n\n| Metric | Baseline | Candidate |\n| --- | ---: | ---: |\n| Flexible extraction | 1267/1319 (96.06%) | 1275/1319 (96.66%) |\n| Strict match | 79.08% | 79.98% |\n\nPrompt and target hashes match for every question. Floating-point ordering changes outputs; this is not bitwise model equivalence. The paired results have 11 candidate-only correct answers and 3 baseline-only correct answers.\n\n- Four-GPU collective test passed: changing sizes, all-gather interleaving in the shared workspace, changed-input CUDA-graph replay.\n- mHC regression selection: 137 passed.\n- 77 saved real operator inputs: maximum relative RMS 2.3e-5 for residuals and 1.5e-4 for normalized inputs.\n- CMake `_C_stable_libtorch` build and pre-commit checks passed.\n\n\n\nServing uses max model length 1048576, max sequences 8, max batched tokens 8192, 10 GiB KV cache, prefix caching disabled, Engram CPU offload, and `--compilation-config '{\"mode\":0,\"cudagraph_mode\":\"FULL_DECODE_ONLY\",\"cudagraph_capture_sizes\":[1,2,4,8]}'`. The sequence cap is a validation setting, not a recipe recommendation.\n\nAI assistance was used for implementation and validation. The human submitter confirmed review and relevant tests for the original submission; the warmup follow-up validation is reported separately below.\n\n## Warmup validation\n\nThe sparse-candidate alignment fix and its tests were extracted into #57667. The validation below was run on combined commit `2219a281f8`, before the split, and includes that fix.\n\nThe missing mHC specialization was DeepGEMM split=4, not a TileLang epilogue. Warmup now covers splits 1/4/16 for both projection widths on GB200. A fresh-cache test compiled six variants, then successfully checked 26 runtime shapes with further compiler invocations forbidden.\n\nA fresh real-weight TP4 server with `enable_jit_warmup=true` and `--jit-monitor-mode error --jit-monitor-verbose` completed 23 requests (736 generated tokens), spanning 3–8192 prompt tokens and mixed batches of up to eight requests. All four ranks enabled the monitor before requests: zero runtime Triton/TileLang/CuTeDSL JIT events, and no DeepGEMM compiler commands in this run. The 66 focused tests below and pre-commit checks passed.\n\nThe original serving measurements above explicitly disabled JIT warmup. These fixes close reproduced intermediate-prefill compilation gaps; they do not establish the cause of the original decode tails or a new steady-state speedup. GSM8K was not repeated for this startup/specialization follow-up, which leaves model arithmetic unchanged.\n\n## Test Plan\n\n```bash\nRAY_ADDRESS=local .venv/bin/python -m pytest \\\n  tests/distributed/test_custom_all_reduce.py -k all_reduce_mhc -q -rs\n.venv/bin/python -m pytest tests/kernels/test_mhc_kernels.py \\\n  -k 'deepseek_v41 or delayed_custom_op' -q -rs\n```\n\nCombined warmup follow-up at `2219a281f8` (66 passed, 2 deselected):\n\n```bash\n.venv/bin/python -m pytest tests/kernels/test_mhc_jit_warmup.py \\\n  tests/model_executor/test_flashinfer_autotune_warmup.py \\\n  tests/model_executor/layers/test_mla_short_prefill_indexer.py \\\n  -k 'not sparse_mqa_logits_prefill_matches_dense_masked and not deep_select' -q -rs\n```\n\n## Rebase validation\n\nRebased onto main `9b49f92344` (merged #57603). Retained the simplified overlap helper and existing fusion eligibility, multicast transport, and 16-token cutoff. The fused CUDA kernel and dispatcher are unchanged. Dropped the obsolete extraction commit so the sparse-indexer alignment fix already on main remains intact.\n\nAt `75e3082ab1`, all pre-commit hooks passed and the focused GB200 GPU suite passed (176 passed, 135 deselected):\n\n```bash\nRAY_ADDRESS=local .venv/bin/python -m pytest \\\n  tests/distributed/test_custom_all_reduce.py tests/kernels/test_mhc_kernels.py \\\n  -k 'all_reduce_mhc or deepseek_v41 or delayed_custom_op' -q -rs\n```\n\nServing benchmarks and GSM8K were not repeated for this rebase; the earlier results above retain their stated scope.\n\n## Broader dispatch validation\n\nRemoved benchmark-only restrictions on DP/PP size, attention backend, LoRA, and speculative decoding. The TP4 group, fixed kernel dimensions, EP exclusion, and multicast workspace checks remain. The kernel and 16-token cutoff are unchanged. The distributed replay test now includes 6/12-token fixed verification and shrinking/growing token counts.\n\nThe completed TP4 DSpark comparison (2026-09-19, before the rebase) used draft length five, FlashInfer FP8 attention, the same checkpoint and matched 210-entry tactics. Baseline retained overlap and disabled only fusion. Both arms scored 125/128 on a matched GSM8K subset; mean acceptance lengths were 3.739 and 3.730. All ten 8K-input/1K-output serving requests per arm succeeded. Mean TPOT was 1.451→1.247 ms, but random-prompt acceptance also changed (5.241→5.803), so this is not an isolated fusion speedup.\n\nThat validation used identical pinned-tactic and startup warmup workarounds in both arms. It is a subset accuracy check for fixed DSpark, not validation of every admitted backend, topology, LoRA configuration, or speculative method. Adaptive shape changes are covered at the distributed-kernel level. No new full GSM8K evaluation was run for this dispatch-only follow-up.\n\nFollow-up `2b4ce61608`: 182 GPU/dispatch tests passed, 135 deselected, using the focused command above on GB200 ×4; all pre-commit hooks passed.\n\n## Cached-tactic warmup extraction\n\nRemoved `_warmup_flashinfer_mxfp8_tactics`, its call site, and its unit test in `f6232a4f7f`. The independent two-file fix is in #57842 at `622e20e1ee`. The mHC projection-split warmup was subsequently extracted to #57843. The remaining autotune suite passed (4 tests), and pre-commit passed. Historical validation above that included the cached-tactic warmup still depends on the extracted fix; this PR alone does not close that runtime compilation gap.\n\n## DeepGEMM projection warmup extraction\n\nRemoved `MHCPrenormGemmWarmup`, its model registration, and tests in `ef850bb5c3`; the standalone fix is #57843. Historical combined validation above included both startup fixes (#57842 and #57843). The fusion kernel and dispatch are unchanged by the extraction.\n\n## Dispatch organization\n\nAt `70f0cb7be4`, `mhc.py` owns the shared `mhc_shifted_post_pre` dispatcher and `supports_mhc_all_reduce` eligibility check. `mega_mhc.py` contains only the DeepGEMM implementation and its support check. This is a code move with unchanged arithmetic, eligibility, and 16-token cutoff. Pre-commit passed; the GPU regression command below passed 192 tests (125 deselected). No accuracy/performance rerun for this structural change.\n\n```bash\nRAY_ADDRESS=local .venv/bin/python -m pytest \\\n  tests/distributed/test_custom_all_reduce.py tests/kernels/test_mhc_kernels.py \\\n  -k 'all_reduce_mhc or deepseek_v41 or delayed_custom_op or deep_gemm_mega_mhc' -q -rs\n```\n"
    },
    {
      "sha": "eb87980585c10f7e5ee8f498fd3830898101d208",
      "pr_number": 57554,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57554",
      "pr_title": "[Build] Fix DeepGEMM CUDA 12.9 release builds",
      "pr_body": "## Purpose\n\nRestore the six CUDA 12.9 release targets that fail in [release-v2 #6858](https://buildkite.com/vllm/release-v2/builds/6858): x86_64 and aarch64 wheels, plus both architectures' default and Ubuntu 24.04 images.\n\nAll six fail while compiling DeepGEMM's host extension because `layout/mega_mhc.cuh` uses `__nv_fp8_e4m3` without including `<cuda_fp8.h>`. CUDA 13's transitive includes mask the missing declaration; CUDA 12.9 does not. The subsequent `y_fp8` errors are consequences of that missing type.\n\nUpdate both synchronized DeepGEMM pins from `a6bbb800` to the merged upstream commit [e1f418c2](https://github.com/vllm-project/DeepGEMM/commit/e1f418c2a4f20818221f6b0e578b4c2f634d4c3f) from [DeepGEMM#12](https://github.com/vllm-project/DeepGEMM/pull/12). The dependency delta is exactly five include lines in two headers; it preserves all current SM90/SM120 and Mega MoE changes.\n\n## Duplicate-work check\n\nThe header fix is merged in DeepGEMM#12; this PR consumes that work in vLLM and does not propose another upstream implementation. The merged commit has vLLM's current pin as its sole parent. The original duplicate-work checks found no competing open vLLM integration PR or matching open issue for `mega_mhc.cuh`.\n\n## Validation\n\nThe merged pin `e1f418c2` and previously tested cherry-pick `70874eca` have the identical Git tree `cd1947d66f49ef498fdeb6f227bb9e52b5ba0787` (including submodule gitlinks). `git diff --exit-code 70874eca e1f418c2` passes with no changes. Local pre-commit, shell syntax, and diff checks were rerun after updating the pin. The six native builds below tested this identical dependency content under the earlier SHA; they were not rerun for this metadata-only pin replacement.\n\n- `git diff --check`: passed in both vLLM and DeepGEMM.\n- `bash -n tools/install_deepgemm.sh`: passed.\n- `pre-commit run --files cmake/external_projects/deepgemm.cmake tools/install_deepgemm.sh`: all applicable hooks passed, including shellcheck and configuration validation.\n- Confirmed the new dependency commit has the old pin as its sole parent and changes only the five include lines from DeepGEMM#12.\n- Focused `g++ -std=c++20 -fsyntax-only` test including both layout headers: CUDA 12.9 reproduced the exact `__nv_fp8_e4m3` baseline failure and passed after the patch; CUDA 13.0 passed before and after. CUDA 12.9 headers came from NVIDIA runtime 12.9.79, nvcc 12.9.86, and CCCL 12.9.27 packages. Tested with the pinned CUTLASS headers in an x86_64 Linux container.\n- Verified `git patch-id --stable` matches the original DeepGEMM#12 patch exactly.\n- Native build validation: [release-v2 #6874](https://buildkite.com/vllm/release-v2/builds/6874), **passed: all six affected targets**. The validation branch uses the original PR head `55b0863c` plus a temporary build-only pipeline for the six affected targets. It retains release build arguments, verifies the five CPython DeepGEMM binaries in each wheel/image, and imports torch/vLLM in final images. It contains no wheel, image, or nightly publication steps.\n\n| Target | Result |\n| --- | --- |\n| Build wheel - aarch64 - CUDA 12.9 | [Passed](https://buildkite.com/vllm/release-v2/builds/6874#01a0b43e-a7b0-441f-a694-d3d663dd1bf8) |\n| Build wheel - x86_64 - CUDA 12.9 | [Passed](https://buildkite.com/vllm/release-v2/builds/6874#01a0b43e-a7b6-4b40-97a6-5fdb6254efb6) |\n| Build release image - x86_64 - CUDA 12.9 | [Passed](https://buildkite.com/vllm/release-v2/builds/6874#01a0b43e-a7bc-4d0a-8d83-bc7d1f5f1b43) |\n| Build release image - aarch64 - CUDA 12.9 | [Passed](https://buildkite.com/vllm/release-v2/builds/6874#01a0b43e-a7bc-46b1-ac7e-0c7bc6761bb6) |\n| Build release image - x86_64 - CUDA 12.9 - Ubuntu 24.04 | [Passed](https://buildkite.com/vllm/release-v2/builds/6874#01a0b43e-a7bd-4435-9bc9-8b3f4fd89fae) |\n| Build release image - aarch64 - CUDA 12.9 - Ubuntu 24.04 | [Passed](https://buildkite.com/vllm/release-v2/builds/6874#01a0b43e-a7bd-4c08-9286-f8a0b048973c) |\n\nValidation commit `9fe90329480c69aa561c04b4ea033bb49cca1e23` differs from the original PR head `55b0863c202b7fcf1c97c6a849ef1c4db07bc47a` only in `.buildkite/release-pipeline.yaml`. Both native wheels contain all DeepGEMM extensions for CPython 3.10–3.14; all four images passed the torch/vLLM import and bundled-extension checks. No artifacts were published.\n\nSeparate existing issue: all four final images build successfully, but optional KV connector installation replaces `torch==2.13.0+cu129` with `torch==2.14.0` (runtime reports `2.14.0+cu130`). This is already addressed by open [PR #56967](https://github.com/vllm-project/vllm/pull/56967), fixing #56829. It is independent of the compile failure fixed here; these build results are not full serving validation.\n\nNo model evaluation is needed for this include-only dependency fix; it changes header visibility during compilation, not kernel logic or model behavior.\n\nAI assistance: OpenAI Codex performed diagnosis, dependency integration, checks, and PR preparation. Opened as a draft for the human submitter to review the complete diff and validation evidence before marking ready.\n\n"
    },
    {
      "sha": "86ce4d10e290e6179e6f1d5ee392f582814d5747",
      "pr_number": 57811,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57811",
      "pr_title": "[BUGFIX][HY4]  Record indexer completion event for full CUDA graph capture",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\n which is introduced by https://github.com/vllm-project/vllm/pull/53781\r\n## Test Plan\r\n\r\n## Test Result\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "3918f3c5a3e389fb17d9c0d629811f3745014db0",
      "pr_number": 57460,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57460",
      "pr_title": "[Profiler] Unify platform-aware torch profiling",
      "pr_body": "## Purpose\r\n\r\nImplement the A+B+C profiler-core split proposed in #56542:\r\n\r\n- rebuild the one-shot `torch.profiler.profile` object for every profiling round (addresses #45987)\r\n- add configurable, platform-validated worker activities while preserving CPU, CUDA, and XPU defaults\r\n- skip worker annotation computation when a CUDA-only torch profile cannot record it\r\n- centralize ordinary profiler logic across GPU, CPU, and XPU workers without changing the profiling RPC/API signatures\r\n  - CPU profiler now also honors `profile_prefix`\r\n\r\n## Duplicate-work assessment\r\n\r\nConsolidates two currently conflicted PRs after the split recommendation on #56542:\r\n\r\n- #45992 only rebuilds the inner one-shot torch profiler. This PR incorporates that approach and additionally handles platform activities and the shared worker lifecycle.\r\n- #55751 adds the activity field, but does not validate activities against the concrete worker platform, preserve frontend CPU semantics for CUDA-only profiling, avoid annotation computation, or centralize CPU/GPU/XPU construction.\r\n\r\nBoth PRs have been cross-linked, and their authors/work are credited here.\r\n\r\n## Test plan and results\r\n\r\n```bash\r\npytest tests/v1/worker/test_gpu_profiler.py -q \r\n# 122 passed\r\n\r\npytest \\\r\n  tests/v1/engine/test_async_llm.py::test_cuda_profiler_requests_reach_engine_core \\\r\n  tests/v1/engine/test_async_llm.py::test_cuda_only_torch_profiler_skips_frontend_cpu_trace \\\r\n  -q\r\n# 2 passed\r\n```\r\n\r\nmanual test: check `CUDA` only works and the profiler can capture multiple rounds\r\nserver\r\n```\r\nvllm serve Qwen/Qwen3-0.6B \\\r\n  --enforce-eager \\\r\n  --profiler-config '{\r\n    \"profiler\": \"torch\",\r\n    \"torch_profiler_dir\": \"/tmp/vllm-profile\",\r\n    \"torch_profiler_activities\": [\"CUDA\"],\r\n    \"torch_profiler_with_stack\": false,\r\n    \"torch_profiler_use_gzip\": false,\r\n    \"torch_profiler_dump_cuda_time_total\": false\r\n  }'\r\n```\r\nclient\r\n```\r\nfor round in 1 2; do\r\n  curl -f -X POST http://localhost:8000/start_profile\r\n\r\n  curl -f http://localhost:8000/v1/completions \\\r\n    -H 'Content-Type: application/json' \\\r\n    -d '{\r\n      \"model\": \"Qwen/Qwen3-0.6B\",\r\n      \"prompt\": \"Explain GPU matrix multiplication:\",\r\n      \"max_tokens\": 32,\r\n      \"ignore_eos\": true\r\n    }'\r\n\r\n  curl -f -X POST http://localhost:8000/stop_profile\r\ndone\r\n```\r\nas expected there are 2 traces in the specified directory. Inspecting them, both are CUDA-only and don't contain the CPU events.\r\n\r\n## AI assistance\r\n\r\nAI assistance (Cursor) was used to help implement and test this change. The human submitter will review every changed line and validate the results before marking the PR ready.\r\n"
    },
    {
      "sha": "6dfd87f59658eba0af0354418ca725c64ef1ed7d",
      "pr_number": 57491,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57491",
      "pr_title": "[ROCm][DSv4.1] Keep the Engram tables in host memory on ROCm",
      "pr_body": "## Purpose\r\n\r\nDeepSeek-V4.1-Flash's Engram n-gram tables are the largest single consumer of device memory — 47.2 GiB per rank at TP=4, about a third of everything the weights hold. CUDA moves them to pinned host memory and reads them over UVA (#56512); on ROCm two `is_cuda()` gates turn that off, so `engram_config` stays `None`, `amd/model.py` falls through to the base `Engram`, and the tables stay on the accelerator.\r\n\r\nNothing in the offload path is platform-specific — `get_accelerator_view_from_cpu_tensor()` already dispatches on `is_cuda_alike()`, and `hipHostRegister` backs pinned host memory — so widen the two gates and import `Engram` from `..nvidia.engram`, as `amd/model.py` already does for `DeepseekV41ModelState`. No new AMD code. The `cpu_offload` tests in `tests/kernels/test_engram.py` carried the same gate and are widened too. `DPSharedEngramStorage` and the DP head sharding arrive with the import but stay inert at `data_parallel_size=1`.\r\n\r\nDuplicate-work check (2026-09-18): #56512 (merged) built the NVIDIA offload and left these gates behind; #56926, #57195 and #56757 continue that path and none touches `amd/` or the gates — #56757's `disk_offload_dir` requires `cpu_offload=True`, so it is blocked on ROCm by the same gate. #56400 has an AMD/ROCm section with no engram item. Prior art: SemiAnalysisAI/InferenceX#3217 by @functionstackx reached the same design as a local patch for an MI355X TP2 recipe, and recorded that hardware evidence was the outstanding gate before upstreaming. `vllm/config/engram.py` is also edited by those three, so this needs a rebase onto whichever merges first.\r\n\r\n## Test plan\r\n\r\n```bash\r\npython -m pytest tests/kernels/test_engram.py -q\r\npython -m ruff check vllm/config/engram.py vllm/config/vllm.py \\\r\n  vllm/models/deepseek_v41/amd/model.py tests/kernels/test_engram.py\r\npython -m ruff format --check vllm/config/engram.py vllm/config/vllm.py \\\r\n  vllm/models/deepseek_v41/amd/model.py tests/kernels/test_engram.py\r\ngit diff --check\r\n\r\n# MI355X (gfx950), ROCm 7.2.3, TP=4, one session per arm, warmup then 3 reps\r\nvllm serve $MODEL --tensor-parallel-size 4 --language-model-only \\\r\n  --tokenizer-mode deepseek_v41 --trust-remote-code --moe-backend aiter \\\r\n  --gpu-memory-utilization 0.9 --max-model-len 33792 --max-num-seqs 128 \\\r\n  --max-cudagraph-capture-size 1024 --max-num-batched-tokens 16384\r\nvllm bench serve --backend vllm --endpoint /v1/completions --model $MODEL \\\r\n  --dataset-name random --random-input-len 4096 --random-output-len 512 \\\r\n  --random-range-ratio 0 --num-prompts 32 --max-concurrency 8 --seed 1234 \\\r\n  --ignore-eos\r\npython tests/evals/gsm8k/gsm8k_eval.py --port 8000 --num-questions 1319 \\\r\n  --num-shots 5 --max-concurrency 64\r\n\r\n# DP shared storage, TP=2 / DP=2\r\nvllm serve $MODEL --tensor-parallel-size 2 --data-parallel-size 2 \\\r\n  --engram-config '{\"cpu_offload\":true,\"dp_shared_memory\":true}' ...\r\n```\r\n\r\n## Test result\r\n\r\n- `tests/kernels/test_engram.py` on gfx950: the two `cpu_offload` tests are the reason for the test change in this PR. Their gates were `is_cuda()`, so on ROCm the whole file was 9 passed / 75 skipped and the offload path had no coverage at all. Widened to `is_cuda_alike()`, `test_engram_lookup_matches_torch` (2 cpu_offload x 2 background x 3 token counts) and `test_engram_head_shards_reconstruct_checkpoint` (2 cpu_offload x 4 TP sizes) give **20 passed**, including the bit-exact comparison against the torch dequant reference reading from pinned host memory.\r\n- The alias is coherent on gfx950, which is what the stricter `is_uva_available()` proposed in #57404 probes for: host writes made after the accelerator view is taken are visible to the device, and device writes are visible to the host, for uint8 and float8_e4m3fn at 4 KiB and 1 MiB.\r\n- ruff check, ruff format --check and `git diff --check` pass on all four files. mypy and pre-commit were not run — neither is installed on the test host.\r\n- TP=4 serving: the offload log line appears once per rank per layer (8 lines, 23.60 GiB each), and `Resolved Engram configuration` appears at all, which it does not on base.\r\n- TP=2 / DP=2 with `dp_shared_memory`: server comes up and serves, four shared allocations of 47.21 GiB logged as \"shared across DP replicas\", 190 GiB resident in `/dev/shm`, no errors. That run is also the only one that reaches `verify_model_config()`, the second gate, since only an explicitly supplied `--engram-config` gets there. Functional check only; no DP performance claim is made.\r\n\r\n## Model evaluation\r\n\r\n`tests/evals/gsm8k/gsm8k_eval.py`, all 1319 problems, five-shot, concurrency 64, MI355X TP=4:\r\n\r\n| | accuracy | invalid rate |\r\n| --- | ---: | ---: |\r\n| base | 0.8999 | 0.15% |\r\n| this PR | 0.9030 | 0.08% |\r\n\r\n+0.31 pp against a 1.16 pp standard error for the difference of two proportions, z = 0.27. The acceptance band was fixed before the run. A second offload build measured 0.9052 (z = 0.46); see the note under Performance.\r\n\r\n## Performance\r\n\r\nThe prefetch is what keeps this free. In the shape Engram actually issues — 3 n-gram orders x 6 local hash heads = 18 rows of 256 B per token, in 2 of 40 layers — a UVA gather matches HBM at decode batch sizes and falls behind only at prefill batch sizes:\r\n\r\n| tokens/step | rows | UVA view | HBM | delta |\r\n| ---: | ---: | ---: | ---: | ---: |\r\n| 1 | 18 | 9.3 us | 10.7 us | -1.4 us |\r\n| 32 | 576 | 9.9 us | 9.8 us | +0.1 us |\r\n| 256 | 4608 | 25.6 us | 9.9 us | +15.7 us |\r\n| 1024 | 18432 | 89.0 us | 10.3 us | +78.7 us |\r\n\r\nThe model-owned prefetch stream, with `background=True` leaving half the SMs to the main stream, covers the last two rows.\r\n\r\n**KV cache capacity**, TP=4, identical across three independent starts:\r\n\r\n| | base | this PR | delta |\r\n| --- | ---: | ---: | ---: |\r\n| Available KV cache memory | 107.8 GiB | 154.38 GiB | **+43.2%** |\r\n| GPU KV cache size | 4,325,113 tokens | 6,194,599 tokens | **+43.2%** |\r\n| Max concurrency @ 33,792 ctx | 127.99x | 183.32x | **+43.2%** |\r\n\r\nPredicted from the startup memory breakdown beforehand as +43.8%; the measurement met it within 0.4%.\r\n\r\nAt TP=2 / DP=2 each rank carries a wider engram shard (47.21 GiB per layer instead of 23.60), so the same change is correspondingly larger. Both arms are the same topology and flags; the base arm simply cannot be given `--engram-config`, because that is the gate this PR widens:\r\n\r\n| | base | this PR | delta |\r\n| --- | ---: | ---: | ---: |\r\n| Available KV cache memory | 57.65 GiB | 151.45 GiB | **+162.7%** |\r\n| GPU KV cache size | 2,313,707 tokens | 6,078,307 tokens | **+162.7%** |\r\n\r\nThe startup breakdown pins down what moved: per rank, \"consumed memory (weights + non-torch)\" is 182.05 GiB on base and 87.62 GiB with the offload, a difference of 94.43 GiB against the 94.42 GiB the two engram shards occupy at this TP.\r\n\r\n**Serving throughput**, TP=4, random 4096 in / 512 out, 32 prompts, concurrency 8, three repetitions each (mean +/- sd). \"paired\" is the offload arm measured back to back with base in one session; \"shipped\" is this PR's exact diff, measured separately:\r\n\r\n| | base | offload, paired | offload, shipped |\r\n| --- | ---: | ---: | ---: |\r\n| mean TPOT | 11.35 +/- 0.02 ms | 11.35 +/- 0.02 ms | 11.35 +/- 0.00 ms |\r\n| median TPOT | 11.22 +/- 0.01 ms | 11.24 +/- 0.02 ms | 11.24 +/- 0.00 ms |\r\n| p99 TPOT | 12.54 +/- 0.01 ms | 12.54 +/- 0.02 ms | 12.56 +/- 0.01 ms |\r\n| mean TTFT | 796.88 +/- 2.43 ms | 794.08 +/- 0.35 ms | 799.97 +/- 1.61 ms |\r\n| p99 TTFT | 1061.31 +/- 1.69 ms | 1054.38 +/- 2.59 ms | 1063.68 +/- 4.60 ms |\r\n| output throughput | 620.64 +/- 0.97 tok/s | 621.10 +/- 1.06 tok/s | 620.61 +/- 0.11 tok/s |\r\n\r\nPaired against base: +0.46 tok/s on a pooled standard error of 0.83, t = 0.55. Shipped against the same base: -0.03 tok/s, -0.005%. Flat either way, p99 included. There are two offload columns because the paired arm reached the offload path through the local copy of `nvidia/engram.py` described above, which is not the diff here; the shipped diff reproduces it exactly, down to the same KV cache figures and offload log lines.\r\n\r\nHost cost at TP=4: 47.2 GiB x 4 ranks = 188.8 GiB of pinned host memory, against 2954 GiB of RAM on the test node. Containers need `--ulimit memlock=-1`.\r\n\r\nAI assistance was used for this change. Every changed line was reviewed by the submitter, and the measurements above were produced by the submitter on the hardware named.\r\n"
    },
    {
      "sha": "3e67044972939772ef6cd06957bf3199a41b5139",
      "pr_number": 57314,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57314",
      "pr_title": "[Doc] Refresh Kthena integration guide",
      "pr_body": "## Purpose\r\n\r\nUpdate the Kthena integration guide for v1.0.0.\r\n\r\nThe existing `405b` role name is rejected by the v1.0.0 webhook because it starts with a number. Rename it to `llama-405b` and update the matching gang policy and pod names.\r\n\r\nAlso update the installation version, align the Ray command examples, and add the missing Service creation and cleanup steps.\r\n\r\nNo matching open PR was found when checking for existing Kthena updates. AI assistance was used; the changes were checked against Kthena v1.0.0 and tested locally.\r\n\r\n## Test Plan\r\n\r\n- Install Kthena v1.0.0 and Volcano 1.15.2 on local Kubernetes.\r\n- Validate both manifests with `kubectl apply --dry-run=server`.\r\n- Check pod readiness, Service endpoints, worker DNS resolution, API requests, and cleanup using a mock backend.\r\n- Run Markdown lint and `git diff --check`.\r\n\r\n## Test Result\r\n\r\nReproduced the admission failure with `405b`; both corrected manifests passed validation.\r\n\r\nAll four test pods became ready. The Service selected only the two entry pods, worker DNS resolution succeeded, and both API requests returned HTTP 200. Cleanup and lint checks passed.\r\n\r\nReal GPU inference and Ray/MP distributed execution were not tested.\r\n\r\nSeparately, vLLM prefill-decode disaggregation has been validated on NVIDIA H200 GPUs in another deployment, though not using the exact manifests in this guide.\r\n\r\n<img width=\"1139\" height=\"423\" alt=\"image\" src=\"https://github.com/user-attachments/assets/22443562-ec71-4868-84bb-598b53fa5a22\" />\r\n\r\n\r\n---\r\n\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [x] The purpose of the PR.\r\n- [x] The test plan.\r\n- [x] The test results.\r\n- [x] The necessary documentation update.\r\n\r\n</details>"
    },
    {
      "sha": "11852548518b13c9a00a4e73dadd19d44e221e2b",
      "pr_number": 54581,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54581",
      "pr_title": "[Bugfix][KV Connector] Propagate cache reset failure during sleep",
      "pr_body": "## Purpose\n\nCheck the cache-reset result during sleep/pause instead of continuing to sleep after a failed reset.\n\nOnly `vllm/v1/engine/core.py` changes:\n- Check the prefix-cache reset result immediately and raise on failure, before clearing MM/encoder caches or sleeping.\n- Complete deferred pause Futures with the exception when pause finalization fails, or with success when it finishes normally.\n\nRelated work: #53082 restructures sleep/pause. This fix is limited to consuming the cache-reset result and propagating deferred failures through the Future; it does not duplicate that refactor.\n\n## Validation\n\n```bash\nVLLM_TARGET_DEVICE=cpu .venv/bin/python -m pytest -o addopts= -q tests/v1/core/test_scheduler.py::test_scheduler_reset_prefix_cache tests/v1/core/test_scheduler.py::test_reset_connector_cache_no_connector_is_no_op_success tests/v1/kv_connector/unit/test_mooncake_store_connector.py::test_scheduler_reset_connector_cache_invokes_connector_reset\n```\n\nResult: 3 passed.\n\nAfter the idle-callback change, an inline CPU probe of the production EngineCore methods with a mocked connector/executor passed all 5 checks: synchronous and deferred sleep with successful and failed resets, plus preservation of the original pause-finalization exception in the Future. Failed resets never invoke executor sleep; idle callbacks do not leak the finalization error.\n\nPre-commit checks and mypy 3.12 passed. Validation covers mocked lifecycle and scheduler/connector behavior, not a live remote KV service or model evaluation. No test files are added.\n\nAI assistance (Codex) was used for simplification and validation.\n"
    },
    {
      "sha": "7ddd461a284d2c318410f29110f1604028b961ee",
      "pr_number": 57742,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57742",
      "pr_title": "[Docs] Add ECMooncakeConnector usage example for EPD",
      "pr_body": "## Purpose\n\nThe disaggregated encoder usage section only describes ExampleConnector. Add an ECMooncakeConnector example that links to the existing full-pipeline integration script, shows a runnable 1E + 1PD command, and explains the default model, baseline comparison, and TCP/RDMA selection.\n\nDuplicate-work check: searched open PRs for `ECMooncakeConnector`, `disaggregated encoder documentation`, and `EPD docs`. #40695 changes the existing ExampleConnector script paths and implements a connector; #56006 fixes stale script names. Neither adds this usage guidance for the existing Mooncake integration script. No issue is associated with this documentation update.\n\n## Test Plan\n\n- `git diff --check`\n- `.venv/bin/pre-commit run --files docs/features/disagg_encoder.md`\n- Verify the referenced script exists and the documented options match its implementation.\n\n## Test Result\n\nAll applicable pre-commit checks and the whitespace check passed. Confirmed the linked script and its model, GPU, and protocol settings. GPU integration tests and model evaluations were not run because this change only updates documentation and does not affect model or serving behavior.\n\nAI assistance: Codex assisted with the documentation change and validation.\n"
    },
    {
      "sha": "d2983f2f163249acbe3053c073cb9830290984a0",
      "pr_number": 56734,
      "pr_url": "https://github.com/vllm-project/vllm/pull/56734",
      "pr_title": "[Bugfix][Spec Decode] Stop dummy draft decode steps from writing KV through stale block-table rows",
      "pr_body": "## Purpose\r\n\r\nFix a KV-cache corruption in Model Runner V2 speculative decoding under data parallelism: an idle DP rank's dummy batch runs the drafter's multi-step decode, which computes real slot mappings through the *persistent* per-slot block tables using `idx_mapping = arange(num_reqs)` (real request-state slots whose rows are stale), and writes drafter-layer K/V into offsets `1..k-1` of the first block of whatever request last occupied those slots. Those blocks are typically already registered in the prefix cache, so every later request that hits them reads corrupted drafter K/V while the target model's K/V stays intact.\r\n\r\nObserved on GLM-5.2 (glm_moe_dsa, MTP k=5, DP=4 EP, prefix caching) as a per-request, exactly-zero MTP acceptance (`p0 = 0.0000`, `acc_len = 1.0`, `drafts == max_tokens`) that latches onto a conversation and disappears after `POST /reset_prefix_cache`; the poisoned rows were NaN (fp8 `0x7F`) in `model.layers.78.self_attn.attn`, with the target layers and indexer cache of the same block clean. On current main the same stale write still happens on every idle step (the written values happen to be finite there, so acceptance is unaffected), i.e. the write path is latent.\r\n\r\n#49757 protected the read side (`get_dummy_block_tables` zeroes `input_block_tables`) and the target's dummy slot mappings are PAD (`prepare_dummy_attn`), but `_compute_slot_mappings_kernel` reads `block_table_ptrs` (the persistent tables), which neither protection covers.\r\n\r\n## Fix\r\n\r\n- `_compute_slot_mappings_kernel`: a row with `idx_mapping < 0` (dummy or CUDA-graph padding request) emits `PAD_SLOT_ID` and never dereferences a negative block-table row.\r\n- `AutoRegressiveSpeculator.propose(dummy_run=True)`: mark every dummy row with `idx_mapping = -1` (already the speculator's convention for padded rows, ignored by `gumbel_sample`).\r\n\r\nThe guard lives in the kernel rather than in Python because the fused multi-step path re-runs `compute_slot_mappings` inside the captured CUDA graph between draft steps; only the kernel can neutralize a replayed dummy run.\r\n\r\n## Test Plan\r\n\r\n- `tests/v1/worker/test_gpu_block_table.py::test_dummy_request_slot_mapping_is_pad` (new): `idx_mapping == -1` yields all-PAD slot mappings; a real row still resolves through its block table.\r\n- End-to-end on 4x GB200 with GLM-5.2 NVFP4, DP=4 + EP, `--speculative-config '{\"method\":\"mtp\",\"num_speculative_tokens\":5}'`, prefix caching on, fp8 KV, block 64, `FULL_DECODE_ONLY` graphs (the production decode geometry), driven by a growing-prefix multi-turn probe plus a unique-prompt load/probe \"transition hunt\":\r\n  - f94666b60d unpatched: hunt trial 1 flips DEAD; 4/4 dead requests hit the same poisoned first block, all healthy requests on that rank do not; dumped drafter K/V rows 1-4 of that block are NaN.\r\n  - f94666b60d + this fix: semantic gate passed, 3x20 chain requests 0 dead, 6/6 hunt trials 0 dead (mean p0 0.61-0.76); 66,164 dummy draft decode steps logged, every slot mapping PAD.\r\n  - main dc07f1638f + this fix: semantic gate passed, 3x20 chain requests 0 dead, 6 hunt trials 0 dead; the dummy draft steps log `slots=[-1]`.\r\n\r\n## Test Result\r\n\r\n```\r\n$ pytest -q tests/v1/worker/test_gpu_block_table.py      # GB200, main + fix\r\n10 passed\r\n$ pre-commit run --files vllm/v1/worker/gpu/block_table.py vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py vllm/v1/worker/gpu/spec_decode/multi_module_mtp/speculator.py tests/v1/worker/test_gpu_block_table.py\r\nall hooks passed\r\n```\r\n\r\nEnd-to-end reproducer (4x GB200, GLM-5.2 NVFP4, DP=4 EP, MTP k=5, prefix caching, fp8 KV, block 64, FULL_DECODE_ONLY):\r\n\r\n| tree | chain probe (3x20) | transition hunt | dummy draft slots |\r\n|---|---|---|---|\r\n| f94666b60d (production) | 0 dead | trial 1 DEAD (1/6, then 1/6 and 2/6 on confirm) | real slots; poisoned block rows 1-4 NaN |\r\n| f94666b60d + fix | 0 dead | 6/6 trials 0 dead | 66,164 steps, all PAD |\r\n| main dc07f1638f | 0 dead | 6/6 trials 0 dead | real slots (finite values) |\r\n| main dc07f1638f + fix | 0 dead | 6/6 trials 0 dead | all PAD |\r\n\r\nThe acceptance metrics above (mean p0 0.61-0.79 in every healthy run) double as the model-quality check: the change only affects dummy batches, and the semantic gate (answer planted at the prompt tail) passed in every run.\r\n\r\n## Notes\r\n\r\n- Duplicate check: `gh issue list`/`gh pr list` searches for the dummy-run / stale block table / draft KV path found no existing report; the closest related changes are #49757 (read-side zeroing) and #55458 (excludes DP padding rows from draft attention metadata; likely why the written values are finite on main).\r\n- A follow-up could make read and write sides share one source by computing slot mappings from the per-batch gathered `input_block_tables`; this PR is the minimal, backportable fix.\r\n- AI assistance: this change was investigated and drafted with Claude Code and reviewed with Codex; the submitter has reviewed every line and run the tests above.\r\n\r\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\r\n\r\n"
    },
    {
      "sha": "9b49f92344312c41ad61e05282c8e6a2d9bafb7f",
      "pr_number": 57603,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57603",
      "pr_title": "[Perf][DSV4.1] Overlap mHC coefficients for small TP batches",
      "pr_body": "## Purpose\n\nOverlap shifted mHC coefficient generation with attention/FFN on small batches. The decoder keeps one post/pre path: the existing mHC dispatcher\nselects overlap or native execution, while the decoder joins the coefficient\nstream before consuming its outputs.\n\n`supports_mhc_overlap` in `.ops.mhc` requires SM100/DeepGEMM, no ubatching,\nand compatible projection dimensions: hidden width divisible by 64, and\n`hc_mult * (hc_mult + 2)` a positive multiple of 8 no greater than 32.\nThe exact H5120/hc4 restrictions and helper assertion are removed.\nCompatible tensor/data/pipeline/expert-parallel configurations, attention backends,\nand LoRA configurations use the same token cutoff, without a separate performance\nallowlist. Larger batches, draft layers, and piecewise/breakable graphs retain\nnative execution. The cutoff is generalized from the TP4 measurements below;\nthose measurements do not establish a gain for every newly enabled configuration.\n\nThe cutoff is **16 tokens**, based on same-instance paired full-forward\nmeasurements with matching tuned GEMM tactics (three samples per arm):\n\n| Tokens | Native ms | Overlap ms | Reduction |\n| --- | ---: | ---: | ---: |\n| 1 | 4.763 | 4.692 | 1.49% |\n| 6 | 6.204 | 5.929 | 4.42% |\n| 8 | 6.644 | 6.360 | 4.28% |\n| 12 | 7.408 | 7.292 | 1.57% |\n| 16 | 7.966 | 7.877 | 1.12% |\n| 24 | 9.153 | 9.365 | -2.31% |\n| 48 | 11.915 | 12.040 | -1.05% |\n| 96 | 15.292 | 15.247 | 0.29% |\n| 192 | 18.820 | 18.977 | -0.84% |\n| 252 | 20.040 | 20.230 | -0.95% |\n\nAll samples are retained; the 24/48-token overlap arms contain outliers\n(SD 0.390/0.388 ms). Larger shapes show no reliable useful gain, so keep native\nexecution there. Selection uses the captured token shape, never actual KV\nlength. Split-kernel warmup is bounded by the same cutoff.\n\n## Validation\n\nBased on main `17e50b9b76`. Latest validation: **79 GPU tests pass**, covering\nexact output parity and changed-input CUDA graph replay across `(hidden_size,\nhc_mult)` = `(4096, 2)`, `(5120, 4)`, `(7168, 4)`, plus decoder/reference parity\nand graph/batch fallbacks. Pre-commit passes.\n\n```bash\n.venv/bin/python -m pytest tests/kernels/test_mhc_kernels.py \\\n  -k 'overlap_preserves_outputs or decoder_mixes_match_torch' -q -rs\n```\n\nOnly transient input/scratch tensors (`x`, `mix`, `sqr`) need side-stream allocator\nrecording. The model retains weights, and callers retain coefficient outputs\nuntil joining the side stream; the helper documents that ownership requirement.\n\nModel/performance measurements below are prior results, not reruns on the new\nmain base or measurements of every newly enabled configuration.\n\nMeasured on GB200×4, model revision `dba1be0a40aa45a94ad051997016db3960a90277`,\nFlashInfer 0.6.18.post1, TP4, DSpark fixed length 5, and synthetic 100K ±5% KV.\nThese are fixed-forward GPU latencies; no default-O2 piecewise-serving gain is\nclaimed. The prior ten-sample native/automatic pair measured 6.206→5.932 ms (4.42%).\n\nThe refactored commit's three-sample same-instance native/automatic comparison\nmeasures **6.210→5.928 ms (4.55%)**, with identical inputs, KV and tuned GEMM\ntactics. Automatic selection activates all 40 target layers. Both arms have\nfinite outputs and zero eager/graph, changed-input and restored-input replay\nerrors. Replay checks do not assert native/overlap full-model bitwise parity.\n\nPrior matched real-weight GSM8K: 1271→1268/1319 (96.36→96.13%; 7 improve, 10 regress).\nDSpark acceptance length 3.870→3.874. Local FP64 and serialized/async checks support\nthe numerical change; full-model hidden states are rounding-sensitive. The\nrefactor preserves the evaluated kernel math and launch order, so this accuracy\nevaluation is reused. Prior same-instance allocator peaks increased 16 MiB allocated\nand 74 MiB reserved per rank; this is not a serving-capacity measurement.\n\nThe projection-width limit of 32 follows DeepGEMM's device-side output-swizzle\nconstraint. Newly enabled dimensions have correctness coverage, not additional\nend-to-end performance measurements.\n\n## Final-head validation (2026-09-21)\n\nExact head `479b025e45`, GB200×4, pinned model revision above, TP4 **without\nDSpark or all-reduce+mHC fusion**, FlashInfer FP8 attention/MXFP4 sparse indexer.\nBaseline uses the same head with overlap disabled before loading.\n\n| Metric | Overlap disabled | Enabled |\n| --- | ---: | ---: |\n| Forward-only, KV8704 | 4.727 /4.730 ms (before/after) | 4.657 ms (−1.5%) |\n| Real8K/1K C1 mean TPOT | 5.013 ms | 4.992 ms (−0.4%) |\n| Median ITL | 5.011 ms | 4.974 ms |\n| P99 ITL | 5.228 ms | 6.049 ms |\n| Full GSM8K flexible extraction | 1266/1319 (95.98%) | 1271/1319 (96.36%) |\n\nForward harness: same initialized synthetic weights/KV, five samples ×20\nCUDA-graph replays after5s warmup per arm; slowest-rank GPU time, excluding\nlm_head/sampling/scheduler/HTTP. All arms pass finite-output and changed-input\nreplay checks (zero replay error). This is not cross-arm bitwise parity.\n\nServing: ten exact8192-input/1024-output requests per arm, greedy, C1, two\nwarmups; FULL_DECODE_ONLY with eager prefill. All requests succeed; candidate\ntail bursts in2/10 requests mean this is not a strong serving-speed claim.\nGSM8K: lm-eval0.4.12, five-shot chat, greedy, seed123, C8, max output1024;\nmatching prompts/targets,10 improve and5 regress, strict formatting79.68% both.\n\n**Warmup caveat:** initial overlap-disabled GSM8K reproduced cached FlashInfer\nCuTe tactic compilation under strict JIT monitoring. Final arms use identical\nstartup-only cached-tactic and DeepGEMM split warmup from the existing fix in\n#57643, plus identical pinned tactics. Zero monitored runtime JIT afterward;\nthese workarounds are not part of this PR. No claim of stock-stack zero JIT or\ncoverage of every newly enabled topology/backend. The79 focused GPU tests\nabove validate the exact head.\n\n## Duplicate-work check\n\nNo open PR found for shifted mHC coefficient/sublayer overlap. #52725 concerns\nXPU norm fusion; #42735 changes BF16 shared staging in the older mHC kernel;\n#56761 changes GLM JIT warmup; #51802 registers NVIDIA V4 warmup variants.\nNone implements this scheduling change.\n\nAI assistance was used to implement, benchmark and test this change.\n\n"
    },
    {
      "sha": "17e50b9b761023d5f2499f062507df1ff49092a4",
      "pr_number": 57779,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57779",
      "pr_title": "[XPU][UT]Bugfix when the process can't see all the world_size meet accuracy issue.",
      "pr_body": "When the DP process can't see the full world_size, eg the UT to check v1/distributed/test_external_lb_dp.py will meet the accuracy issue can see below:\r\nNCCL_CUMEM_HOST_ENABLE=0 TP_SIZE=1 DP_SIZE=2 pytest -sv v1/distributed/test_external_lb_dp.py::test_external_lb_completion_streaming[api-1-standard-ibm-research/PowerMoE-3b]\r\nerror like:\r\n> assert \"\".join(chunks) == single_output, (\r\n\"Streamed output should match non-streamed output.\"\r\n)\r\nE AssertionError: Streamed output should match non-streamed output.\r\nE assert 'PictureConti...tinueContinue' == 'PictureıFallıdw'\r\nE\r\nE - PictureıFallıdw\r\nE + PictureContinueContinueContinueContinue\r\nv1/distributed/test_external_lb_dp.py:312: AssertionError\r\n\r\nthis pr is to fix this."
    },
    {
      "sha": "96791737887224c8bfec0a791517759f62e4d93b",
      "pr_number": 51052,
      "pr_url": "https://github.com/vllm-project/vllm/pull/51052",
      "pr_title": "[KVConnector][MoRIIO] Transfer hybrid mamba/KDA recurrent state in READ mode",
      "pr_body": "## Purpose\n\nEnable MoRIIO READ-mode disaggregated serving for hybrid attention plus Mamba/KDA models with homogeneous prefill/decode TP. Recurrent conv and SSM state must move with attention KV; otherwise decode starts from empty state.\n\nThe scope is deliberately narrow: READ mode, no speculative decoding, and equal producer/consumer TP. Hybrid WRITE and heterogeneous-TP recurrent-state relayout remain follow-ups.\n\n## Design\n\nThe three commits separate completion semantics, memory registration, and the READ data path:\n\n| commit | responsibility |\n|---|---|\n| 1 | Wait for every transfer status belonging to a request. |\n| 2 | Register and address packed conv/SSM state as separate MoRIIO regions. |\n| 3 | Carry per-group state block IDs through scheduling and issue KDA READs. |\n\nImportant constraints and behavior:\n\n- One transferable attention group is supported.\n- Multiple equivalent Mamba groups are preserved separately in the existing block-ID payload. Each KDA layer selects its own group; group IDs are not flattened together.\n- Packed `[B, 1, 1, C]` byte pages are unpacked with the same `squeeze -> slice -> view` contract as `MambaBase.bind_kv_cache`. This stays zero-copy when blocks are strided inside a shared multi-layer allocation.\n- The producer stops at `h(N-1)` and decode recomputes the last prompt token to derive `h(N)`. Recurrent state is transferred even on a full local attention hit.\n- KDA conv and SSM reads are both complete before forward execution.\n- A READ aborted before decode allocation releases producer blocks without opening sessions or posting RDMA operations.\n- Unsupported WRITE, speculative, heterogeneous-TP, incompatible Mamba-spec, and invalid packed-layout cases fail explicitly.\n\nThe router protocol is unchanged. Attention and Mamba block groups continue to use `remote_block_ids`; no KDA-specific side channel is added.\n\n## Validation\n\nFinal head: `04412570c03493fbdaea4848fbf4134f2a266e96`\n\nFocused ROCm + mori unit suite on MI355X:\n\n```text\ntests/v1/kv_connector/unit/test_moriio_transfer_completion.py\ntests/v1/kv_connector/unit/test_moriio_kv_layout.py\ntests/v1/kv_connector/unit/test_moriio_hma_scheduler.py\ntests/v1/kv_connector/unit/test_moriio_tp_ack.py\ntests/v1/kv_connector/unit/test_moriio_connector.py\n\n153 passed, 14 warnings\n```\n\nReal-weight Kimi-Linear validation on two MI355X nodes:\n\n```text\nmodel:        moonshotai/Kimi-Linear-48B-A3B-Instruct\nprefill:      TP2/DCP2\ndecode:       TP2/DCP2\ntransport:    MoRIIO RDMA READ\nKV:           fp8\nspeculation:  disabled\nrouted smoke: HTTP 200, 17 * 6 = 102\nGSM8K N50:    44/50 = 0.88\nerrors:       0\n```\n\nThe result matches the established `44/50` baseline. Both ranks on both roles selected RDMA, both decode ranks completed eager handshake, and both roles registered 20 KDA layers. Final logs contain no traceback, EngineCore failure, MR-registration failure, WR flush, HSA fault, or `TransferError`.\n\nAfter six independent review/fix/validation rounds, the final head also passed full Kimi-K3 validation:\n\n```text\nprefill:       TP8/DCP8, AITER, FULL_AND_PIECEWISE\ndecode:        TP8/DCP8, AITER, FULL_DECODE_ONLY\ntransport:     MoRIIO RDMA READ\nKV/interleave: fp8 / 1536\nprefix cache:  enabled\nspeculation:   disabled\nGSM8K full:    1279/1319 = 0.96967\nerrors:        0\n```\n\nAll eight ranks on both roles selected RDMA and registered 69 KDA layers; all eight decode ranks completed eager handshake. The final K3 logs contain none of the hard errors listed above.\n\n### AgentX long-context validation\n\nThe production K3 stack was also exercised with the public SemiAnalysis AgentX trace. This run used the same MoRIIO hybrid READ path plus DCP support that is already in current main:\n\n```text\nP: TP8/DCP8 + LMCacheMP, 1799 GiB DRAM, FULL_AND_PIECEWISE\nD: TP8/DCP8, FULL_DECODE_ONLY\nKV: fp8, effective attention block/interleave 1536\nMoRIIO: RDMA READ\nspeculation: disabled\nconcurrency: 40\nduration: 3600 seconds\n```\n\nThe benchmark command was:\n\n```bash\naiperf profile --scenario inferencex-agentx-mvp --url http://localhost:30000 --endpoint /v1/chat/completions --endpoint-type chat --streaming --model Kimi-K3 --tokenizer moonshotai/Kimi-K3 --concurrency 40 --benchmark-duration 3600 --stats-interval 30 --random-seed 42 --failed-request-threshold 0.10 --trajectory-start-min-ratio 0.25 --trajectory-start-max-ratio 0.75 --warmup-requests-per-lane 10 --trace-idle-gap-cap-seconds 300 --warmup-grace-period 1800 --use-server-token-count --no-gpu-telemetry --tokenizer-trust-remote-code --num-dataset-entries 393 --slice-duration 1.0 --public-dataset semianalysis_cc_traces_weka_062126\n```\n\nOne-hour c40 result:\n\n```text\nvalid requests: 2019\nempty-response errors: 3\ninput throughput: 70,236.35 tok/s\noutput throughput: 478.10 tok/s\nTTFT p50/p90/p95/p99: 2.90s / 11.87s / 24.51s / 160.88s\ntheoretical prefix hit: 96.73%\nmeasured P local+external hit: 92.18%\n```\n\nThe final #51052 head was separately revalidated for accuracy with Kimi-K3 GSM8K full/c64 (`1279/1319`, zero request errors). The AgentX run validates the long-context production stack; the GSM8K run isolates final-head correctness.\n\n## Failure semantics\n\nHybrid READ failures fail closed before forward execution. Current scheduler invalid-block recovery is single-group-oriented and cannot safely reconstruct recurrent state, so request-level HMA recovery is left to a separate change.\n\n## Out of scope\n\n- Hybrid WRITE\n- DSpark or other speculative decoding\n- Heterogeneous-TP recurrent-state transfer\n- Request-scoped HMA transfer recovery\n\n## AI assistance\n\nAI assistance was used for analysis, implementation, and validation. The changes and recorded evidence still require maintainer review.\n"
    },
    {
      "sha": "01f1f58f10c616850a5f5ade129a7b58d62075fb",
      "pr_number": 57450,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57450",
      "pr_title": "[ROCm][CI] Query HIP device memory for test GPU teardown waits.",
      "pr_body": "## Purpose \r\n- PR 1 to enable MI355 DPX jobs\r\n- On ROCm, measure GPU teardown / idle memory with `torch.cuda.mem_get_info` (HIP logical devices) instead of amdsmi physical indices.\r\n- Raise ROCm teardown headroom to 4 GiB to match known driver/runtime residency.\r\n- Stop remapping HfRunner memory checks through `get_physical_device_indices` on paths that now use HIP devices.\r\n\r\n## Why\r\namdsmi on DPX `gpu-0` reports the parent MI350 card (~288 GiB) and includes the sibling partition's VRAM, so `_wait_for_gpu_memory_release` false-fails. HIP is partition-scoped (~144 GiB NPS2).\r\n\r\n## Test Plan\r\n- [ ] `tests/entrypoints/unit_tests/test_remote_vllm_server.py`\r\n- [ ] DPX: teardown wait sees partition memory, not parent-card memory\r\n- [ ] Existing AMD CI (`vllm/amd-ci`) does not regress on SPX/MI300\r\n\r\n"
    },
    {
      "sha": "4868312128172a424a7cf4c90f25c53b49186346",
      "pr_number": 57621,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57621",
      "pr_title": "[Refactor] Remove dead kernel code",
      "pr_body": "\r\n## Purpose\r\n\r\nRemove dead kernel code\r\n"
    },
    {
      "sha": "49ee12d742b4a5524d2a3a3ee6c3fb77e4913073",
      "pr_number": 57382,
      "pr_url": "https://github.com/vllm-project/vllm/pull/57382",
      "pr_title": "[Misc] Rename --enable-mamba-fine-grained-prefix-cache (#53945 follow-up)",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\n\r\nFollow-up to #53945, as requested by @ZJY0516 \r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    }
  ]
}