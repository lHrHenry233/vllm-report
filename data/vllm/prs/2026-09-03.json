{
  "date": "2026-09-03",
  "repo": "vllm-project/vllm",
  "fetched_at": "2026-09-04T17:05:39.263766+08:00",
  "commits": [
    {
      "sha": "4ae6228284c4e9776f62d0867f543e621864176c",
      "pr_number": 54325,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54325",
      "pr_title": "[Bugfix][KV Connector] Populate SimpleCPUOffload BlockStored metadata",
      "pr_body": "## Purpose\r\n\r\n`SimpleCPUOffloadScheduler` stores KV blocks in a CPU or disk-backed `BlockPool`. Request metadata is no longer available when asynchronous stores complete, and events from the shared pool do not describe the offload tier correctly.\r\n\r\nThis PR:\r\n\r\n1. **Populates `BlockStored` metadata** — Snapshots `token_ids`, `parent_block_hash`, `lora_id`, `lora_name`, and `extra_keys` while the request is available. The metadata is carried through `TransferMeta` until asynchronous completion.\r\n\r\n2. **Emits events for every registered hash** — Emits `BlockStored` for primary and secondary hashes, matching the hashes registered in the CPU prefix cache and subsequently included in `BlockRemoved` events.\r\n\r\n3. **Reports the correct offload medium** — Labels stored and removed events as `MEDIUM_CPU` or `MEDIUM_STORAGE` and marks them as local.\r\n\r\n4. **Rejects invalid disk capacity** — Requires `disk_capacity_bytes > 0` when `kv_offload_backend=\"disk\"` so disk-backed events cannot be mislabeled as CPU events.\r\n\r\n5. **Guards Mamba+DCP metadata** — If captured token metadata does not match the event block size, emits hash-only metadata instead of misleading token, parent-hash, or extra-key values. #49962 owns the underlying geometry fix.\r\n\r\n6. **Threads multimodal cursors** — Carries `curr_mm_idx` across metadata generation within each group to avoid repeated multimodal scans.\r\n\r\nThis PR builds on #53532, which already implements CPU hash registration, secondary-hash preservation, finished-request flushing, and `has_pending_push_work()`.\r\n\r\n## Test Plan\r\n\r\n```bash\r\n.venv/bin/python -m pytest \\\r\n  tests/v1/simple_kv_offload/test_scheduler.py \\\r\n  tests/v1/simple_kv_offload/test_kv_events.py \\\r\n  tests/v1/kv_connector/unit/test_simple_cpu_offload_connector.py \\\r\n  -v --tb=short -p no:cacheprovider\r\n```\r\n\r\n## Test Result\r\n\r\n```text\r\n50 passed, 14 warnings in 8.32s\r\n```\r\n\r\nAll relevant pre-commit hooks pass, including ruff-check, ruff-format, mypy-3.10, typos, SPDX, forbidden imports, and configuration validation.\r\n\r\n## Duplicate-Work Check\r\n\r\n- #53532 is merged and provides the underlying cache registration, finished-request flush, secondary-hash preservation, and pending-work behavior. This PR adds offload-tier event metadata to those paths.\r\n- #49962 fixes hybrid DCP block geometry. This PR intentionally leaves that fix there and only prevents invalid event metadata in the meantime.\r\n- #53917 addresses DCP recovery geometry and does not implement these events.\r\n- #52103 targets the separate `vllm/v1/kv_offload/` implementation.\r\n\r\n## Model Evaluation\r\n\r\nNot applicable. This change affects KV-cache event metadata and does not alter model execution or generated output.\r\n\r\n## AI Assistance\r\n\r\nThis PR was developed with AI assistance. All changes were reviewed line by line and tested locally.\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [x] The test plan, such as providing test command.\r\n- [x] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>"
    },
    {
      "sha": "e55b93f2969d76b854eae158b6d17baea757d3e8",
      "pr_number": 55041,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55041",
      "pr_title": "[Core] Deprecate \"all\" mamba cache mode",
      "pr_body": "And fall back to MRV1 if configured since it's not implemented in MRV2."
    },
    {
      "sha": "31e9c13685b484d4ba67dc4e73019b23402609c7",
      "pr_number": 54879,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54879",
      "pr_title": "[Bugfix][KV Connector] Safely fill circular buffers in DecodeBench",
      "pr_body": "## Purpose\r\n\r\nDecodeBench fills allocated cache blocks with synthetic values to emulate an external prefill. Qwen QSA circular buffers can pack exact `int64` MRoPE positions alongside BF16 key state. Applying the configured nonzero BF16 fill\r\nto the entire circular-buffer row corrupts those positions, which can make the QSA pre-indexer access its cosine/sine table out of bounds and trigger a CUDA IMA.\r\n\r\nThis change identifies KV-cache groups containing `CircularBufferSpec` layers and explicitly zero-fills their selected blocks. Other cache groups continue to use the configured DecodeBench mean and standard deviation. The regression test also verifies that only the selected circular-buffer row is modified.\r\n\r\n### Duplicate-work check\r\n\r\nNo issue is linked to this change. Searches of open vLLM PRs and issues for `DecodeBench CircularBuffer`, `DecodeBench Qwen3.8`, and `QSA illegal memory DecodeBench` found no existing fix.\r\n\r\n## Test Plan\r\n\r\n- Exercise a worker-side `CircularBufferSpec` cache initialized with nonzero\r\n  values and verify that DecodeBench zeros the selected row while leaving every\r\n  other row unchanged.\r\n- Run the complete DecodeBench connector unit-test file.\r\n- Run pre-commit on both changed files.\r\n- Run a Qwen3.8 Flash Next DecodeBench GPU E2E with the default nonzero fill\r\n  configuration.\r\n- Run Qwen3-0.6B as a non-circular DecodeBench control.\r\n\r\nCommands run locally:\r\n\r\n```bash\r\n../vllm/.venv/bin/python -c 'import runpy,sys,types; sys.modules[\"readline\"]=types.ModuleType(\"readline\"); sys.argv=[\"pytest\",\"tests/v1/kv_connector/unit/test_decode_bench_connector.py\",\"-q\"]; runpy.run_module(\"pytest\",run_name=\"__main__\")'\r\n../vllm/.venv/bin/pre-commit run --files vllm/distributed/kv_transfer/kv_connector/v1/decode_bench_connector.py tests/v1/kv_connector/unit/test_decode_bench_connector.py\r\ngit diff --check\r\n```\r\n\r\nEquivalent model-server invocations used for the E2E runs:\r\n\r\n```bash\r\nvllm serve Qwen/Qwen3.8-Flash-Next-FP8 \\\r\n  --tensor-parallel-size 4 \\\r\n  --max-model-len 4096 \\\r\n  --max-num-batched-tokens 4096 \\\r\n  --max-num-seqs 4 \\\r\n  --enable-prefix-caching \\\r\n  --kv-transfer-config \\\r\n  '{\"kv_connector\":\"DecodeBenchConnector\",\"kv_role\":\"kv_both\",\"kv_connector_extra_config\":{\"fill_mean\":0.015,\"fill_std\":0.0}}'\r\n\r\nvllm serve Qwen/Qwen3-0.6B \\\r\n  --max-model-len 4096 \\\r\n  --max-num-batched-tokens 4096 \\\r\n  --max-num-seqs 8 \\\r\n  --enable-prefix-caching \\\r\n  --kv-transfer-config \\\r\n  '{\"kv_connector\":\"DecodeBenchConnector\",\"kv_role\":\"kv_both\"}'\r\n```\r\n\r\n## Test Result\r\n\r\n- `tests/v1/kv_connector/unit/test_decode_bench_connector.py`: 12 passed.\r\n- Changed-file pre-commit hooks: passed, including Ruff, mypy, SPDX, and the\r\n  repository-specific validation hooks.\r\n- `git diff --check`: passed.\r\n- Qwen3.8 Flash Next, tensor parallel size 4: 10/10 completion requests\r\n  returned HTTP 200 with the requested output lengths. This included the prior\r\n  3,002-token prompt plus 512-token generation regression shape, a 1,024-token\r\n  generation, replayed prefixes, and concurrent requests. The server remained\r\n  healthy, with no CUDA illegal memory access, engine failure, or CUDA core\r\n  dump.\r\n- Qwen3-0.6B non-circular control: 8/8 completion requests returned HTTP 200,\r\n  including replayed-prefix, long-generation, and concurrent workloads. The\r\n  normal attention group retained the configured default `fill_mean=0.015`,\r\n  and the run observed a 29.6% local prefix-cache hit rate.\r\n\r\nModel accuracy evaluation is not applicable because DecodeBench deliberately uses synthetic KV values and does not produce meaningful accuracy results. The model-serving E2E tests above cover the affected circular-buffer path and a non-circular control.\r\n\r\nAI assistance disclosure: OpenAI Codex was used for this PR."
    },
    {
      "sha": "edc0fb7e03b43fc17265c117f92021813711c6d8",
      "pr_number": 55054,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55054",
      "pr_title": "Optimize PLE MTP metadata transfers",
      "pr_body": "## Purpose\n\nThe Qwen4Exp PLE speculative-decode metadata builder performs several small host-to-device request-index transfers every decode step. Using synchronous `.to(device)` calls on this path introduces stream synchronization and leaves gaps between otherwise unchanged GPU kernels.\n\nThis PR uses `async_tensor_h2d` for the speculative, non-speculative, and decode request indices, then reuses those device tensors for the accepted-token and computed-token gathers. It removes the synchronization points without changing request ordering or model math.\n\nOpen-PR searches for `short_conv_attn async_tensor_h2d`, `spec_req_idx`, and `PleShortConvAttentionMetadataBuilder` found no existing PR that implements this optimization.\n\n## Performance Impact\n\nNsight Systems validation used GB300, FP8, TP1, MTP3, `FULL_DECODE_ONLY` CUDA graphs, 8K input, 1K output, C1, and 20 profiled decode steps on the same node:\n\n| Metric | Base | Optimized | Change |\n| --- | ---: | ---: | ---: |\n| GPU activity span / step | 11.185 ms | 9.272 ms | -17.102% |\n| `cudaStreamSynchronize` / step | 2 | 0 | -100% |\n| Summed kernel time / step | 10.810 ms | 10.813 ms | +0.003 ms |\n\nThe unchanged kernel time and shorter activity span confirm that the gain comes from removing metadata-transfer synchronization rather than changing GPU kernels.\n\nFive same-node alternating Base/Optimized pairs with the same FP8 TP1 MTP3 8K/1K configuration measured output-throughput changes of:\n\n| Concurrency | Output throughput change |\n| ---: | ---: |\n| C1 | +9.877% |\n| C2 | +4.740% |\n| C4 | +7.287% |\n| C8 | +0.409% |\n\nThese measurements were collected with the identical code delta before its final rebase onto current `main`; latest-main CI validation is still pending.\n\n## Test Plan\n\n- Run Ruff lint and formatting checks on the changed file.\n- Run `git diff --check`.\n- Run the Qwen4Exp PLE tests in a complete CUDA development environment.\n- Let upstream CI exercise the latest-main dependency matrix.\n\n## Test Result\n\n- `ruff check vllm/v1/attention/backends/short_conv_attn.py`: passed.\n- `ruff format --check vllm/v1/attention/backends/short_conv_attn.py`: passed.\n- `git diff --check`: passed.\n- AIME 2026 correctness evaluation of the identical code delta: 239/240 samples correct, with no request errors or truncations.\n- `pytest tests/models/qwen4_exp/test_ple.py -q`: not started on the host because the available development environment lacks `numpy` and `torch`; this PR remains a draft pending CUDA test and CI completion.\n\nAI assistance was used to prepare this draft. Before marking it ready for review, the human submitter must understand and review every changed line and confirm the relevant tests and evaluation results.\n\n---\n<details>\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\n\n- [x] The purpose of the PR is described.\n- [x] The test plan is described.\n- [x] Available correctness and performance results are included.\n- [x] No documentation update is required for this internal metadata-path optimization.\n</details>\n"
    },
    {
      "sha": "bf95f58d10891293aa6fde4e21f184d12ec0ba34",
      "pr_number": 54651,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54651",
      "pr_title": "[Core] Triton kernel for small-batch top-p only masking",
      "pr_body": "### Split-row Triton top-p pipeline — dispatch and GB200 results\r\n\r\n#### Which cases use the new split-row pipeline\r\n\r\nDispatch in `apply_top_k_top_p_triton` (with the `batch >= 8` gate removed, all CUDA batches reach this):\r\n\r\n| Condition | Path |\r\n|---|---|\r\n| batch > 64, or p is None, or CPU/XPU | monolithic kernel (unchanged) |\r\n| batch ≤ 64, pure top-p batch (k is None) | **split pipeline only**; per row, only rows with p < 1.0 are processed |\r\n| batch ≤ 64, k present (mixed/top-k batches) | monolithic kernel with `STANDALONE_TOPP=False` handles rows with k < vocab (top-k-only and top-k+top-p rows, the latter including its fast top-p-after-top-k path); **split pipeline** handles only rows with k = vocab & p < 1.0 (\"p-only\" rows); rows with neither are untouched by both |\r\n\r\nPer-row rule inside the pipeline: a row is processed iff `p < 1.0` and (`k` is None or `k ≥ vocab`). Note ROCm's torch `device.type` is `\"cuda\"`, so MI350X/MI355X also take the split path (verified there).\r\n\r\nScope reminder: on CUDA the default sampler is FlashInfer; this Triton path is reached when FlashInfer is disabled/unsupported, or for per-request generators, fp64-gumbel, or processed-logprobs modes.\r\n\r\n#### GB200, vocab = 200,000 (µs medians; speedup = pytorch/triton)\r\n\r\nHardware: NVIDIA GB200 (sm100, 152 SMs), torch 2.13.0+cu130, triton 3.7.1, fp32 logits, k=64, p=0.9, CUDA-event medians (500 iters, 50 warmup).\r\n\r\n| batch | topk | topp | topk+topp | mixed | topp 1-on¹ | topp 1-off² |\r\n|---|---|---|---|---|---|---|\r\n| 1 | 1.21x | **1.56x** (136µs) | 1.36x | 1.36x | 1.54x | 1.94x |\r\n| 2 | 1.20x | 3.56x | 2.38x | 1.90x | 3.62x | 3.64x |\r\n| 4 | 1.23x | 3.67x | 2.42x | 1.87x | 3.75x | 3.75x |\r\n| 8 | 1.37x | 3.02x | 2.59x | 1.74x | 3.15x | 3.11x |\r\n| 16 | 1.84x | 2.50x | 3.01x | 1.64x | 2.56x | 2.55x |\r\n| 32 | 3.00x | 2.04x | 3.91x | 1.55x | 2.07x | 2.06x |\r\n| 64 | 4.66x | 1.68x | 5.43x | 1.44x | 1.71x | 1.69x |\r\n| 128 | 13.1x | 2.85x | 15.0x | 3.67x | 3.64x | 2.94x |\r\n| 1024 | 8.97x | 1.85x | 10.6x | 2.45x | 16.4x | 1.86x |\r\n\r\n¹ exactly one request in the batch has top-p=0.9, rest unconstrained.\r\n² all-but-one have top-p=0.9.\r\n\r\nEvery composition wins at every batch size at 200k vocab — the slowest cell is topk-only bs=1 (1.21x) and top-p holds 1.5x–3.7x through bs=64 (where the monolith used to lose 3x at bs=1).\r\n\r\nThis was vibe-coded with Kimi K3\n\n<!-- This is an auto-generated comment: release notes by coderabbit.ai -->\n## Summary by CodeRabbit\n\n- **Performance**\n  - Expanded use of optimized Triton sampling across supported platforms and batch sizes.\n  - Improved efficiency for smaller-batch top-k and top-p sampling workloads.\n- **Bug Fixes**\n  - Fixed top-p filtering for grammar-constrained rows in mixed sampling batches.\n  - Made boundary handling deterministic when multiple tokens share the same sampling score.\n  - Improved consistency between optimized Triton sampling and the standard PyTorch implementation.\n<!-- end of auto-generated comment: release notes by coderabbit.ai -->"
    },
    {
      "sha": "bb363db9a5ec2edc7b39e99b00af363a89d1fb81",
      "pr_number": 54982,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54982",
      "pr_title": "feat: Add support for reasoning_token_count to reasoning parser",
      "pr_body": "## Purpose\r\n\r\nThis PR adds support for `count_reasoning_tokens` to the reasoning parser. This is necessary to ensure that we are able to return reasoning_tokens in usage data, since the [wiring](https://github.com/vllm-project/vllm/pull/45802) introduced by the vllm maintainers assumes this method has been implemented.\r\n\r\n## Test Plan\r\n\r\nUnit tests\r\n\r\n## Test Result\r\n\r\nAll passed\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "848ab131bcdb5264bcff0d802f47f7d4adb0f548",
      "pr_number": 55062,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55062",
      "pr_title": "[Perf] Accumulate Conformer attention scores with baddbmm",
      "pr_body": "## Purpose\n\nConformer relative attention currently materializes the content scores, adds\nthe shifted positional scores, and then launches a separate in-place scale.\nThis PR uses standard `torch.baddbmm` to accumulate the scaled content scores\ndirectly into the scaled positional scores:\n\n```text\nbefore: {bmm: 2, add: 1, mul_: 1}\nafter:  {bmm: 1, baddbmm: 1}\n```\n\nThis removes one score-sized intermediate and the separate add/scale kernels.\nIt keeps one portable PyTorch path with no custom kernel, architecture gate,\ndevice branch, or device-specific tuning.\n\n## Test Plan\n\n### Regression test\n\n```bash\npython -m pytest -q tests/models/multimodal/test_conformer_encoder.py\n```\n\nThe test instantiates the real module and compares final output and post-softmax\nattention against the pre-change implementation for sequence lengths 2 and 8.\nIt covers masking and production-style batch-1 positional broadcasting.\n\n### Lint and pre-commit\n\n```bash\npre-commit run --files \\\n  tests/models/multimodal/test_conformer_encoder.py \\\n  vllm/model_executor/models/conformer_encoder.py\n```\n\n### RTX 4090 CUDA Graph microbenchmark\n\n```bash\npython tools/bench_vllm_conformer_baddbmm.py \\\n  --mode benchmark \\\n  --batches 1 4 16 --heads 4 8 \\\n  --sequences 64 128 256 512 1024 \\\n  --head-dims 32 64 --dtypes float16 bfloat16 \\\n  --warmups 25 --iterations 200 --repeats 5 \\\n  --output portability-benchmark.csv\n```\n\nHardware/software: NVIDIA RTX 4090, driver 595.71.05, CUDA 13.0,\nPyTorch 2.13.0+cu130. No configuration changes.\n\n| B | H | T | D | dtype | Old p10/p50/p90 (us) | New p10/p50/p90 (us) | p50 speedup | Old/New extra peak allocation (MiB) |\n|---:|---:|---:|---:|:---|:---|:---|---:|---:|\n| 1 | 4 | 64 | 64 | FP16 | 9.18 / 9.18 / 9.20 | 8.28 / 8.28 / 8.29 | 1.11x | 0.16 / 0.12 |\n| 1 | 8 | 128 | 32 | BF16 | 9.62 / 9.80 / 10.54 | 8.71 / 9.55 / 9.56 | 1.03x | 1.25 / 1.00 |\n| 4 | 8 | 512 | 32 | FP16 | 170.92 / 170.94 / 171.09 | 138.66 / 138.72 / 139.07 | 1.23x | 80.03 / 64.03 |\n| 16 | 4 | 256 | 64 | BF16 | 100.56 / 100.62 / 100.73 | 93.93 / 94.03 / 94.05 | 1.07x | 40.03 / 32.03 |\n| 16 | 8 | 512 | 64 | FP16 | 1082.85 / 1083.32 / 1083.42 | 955.74 / 955.93 / 956.04 | 1.13x | 320.13 / 256.13 |\n| 16 | 8 | 1024 | 64 | BF16 | 4084.58 / 4085.03 / 4085.41 | 3514.84 / 3515.28 / 3515.57 | 1.16x | 1280.25 / 1024.25 |\n\nAcross all 120 CUDA Graph rows, the geometric-mean speedup was approximately\n1.11x; the median was 1.09x and the observed minimum was 1.03x. All 240\neager/CUDA Graph rows passed numerical validation with zero OOM rows and zero\nharness-reported allocation regressions.\n\nThe non-contiguous Q/K operands require the same two reshape copies in both the\nold `matmul` path and the new path. The measured allocation reduction comes\nfrom removing the additional score-sized add result. FP16/BF16 changed-input\nCUDA Graph replay and a focused `torch.compile(fullgraph=True)` smoke check also\npassed.\n\nThe standalone harness, raw CSV, and gate output are available in the\n[benchmark artifact Gist](https://gist.github.com/Levius-Fubuki/c125b9c6696331d65acef27d3d7fe5f1).\n\n## Test Result\n\n```text\n2 passed\n```\n\n`ruff-format`, `ruff-check`, SPDX-header, forbidden-import, new-`torch.cuda`\nAPI checks, and `git diff --check` passed locally.\n\n## Limitations\n\nThese are isolated score-chain results from one RTX 4090 run, not end-to-end\nConformer throughput. No performance claim is made for other architectures or\nbackends. Full upstream cross-backend CI still requires a maintainer `ready` or\n`verified` label. The positional tensor uses batch `B` in the performance\nharness; the real-module regression test separately covers production batch-1\nbroadcasting.\n\n## AI assistance\n\nAI assistance was used during implementation, testing, benchmarking, and PR\ndrafting. The human contributor reviewed and takes responsibility for the\nchange and evidence.\n"
    },
    {
      "sha": "758c79e1c5fc228a78d154f59dc0521e063e7a93",
      "pr_number": 55083,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55083",
      "pr_title": "[Bugfix] Retain vocab embeddings during replacement",
      "pr_body": "## Purpose\r\n\r\nPR #54760 tracked vocabulary embeddings using only their Python object IDs. During recursive replacement, an embedding can be released and its object ID reused by a subsequently created module such as `QKVParallelLinear`. This causes the new module to be misclassified as an embedding and crashes while accessing `num_embeddings`.\r\n\r\n## Reproducer\r\n\r\n```bash\r\n.venv/bin/python -m pytest \\\r\n  'tests/models/transformers/test_backend.py::test_pooling[v2-TransformersForSequenceClassification]' \\\r\n  -q\r\n```\r\n\r\n### Main output:\r\n\r\n```text\r\nAttributeError: 'QKVParallelLinear' object has no attribute 'num_embeddings'\r\nFAILED tests/models/transformers/test_backend.py::test_pooling[v2-TransformersForSequenceClassification]\r\n```\r\n\r\n### Branch  Output:\r\n\r\nThe deterministic strong-reference probe reports:\r\n\r\n```text\r\nstrong-reference lifetime probe: PASS\r\n```\r\n\r\nThe focused existing replacement tests report:\r\n\r\n\r\n```text\r\n8 passed, 17 deselected, 14 warnings in 11.88s\r\n```\r\n\r\nTest Plan and Results:\r\n\r\n```bash\r\nVLLM_TARGET_DEVICE=cpu CUDA_VISIBLE_DEVICES='' \\\r\n.venv/bin/python -m pytest tests/models/transformers/test_backend.py \\\r\n  -k 'replace_plain_embedding or replace_infers_shape_and_dtype or replace_inherited_embedding or replace_is_idempotent or only_vocab_tables_are_replaced or per_layer_vocab_table_is_replaced or composed_input_embeddings_are_replaced or missing_input_embeddings_are_skipped' \\\r\n  -q\r\n```\r\n\r\n```text\r\n8 passed, 17 deselected, 14 warnings in 11.88s\r\n```\r\n\r\n## AI assistance disclosure\r\n\r\nOpenAI Codex (GPT-5) assisted with root-cause analysis and drafting the code."
    },
    {
      "sha": "ee0a4c46ae771af034ae676d44e932469586e7be",
      "pr_number": 55111,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55111",
      "pr_title": "[Bugfix] Account for PCP in multi-node world size validation",
      "pr_body": "## Purpose\n\n`EngineArgs.create_engine_config` recomputes the world size for the `--nnodes` divisibility check, but omits `prefill_context_parallel_size`:\n\n```python\nworld_size = (\n    self.data_parallel_size\n    * self.pipeline_parallel_size\n    * self.tensor_parallel_size\n)\n```\n\n`ParallelConfig.world_size` already multiplies in PCP, so this local recomputation disagrees with the authoritative value.\n\nWith `TP=1 / PP=1 / PCP=2` across 2 nodes, the check computes `world_size = 1`, and `1 % 2 != 0` aborts the launch before the engine starts:\n\n```\nValueError: Invalid data-parallel launch options: `--nnodes 2` must evenly\ndivide the total world size (1). Adjust `--nnodes`, `--data-parallel-size`,\n`--pipeline-parallel-size`, or `--tensor-parallel-size`.\n```\n\nThere is no way to work around this from the CLI: raising TP or PP to satisfy the check would change the parallelism layout that was actually requested.\n\nThis PR multiplies PCP into both `world_size` and `world_size_within_dp`, and adds `--prefill-context-parallel-size` to the error message since it now contributes to the total.\n\n## Not a duplicate\n\nSearched open and closed PRs for overlapping work:\n\n- #51490 — *Fix DCP max-size check to account for PCP-induced KV duplication*: also a PCP validation fix, but targets the DCP max-size check in the KV cache path, not the `--nnodes` world size computation.\n- #33207 — *[WIP] PCP simplification*: draft, no activity on this code path.\n- #53743 — *Fix ZeroDivisionError with external LB when DP size exceeds node count*: touches the same `nnodes` block but fixes a DP/external-LB division issue; orthogonal to the missing PCP factor.\n\nNo open PR or issue covers the PCP factor missing from the multi-node world size check.\n\n## Test Plan\n\n```bash\npytest tests/engine/test_arg_utils.py -k multi_node_world_size -v\npytest tests/engine/test_arg_utils.py\n```\n\nThe new test constructs the failing configuration (`TP=1`, `PCP=2`, `nnodes=2`) and asserts the resulting `parallel_config.world_size == 2`. It monkeypatches `HAS_TRITON` because PCP requires the V2 model runner, which is gated on Triton availability.\n\nTo confirm the test actually guards the bug, revert the `arg_utils.py` hunk and re-run — it fails with the exact `world size (1)` error above.\n\n## Test Result\n\n```\n$ pytest tests/engine/test_arg_utils.py -k multi_node_world_size -v\nplatform darwin -- Python 3.10.11, pytest-9.1.1, pluggy-1.6.0\nconfigfile: pyproject.toml\ntests/engine/test_arg_utils.py::test_multi_node_world_size_includes_pcp PASSED [100%]\n================ 1 passed, 97 deselected, 14 warnings in 3.19s =================\n\n$ pytest tests/engine/test_arg_utils.py\n======================= 98 passed, 14 warnings in 20.45s =======================\n```\n\nReverting the `arg_utils.py` hunk and re-running the new test reproduces the\nreported failure, confirming the test guards the fix:\n\n```\nE  ValueError: Invalid data-parallel launch options: `--nnodes 2` must evenly\n   divide the total world size (1). Adjust `--nnodes`, `--data-parallel-size`,\n   `--pipeline-parallel-size`, or `--tensor-parallel-size`.\n```\n\nLint (`ruff==0.14.0`, matching `.pre-commit-config.yaml`):\n\n```\n$ ruff check vllm/engine/arg_utils.py tests/engine/test_arg_utils.py\nAll checks passed!\n$ ruff format --diff vllm/engine/arg_utils.py tests/engine/test_arg_utils.py\n2 files already formatted\n```\n\n## Model evaluation\n\nNot applicable. This change only affects launch-time argument validation in `EngineArgs.create_engine_config`. It does not touch model execution, sampling, or KV cache behaviour, and cannot alter model output for any configuration that already starts successfully. Configurations that previously started are unaffected: with `PCP=1` (the default) both expressions are numerically unchanged.\n\n## Real-world validation\n\nThe equivalent change was verified on a multi-node prefill/decode-disaggregated deployment whose prefill role runs with `TP=1` and `PCP=2` across 2 nodes. The role fails to start without the fix and comes up cleanly with it.\n\n## AI assistance disclosure\n\nThis change was developed with AI assistance (Claude Code). The submitter reviewed every changed line, ran the tests listed above locally, and can defend the change end-to-end.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n"
    },
    {
      "sha": "facd9a74a1cd1b9fed324cdc2cceb8d54fdad3d0",
      "pr_number": 54856,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54856",
      "pr_title": "[Model Runner V2][Spec Decode] Skip DP sync for all speculator uniform decodes",
      "pr_body": "# Summary\r\nContinuation of https://github.com/vllm-project/vllm/pull/53694, which removed the DP sync for MTP/EAGLE draft prefill. This PR removes the remaining DP syncs for the rest of the speculators: MTP, EAGLE, DFlash 1 & 2, DSpark. In the MTP/EAGLE case, it removes the DP sync before the multi-step-decode loop to draft the last N-1 tokens. In the DFlash/DSpark case, it removes the DP sync before drafting the B x N tokens in a single model forward.\r\n\r\n# This PR\r\nExtends the DP sync state to contain the agreed-upon number of requests between DP ranks. This is computed right before the GPUModelRunner's target model forward, during the call to `dispatch_cg_and_sync_dp`. That same state is used by the `AutoRegressiveSpeculator` and `DFlashSpeculator` to construct the uniform batch DP state needed to fetch the correct batch descriptor (`_build_uniform_batch_dp_sync`).\r\n\r\n# Evals/Benchmarks\r\n\r\n**Setup.** GSM8K (1319 questions), `dp=2, tp=2, --enable-expert-parallel` on 4×GB200. Perf via `vllm bench serve --dataset-name custom --dataset-path gsm8k.jsonl --backend openai-chat --request-rate inf --max-concurrency 64 --num-warmups 64 --num-prompts 1319 --custom-output-len 2048 --temperature 0 --top-p 1.0`. Perf figures are the **median of 6 runs (2 servers × 3 reps) per side**, run back-to-back in ABBA order (after → before → before → after) so run position cannot be mistaken for a branch effect; accuracy is one full-dataset pass per server. \r\n\r\n## DeepSeek-V4-Flash + MTP=3 + DP=2 (`AutoRegressiveSpeculator`)\r\n\r\n| metric | before | after | Δ |\r\n| --- | ---: | ---: | ---: |\r\n| **GSM8K accuracy** | 0.944 / 0.948 | **0.946 / 0.950** | +0.002 |\r\n| Invalid responses | 0.001 / 0.000 | 0.000 / 0.000 | — |\r\n| Output token throughput (tok/s) | 2888.72 | **2949.16** | **+2.1%** |\r\n| Mean TPOT (ms) | 21.06 | 20.66 | −1.9% |\r\n| Median TPOT (ms) | 21.58 | 21.13 | −2.1% |\r\n| P99 TPOT (ms) | 26.89 | 25.77 | −4.2% |\r\n| Mean TTFT (ms) | 368.48 | 358.22 | −2.8% |\r\n| Acceptance rate | 60.20% | 60.20% | 0.00pp |\r\n| Acceptance length | 2.81 | 2.81 | — |\r\n| Per-position acceptance | 89.00 / 63.11 / 28.46 | 88.98 / 63.08 / 28.53 | flat |\r\n\r\n**Draft DP-syncs eliminated.** Counting both exits of `dispatch_cg_and_sync_dp` over a full run — speculator-side collectives go to zero, the target's own sync is untouched:\r\n\r\n```\r\nModelCudaGraphManager:collect            833   <- target's own, irreducible\r\nSpeculatorCudaGraphManager:reuse_padded 1366\r\nSpeculatorCudaGraphManager:reuse_eager   298   = 2 × 832 target steps\r\nSpeculatorCudaGraphManager:collect      ABSENT\r\n```\r\n\r\n<details><summary>individual runs (output tok/s)</summary>\r\n\r\n```\r\nbefore: 2848.07, 2867.45, 2875.74, 2901.70, 2922.46, 2922.66\r\nafter:  2879.50, 2900.86, 2937.61, 2960.72, 2971.21, 2989.20\r\n```\r\n</details>\r\n\r\n## DeepSeek-V4-Flash-DSpark + DSpark N=5 + DP=2 (`DSparkSpeculator`)\r\n\r\n| metric | before | after | Δ |\r\n| --- | ---: | ---: | ---: |\r\n| **GSM8K accuracy** | 0.955 / 0.947 | **0.945 / 0.954** | −0.002 |\r\n| Invalid responses | 0.000 / 0.000 | 0.000 / 0.000 | — |\r\n| Output token throughput (tok/s) | 3384.32 | **3425.93** | **+1.2%** |\r\n| Mean TPOT (ms) | 17.70 | 17.46 | −1.4% |\r\n| Median TPOT (ms) | 18.04 | 17.84 | −1.1% |\r\n| P99 TPOT (ms) | 23.21 | 22.87 | −1.5% |\r\n| Mean TTFT (ms) | 378.38 | 381.53 | +0.8% |\r\n| Acceptance rate | 60.28% | 60.14% | −0.14pp |\r\n| Acceptance length | 4.01 | 4.01 | — |\r\n| Per-position acceptance | 87.45 / 72.66 / 58.78 / 46.59 / 35.89 | 87.39 / 72.67 / 58.61 / 46.40 / 35.69 | flat |\r\n\r\n**Draft DP-syncs eliminated.** Counting both exits of `dispatch_cg_and_sync_dp` over a full run — speculator-side collectives go to zero, the target's own sync is untouched:\r\n\r\n```\r\nModelCudaGraphManager:collect            500   <- target's own, irreducible\r\nDFlashCudaGraphManager:reuse_padded      499   = 1 × ~500 target steps\r\nDFlashCudaGraphManager:collect        ABSENT\r\n```\r\n\r\n<details><summary>individual runs (output tok/s)</summary>\r\n\r\n```\r\nbefore: 3297.92, 3342.87, 3369.18, 3399.47, 3475.75, 3500.59\r\nafter:  3382.73, 3390.52, 3422.76, 3429.09, 3429.75, 3459.46\r\n```\r\n</details>\r\n"
    },
    {
      "sha": "0d3ede3e3bd62cae2fc5c465b9bc2e62028b1e71",
      "pr_number": 54969,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54969",
      "pr_title": "[Bugfix][Model] Enable torch.compile for StableLM",
      "pr_body": "## Purpose\r\n`StableLM` is missing the `@support_torch_compile` decorator, leaving the model uncompiled. Since `CudaGraphManager.capture()` requires a compiled module to record piecewise CUDA graphs, engine init fails under the default `cudagraph_mode=FULL_AND_PIECEWISE` with:\r\n\r\nRuntimeError: StablelmForCausalLM: piecewise CUDA graphs (cudagraph_mode=FULL_AND_PIECEWISE) unavailable, model is not torch-compiled and breakable CUDA graph is off.\r\n\r\nThis PR enables torch.compile for StableLM, allowing piecewise capture to succeed.\r\n\r\n\r\n## Test Plan\r\n`pytest tests/models/language/generation/test_common.py -k stablelm`\r\n## Test Result\r\nBefore:\r\n `2 failed, 2 skipped`, both failures from the error above.\r\nAfter:\r\n `2 passed, 2 skipped`\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n\n\n<!-- This is an auto-generated comment: release notes by coderabbit.ai -->\n\n## Summary by CodeRabbit\n\n* **Performance**\n  * Enabled `torch.compile` support for the StableLM model, which may improve inference performance in compatible environments.\n\n<!-- end of auto-generated comment: release notes by coderabbit.ai -->"
    },
    {
      "sha": "c21751c90b1b1d4cfd8fd6e951feaeb539171cfc",
      "pr_number": 54251,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54251",
      "pr_title": "[Kernel] Warm up Qwen GDN gated RMSNorm",
      "pr_body": "## Purpose\n\nRefs #49349.\n\nQwen3.5's GDN output projection calls the FLA `layer_norm_fwd_kernel` through\n`RMSNormGated`. With cold Triton/vLLM caches, the first real request compiled\nthis specialization after the JIT monitor became active:\n\n```text\nACTIVATION='silu', BLOCK_N=128, HAS_BIAS=False, HAS_Z=True,\nIS_RMS_NORM=True, N=128, NORM_BEFORE_GATE=True, ROWS_PER_BLOCK=4,\nX/Y/W/Z='*bf16'\n```\n\nThis PR moves that compilation into startup warmup by:\n\n- adding a shared `VllmJitKernel` wrapper for the FLA layer-norm forward kernel;\n- using the same compile-key dispatch contract for runtime execution and\n  compile-only warmup;\n- enumerating the `M` specialization and `ROWS_PER_BLOCK` keys implied by the\n  configured token limit and TP-local Qwen value-head count; and\n- registering the Qwen GDN gated RMSNorm configuration with its actual dtypes,\n  head dimension, activation, and gate ordering.\n\nThe warmup uses `TritonWarmupTensor` descriptors and does not allocate or launch\ndummy inference tensors.\n\nThis does not duplicate #51134, which removes an obsolete FlashInfer startup\nwarning but does not warm this Triton kernel. It also does not overlap #50279,\nwhich adds an SM103-specific fused kernel rather than general compile-key warmup\nfor the existing FLA implementation.\n\n## Test Plan\n\nRun focused key-coverage and kernel-correctness tests on an RTX 3090:\n\n```bash\nCUDA_VISIBLE_DEVICES=5 .venv/bin/python -m pytest -q \\\n  tests/kernels/test_fla_layernorm_guard.py::test_layer_norm_fwd_warmup_keys_cover_qwen_gdn \\\n  'tests/kernels/test_fla_layernorm_guard.py::test_layer_norm_fwd_with_gate[True-True-dtype1-128-1]' \\\n  'tests/kernels/test_fla_layernorm_guard.py::test_layer_norm_fwd_with_gate[True-True-dtype1-128-512]' \\\n  'tests/kernels/test_fla_layernorm_guard.py::test_layer_norm_fwd_with_gate[True-True-dtype1-128-8191]'\n```\n\nRun changed-file checks:\n\n```bash\npre-commit run ruff-check --files \\\n  tests/kernels/test_fla_layernorm_guard.py \\\n  vllm/model_executor/warmup/qwen_triton_warmup.py \\\n  vllm/third_party/flash_linear_attention/ops/layernorm_guard.py\npre-commit run ruff-format --files <same files>\npre-commit run typos --files <same files>\npre-commit run --hook-stage manual mypy-3.10 --files <same files>\ngit diff --check\n```\n\nRun Qwen3.5-0.8B E2E generation with fresh Triton/vLLM caches and\n`--jit-monitor-mode error` for TP=1 and TP=2.\n\n## Test Result\n\n- Focused pytest: `8 passed, 14 warnings in 5.67s`.\n- `ruff-check`, `ruff-format`, `typos`, `mypy-3.10`, and `git diff --check`:\n  passed.\n- Cold-cache TP=1 E2E completed with `--jit-monitor-mode error`; no runtime JIT\n  was reported.\n- Cold-cache TP=2 E2E completed on two RTX 3090 GPUs with\n  `--jit-monitor-mode error`; no runtime JIT was reported. Shutdown later emitted\n  unrelated force-kill and leaked semaphore/shared-memory warnings after the\n  successful result.\n- One cold-cache TP=1 A/B measurement reduced first-request time from\n  `0.2791 s` to `0.0933 s` (about 66.6%). Startup time was `124.117 s` before\n  and `124.403 s` after. These are single-run latency observations, not a\n  statistical benchmark.\n\nThe full all-hook pre-commit run was not completed because pre-commit attempted\nto install the unrelated `actionlint` Go environment and timed out reaching\n`proxy.golang.org`. All changed-file Python hooks listed above passed.\n\nThe isolated E2E worktree reused the build-generated FlashMLA Python interface\nfrom the same verified vLLM environment for an unrelated startup warmup import;\nthat generated file is not tracked and is not part of this PR.\n\nNo model-quality evaluation was run because this change relocates compilation\nfrom request time to startup without changing the kernel math. The focused\nkernel tests compare results against the existing reference implementation, and\nthe Qwen E2E generation completed successfully.\n\nAI tools assisted with investigation, implementation, validation scripting, and\ndrafting this description. I reviewed every changed line, understand the change\nend-to-end, and verified the test commands and results above.\n\n---\n<details>\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\n\n- [x] The purpose of the PR, including the related issue.\n- [x] The test plan, including commands.\n- [x] Test results and before/after E2E evidence.\n- [x] No user-facing documentation update is required.\n</details>\n\n"
    },
    {
      "sha": "4cc0cb6f76a608622a29d9ea8f4d415697e21364",
      "pr_number": 55094,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55094",
      "pr_title": "[CI][AMD] Avoid expandable segments in LoRA TP tests",
      "pr_body": "- Remove `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` only from the AMD LoRA tensor-parallel mirror.\n- Retain the setting for NVIDIA, where memory-constrained L4 tests still need it.\n- Root cause: expandable-segment allocations are incompatible with the ROCm custom all-reduce IPC path.\n- The setting reached the AMD mirror when [#52350](https://github.com/vllm-project/vllm/pull/52350) (`f5711fa138d06cfff6be8ccb8a4d98eafa8a3b50`) sharded the distributed tests.\n- [PR #43923](https://github.com/vllm-project/vllm/pull/43923) addresses the same allocator/custom-all-reduce interaction in CUDA runtime code but does not fix this AMD CI mirror.\n- Scope is limited to the allocator export; shard assignments and model behavior are unchanged.\n\nThe AMD workers failed before reaching the LoRA assertions because expandable-segment buffers could not be imported or registered through the ROCm IPC path. This change removes only the incompatible AMD export while preserving NVIDIA OOM mitigation. `.venv/bin/pre-commit run --files .buildkite/test_areas/lora.yaml` passed all applicable checks. A model evaluation is not applicable because model execution code is unchanged. AI assistance was used for investigation, implementation, validation, and PR preparation. The human submitter must review every changed line and confirm the evidence before marking this draft ready."
    },
    {
      "sha": "e6eb9074e9f1fce6115dc036b40d07ebaa926979",
      "pr_number": 53905,
      "pr_url": "https://github.com/vllm-project/vllm/pull/53905",
      "pr_title": "[CI] Bump Transformers version to 5.16.1",
      "pr_body": ""
    },
    {
      "sha": "096d8e8ce61f43a7e142fba4b8ccb845d793c737",
      "pr_number": 55057,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55057",
      "pr_title": "[ROCm][CI] Add MiniMax reduce RMS kernel coverage",
      "pr_body": "- Add a two-GPU ROCm test that forces MiniMax tensor-parallel Q/K RMSNorm through AITER custom all-reduce and compares it with the eager reference.\r\n- Cover BF16 and FP16 across representative token counts while verifying that the V output remains unchanged and the fallback is not silently selected.\r\n- Add dedicated MiniMax kernel jobs for MI300 and MI355, including an AMD mirror that runs the same full-file pytest command as the upstream NVIDIA job.\r\n\r\nMiniMax Q/K RMSNorm has distinct fused all-reduce paths on NVIDIA and ROCm, but the ROCm AITER path lacked targeted multi-GPU correctness coverage. The new test exercises production dispatch with AITER custom all-reduce enabled, fails if execution falls back, and checks its results against the eager implementation. Dedicated MI300 and MI355 jobs provide the two-GPU AMD environment required by the test while preserving the upstream full-file test command.\r\n"
    },
    {
      "sha": "1f76efaa2195485b92cb04215aba6fb8f5fe523d",
      "pr_number": 55063,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55063",
      "pr_title": "[Model] Add K2-Horizon model support",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\n\r\nAdding K2-Horizon model architecture, including its reasoning parser and tool parser.\r\n\r\nThe changes include:\r\n\r\n- Adding the K2-Horizon model implementation in `vllm/model_executor/models/k2_horizon.py`.\r\n- Adding the K2-Horizon reasoning parser in `vllm/reasoning/k2_horizon_reasoning_parser.py`.\r\n- Adding the K2-Horizon tool-call parser in `vllm/tool_parsers/k2_horizon_tool_parser.py`.\r\n\r\n## Test Plan\r\n\r\nTest K2-Horizon model loading and inference using the vLLM OpenAI-compatible server, including reasoning and tool calling.\r\n\r\n```bash\r\nvllm serve \\\r\n    --model IFM/K2-Horizon-36B \\\r\n    --model-impl vllm \\\r\n    --tensor-parallel-size 8 \\\r\n    --dtype bfloat16 \\\r\n    --trust-remote-code \\\r\n    --reasoning-parser k2_horizon \\\r\n    --enable-auto-tool-choice \\\r\n    --tool-call-parser k2_horizon\r\n```\r\n\r\n## Test Result\r\n\r\nTested:\r\n\r\n- Model loading with the native vLLM implementation.\r\n- Tensor-parallel and pipeline-parallel inference.\r\n- Serving through the OpenAI-compatible Chat Completions API.\r\n- Live reasoning and tool-call responses.\r\n\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [x] The test plan, such as providing test command.\r\n- [x] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [x] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "092334c828e1114d6d8fb5828f7b0580e57e4a56",
      "pr_number": 52923,
      "pr_url": "https://github.com/vllm-project/vllm/pull/52923",
      "pr_title": "[Bugfix] Wait for offload keys before storing chunks",
      "pr_body": "## Summary\n\n- cap storable offload chunks by the number of available offload keys, in addition to computed tokens and allocated KV blocks\n- defer a chunk when its request block hash has not arrived yet instead of asserting in `_build_store_jobs`\n- add a CPU-only regression that verifies the deferred chunk is stored after the missing hash becomes available\n\nFixes #52170.\n\n## Why this happens\n\n`_build_store_jobs` can observe allocated KV blocks before `Request.block_hashes` has produced the matching offload key. `storable_chunks` previously used only the token-derived and block-allocation bounds, so the key and block slices could have different lengths. Treating available keys as the third bound preserves `next_stored_chunk_idx`, allowing the chunk to be reconsidered on a later scheduler step.\n\n## Duplicate-work checks\n\nNo open PR was found for this issue or fix area on August 19, 2026:\n\n```bash\ngh issue view 52170 --repo vllm-project/vllm --comments\ngh pr list --repo vllm-project/vllm --state open --search '52170 in:body'\ngh pr list --repo vllm-project/vllm --state open --search 'OffloadingConnector _build_store_jobs offload_keys offload_block_ids'\n```\n\n## Tests\n\nThe new regression reproduced the reported `assert len(offload_keys) == len(offload_block_ids)` failure before the fix.\n\n```bash\nHF_HOME=/tmp/vllm-test-hf-cache HF_HUB_OFFLINE=1 \\\nPYTHONPATH=/export/home/lishuo121/vllm-worktrees/offloading-store-jobs \\\n/export/home/lishuo121/vllm/.venv/bin/python -m pytest \\\n  tests/v1/kv_connector/unit/offloading_connector/test_scheduler.py \\\n  -k store_jobs_wait_for_missing_offload_key -v\n# 1 passed\n\nHF_HOME=/tmp/vllm-test-hf-cache HF_HUB_OFFLINE=1 \\\nPYTHONPATH=/export/home/lishuo121/vllm-worktrees/offloading-store-jobs \\\n/export/home/lishuo121/vllm/.venv/bin/python -m pytest \\\n  tests/v1/kv_connector/unit/offloading_connector/test_scheduler.py -v\n# 121 passed\n\npre-commit run --files \\\n  vllm/distributed/kv_transfer/kv_connector/v1/offloading/scheduler.py \\\n  tests/v1/kv_connector/unit/offloading_connector/test_scheduler.py\n# passed\n```\n\n## Model evaluation\n\nNot applicable. This changes scheduler bookkeeping for KV offload store jobs and does not affect model outputs or accuracy.\n\n## AI assistance\n\nAI assistance was used to investigate, implement, and test this change. The human submitter reviewed the changed lines and is responsible for understanding and defending the change end-to-end.\n\n\n"
    },
    {
      "sha": "859dd395128adbce604039f2d725be1067fdf9a3",
      "pr_number": 54962,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54962",
      "pr_title": "[Bugfix][Core] Wait for the previous PP tensor sends before the next forward pass",
      "pr_body": "## Purpose\n\nFixes silent output corruption under pipeline parallelism introduced by #49274.\n\n**Symptom.** GLM-5.3 (`GlmMoeDsaForCausalLM`, `Inferact/GLM-5.3-NVFP4`) served with\n`--pipeline-parallel-size 4` (4×H200) or `8` (8×H100), `--enable-chunked-prefill`\n(`max_num_batched_tokens=8192`), `--enable-prefix-caching`, bf16 KV cache: prompts of roughly\n8.2k–12k tokens come back as garbage on most fresh prefills (reasoning ends after 1–100 tokens,\nthen digit soup such as `1.0.0.0.5,0.9,0.1…` or `locklocklock…`). Longer prompts fail at a lower\nrate. With a strict `json_schema` the grammar still holds, so the output is `{` followed by\nlegal whitespace up to `max_tokens`. Nothing is logged. Because the corrupted prefill is written\nto the prefix cache, an affected prompt then fails deterministically on every repeat until its\nblocks are evicted — which is how this was first reported to us as \"one specific 31k-token prompt\nalways collapses, a one-character change to the system message fixes it\".\n\n**Bisection (the strongest evidence).** Same node, weights, flags and traffic throughout; each\nbuild probed over a port-forward with a unique `cache_salt` per request so every trial is a fresh\nprefill. \"Garbage\" = the probe above; \"strict schema\" = a `json_schema` whose only legal document\nis a sentinel string, sent with a 9,357-token prompt.\n\n| build | garbage at 8.3k–9.5k tokens | strict schema @ 9,357 tokens |\n|---|---|---|\n| `v0.28.0` (PP) | 0/44 | n/a (older masking bug) |\n| `nightly-46638857f` (Aug 26) | 0/38 | – |\n| `nightly-6f7df92a8` (Aug 28) | 0/18 | – |\n| `nightly-1dc464d42` (Aug 30) | 0/36 | 6/6 ok |\n| post-merge image `9d0fe9bac` (#54427) | 0/18 | – |\n| post-merge image `7ab292348` (#54313, **parent of #49274**) | **0/18** | **6/6 ok** |\n| post-merge image `c92b29a1d` (**#49274**) | **9/18** | **0/6** (`{` + whitespace) |\n| `nightly-44fe2a392` (Aug 31) | 6/12 | – |\n| `nightly-7c5dc571c` (Sep 1) | 29/30 | 8/8 masked, content corrupt |\n| `nightly-73029d424` (Sep 2), **TP+DCP instead of PP**, same model | 0/108 | 12/12 ok |\n\nThe parent commit is clean and #49274 itself is not, on the same hardware in the same hour. The\nsame build with tensor parallelism instead of pipeline parallelism is clean, and every non-PP\nmodel on the same images was clean (0/152), which is consistent with the defect living in the\ninter-stage send.\n\n**Root cause.** #49274 had two effects. Making the gloo metadata send non-blocking was its stated\npurpose (the vllm-ascend virtual-pipeline deadlock). It also **removed the wait at the top of\n`Worker.execute_model` on the previous step's send handles** and replaced it with\n`GroupCoordinator._pending_isends`, a list of Python references reaped lazily on the *next*\n`isend_tensor_dict` call, gated on `is_completed()`. That preserves buffer lifetime but not\nstream ordering: `Work.wait()` on an NCCL send makes the compute stream wait on the\ncommunication stream; `is_completed()` polling and `record_stream(current_stream)` do not.\nAfter the change, step N+1's forward pass can start while step N's inter-stage send is still\nreading its source tensors, and whenever N+1 writes into the same memory (CUDA-graph static\noutputs, reused activation buffers) the next stage receives a mix of N and N+1. The reaper runs\nonly when N+1's own send is posted, after N+1's forward has already written. A large send (a full\n8192-token chunk, tens of MB per stage) followed by a short step is the widest window, which\nmatches the prompt-length window we measured. The mechanism is our reading of the diff; the\nbisection is the hard evidence.\n\n**Fix.** Keep #49274's non-blocking metadata send and its retention list unchanged. Restore the\nwait for the **device-side tensor handles** (`handles[1:]`) at the top of the next\n`execute_model`, which is exactly the pre-#49274 behaviour for the tensor sends. The gloo\nmetadata handle (`handles[0]`) is deliberately not waited there — `_RetainedHandle.wait()` is\nidempotent and the coordinator's reaper still drops it — so the deadlock fix is preserved.\n\n**Not a duplicate.** `gh pr list --state open --search \"isend_tensor_dict\"`, `\"49274 in:body\"`\nand `\"pipeline parallel send wait\"` return no PR touching this path. #54908 fixes a different\ndefect (DCP dense-prefill K materialisation) and does not affect PP.\n\n## Test Plan\n\n```bash\n# new unit test: previous step's device handles are waited before the forward pass,\n# this step's device handles are kept, the metadata handle is left to the reaper\npytest tests/v1/worker/test_gpu_worker.py -k pp_send -v\n\npre-commit run --files vllm/v1/worker/gpu_worker.py tests/v1/worker/test_gpu_worker.py\n```\n\nEnd-to-end reproduction (any PP deployment; GLM-5.3-NVFP4 is what we used): serve with\n`--pipeline-parallel-size 2` or more, `--enable-chunked-prefill --max-num-batched-tokens 8192\n--enable-prefix-caching`; send a ~9,300-token prompt with `temperature: 0`, `max_tokens: 200`\nand a unique `cache_salt` per request, 20 times; on `c92b29a1d`…current main a large fraction\nreturn digit soup, on `7ab292348` none do. The same request under `--tensor-parallel-size N`\nis clean on every build.\n\n## Test Result\n\n- `pre-commit run --files …`: ruff check/format, typos, mypy-3.10, SPDX, forbidden-import and\n  the other local hooks pass.\n- Unit test, run inside `vllm/vllm-openai:nightly-73029d424` (arm64) against the installed\n  package: with the image's unpatched `gpu_worker.py` the new test **fails** as intended\n  (`assert ['forward', 'isend'] == ['wait:prev-tensor', 'forward', 'isend']`); with the patched\n  file copied over it, `tests/v1/worker/test_gpu_worker.py` is **8 passed**.\n- End-to-end on the patched build: not yet run by us — we have no GPU build environment on the\n  machine this was prepared on. The bisection table above is on unpatched builds. We can run\n  the patched build on the same 8×H100 PP=8 canary if a wheel or image is available; happy to\n  do so before this leaves draft.\n\n## AI assistance disclosure\n\nThis PR was prepared with AI assistance (Claude Code): the bisection was driven and the patch\nand unit test were drafted with it. The human submitter is reviewing every changed line and the\nevidence before marking the PR ready; it is opened as a draft for that reason.\n\n---\n<details>\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\n\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\n- [x] The test plan, such as providing test command.\n- [x] The test results, such as pasting the results comparison before and after, or e2e results\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\n</details>\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n\n<!-- This is an auto-generated comment: release notes by coderabbit.ai -->\n\n## Summary by CodeRabbit\n\n- **Bug Fixes**\n  - Improved GPU pipeline execution by ensuring intermediate data transfers from the previous step complete before the next forward pass begins.\n  - Preserved asynchronous transfer behavior while improving synchronization between pipeline stages.\n\n- **Tests**\n  - Added coverage to verify correct ordering and handling of pipeline data transfers.\n\n- **Chores**\n  - Increased the timeout for the H200 MIG 35GB Core, KV, and Metrics test step to improve test reliability.\n\n<!-- end of auto-generated comment: release notes by coderabbit.ai -->"
    },
    {
      "sha": "ee17d0d869203ef9a35ad73358a4987bba14b1fc",
      "pr_number": 51445,
      "pr_url": "https://github.com/vllm-project/vllm/pull/51445",
      "pr_title": "[Frontend] Use server-generated keys for late-interaction query caches",
      "pr_body": "# Use server-generated keys for late-interaction query caches\n\n## What this fixes\n\nFlash late-interaction scoring used the caller-controlled `X-Request-Id` header in its\ntemporary query-cache key. Two concurrent requests using the same header therefore used\nthe same cache entry.\n\nFor example, a victim and another client could both send `/score` with\n`X-Request-Id: shared-id`, but with different queries. Before this change, the second\nquery could replace the first query's stored embedding before the victim's document was\nscored. In the reproduced run, the victim's normal score was `369.72`; during the\ncollision it became `5.02`, exactly matching the score for the other client's query.\n\nAfter this change, vLLM creates a fresh server-generated namespace for each scoring\nrequest. The query and document phases share that private key, but another request cannot\nchoose the same one through its header.\n\n## Why it matters\n\nA network client who knows or guesses another client's request ID and wins a concurrency\nrace could change that client's `/score` or `/rerank` result. The attacker did not learn\nthe victim's query or document, and the server remained healthy, but the victim received\nthe wrong score.\n\n## The change\n\nThe query-encoding phase now builds its cache keys from `random_uuid()` and stores them\non `PoolingServeContext`. The document-scoring phase reuses those stored keys instead of\nrebuilding them from the public request ID. It fails clearly if the query keys were not\ninitialized.\n\n## How it was tested\n\nThe regression test uses two contexts with the same caller request ID. Without the fix,\nthe test failed because their query keys matched. With the fix, it passed and confirmed\nthat each request has a distinct key while its own query and document phases still agree.\n\n## Reference\n\nAdvisory: GHSA-2phq-3phc-84px\n\nTrack B: this is a public PR against `vllm-project/vllm@main`.\n\n## Credit\n\n- @KernelClint (Clinton Thomas) and @dhalf (Lucas Bourtoule)\n- Patch the Planet (Trail of Bits + OpenAI collaboration); discovered using GPT-5.5-Cyber\n\nAll commits include DCO `Signed-off-by` trailers.\n"
    },
    {
      "sha": "3f1af35e012a283a58caf17216c108d8fca9e6c5",
      "pr_number": 54994,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54994",
      "pr_title": "[Bugfix][Multimodal] Handle prefix-covered items in SHM worker cache",
      "pr_body": "## Purpose\n\nPrevent repeated multimodal requests from terminating `EngineCore` when\nprefix caching and the shared-memory multimodal processor cache are enabled\ntogether.\n\nThe production trigger chain is:\n\n```text\nsecond request with the same image and prompt\n  -> prefix cache covers the image placeholder range\n  -> NewRequestData.from_request()\n  -> strip_covered_mm_data() replaces the feature payload with data=None\n  -> worker_base._apply_mm_cache()\n  -> ShmObjectStoreReceiverCache.get_and_update_features()\n  -> touch_receiver_cache_item() asserts that data is not None\n  -> EngineCore terminates and the request returns HTTP 500\n```\n\n`data=None` is intentional for ordinary payloads. #52041 avoids broadcasting\ntensor payloads for multimodal items whose placeholder range is fully covered\nby the prefix cache, because no encoder input can be scheduled for those items.\nHowever, an SHM cache hit does not carry the tensor payload: it carries only an\n`address` and `monotonic_id`. Stripping that small descriptor leaves the worker\nwithout the address required by its receiver cache.\n\nThe address is also part of the SHM lifetime protocol. Each sender-side cache\nhit increments a writer reference, and each worker must acknowledge that\nreference through the existing touch/get path. Merely allowing the worker to\nskip `data=None` would stop the crash but leave the oldest object permanently\nnon-reclaimable, eventually blocking FIFO ring-buffer eviction.\n\nThis change makes `strip_covered_mm_data()` preserve address-only SHM items.\nSHM workers therefore retain the existing resolution and reference-counting\npath for cached objects. Ordinary tensor payloads are still stripped; the SHM\nreceiver skips those resulting `None` features because an uncached payload has\nno SHM reference to acknowledge and no encoder input will consume it. This also\ncovers the existing fallback when an item is too large for SHM or the ring\nbuffer cannot currently accept it. The LRU receiver behavior is unchanged,\nincluding its `data=None` cache lookup and P0/P1 drift recovery.\n\n### Duplicate-work check\n\nSearches of open vLLM pull requests and issues for SHM multimodal receiver\nfailures, prefix-cache-stripped multimodal data, and `Expected an address item`\nfound no open fix for this crash.\n\nThis does not duplicate #52040 or #52041. They introduced the optimization that\nomits fully covered multimodal tensor payloads from the worker broadcast; this\nPR preserves the already-small SHM descriptor that the receiver requires. Open\nPR #51349 changes the underlying shared-memory transport design but does not\naddress this `data=None` worker-cache failure.\n\n## Test Plan\n\nRun the existing multimodal cache and scheduler-output tests:\n\n```bash\npython -m pytest -q \\\n  tests/multimodal/test_cache.py -k 'not sleep_wake' \\\n  tests/v1/core/test_output.py\n```\n\nRun all pre-commit hooks selected for the changed production file:\n\n```bash\npre-commit run --files \\\n  vllm/multimodal/utils.py \\\n  vllm/multimodal/cache.py\ngit diff --check\n```\n\nFor end-to-end validation, start the server on an NVIDIA A100 40 GB with:\n\n```bash\nvllm serve llava-hf/llava-onevision-qwen2-0.5b-ov-hf \\\n  --served-model-name llava-onevision \\\n  --enable-prefix-caching \\\n  --mm-processor-cache-type shm \\\n  --mm-processor-cache-gb 0.1 \\\n  --mm-shm-cache-max-object-size-mb 64 \\\n  --enforce-eager\n```\n\nSubmit the same image and prompt repeatedly to `/v1/chat/completions`, then send\na different image and return to the first image. Compare the unmodified\nimplementation with this change, verifying HTTP status, prefix-cache statistics,\nengine health, and generated text.\n\nRepeat the identical-request test with\n`--mm-shm-cache-max-object-size-mb 1`. This forces the processed image to remain\nuncached and exercises the separate prefix-covered `data=None` fallback path.\n\n## Test Result\n\nExisting focused tests and static checks:\n\n```text\ntests/multimodal/test_cache.py and tests/v1/core/test_output.py\n  18 passed, 1 deselected\n\npre-commit hooks for both changed production files\n  all applicable hooks passed, including ruff, mypy, SPDX, and repository checks\n\ngit diff --check\n  passed\n```\n\nEnd-to-end A/B result with\n`llava-hf/llava-onevision-qwen2-0.5b-ov-hf`, eager execution, and greedy\ndecoding:\n\n| Request | Unmodified | This PR |\n| --- | --- | --- |\n| First image request | HTTP 200 | HTTP 200 |\n| Identical repeated requests | second request: HTTP 500; `EngineCore` terminated | all HTTP 200 |\n| Different image, then original image | not reachable after engine failure | both HTTP 200 |\n| Final `/health` | not reachable after engine failure | HTTP 200 |\n| Generated text for successful requests | `The image is a simple text-based graphic` | same |\n\nOn the unmodified run, the second request had 1,344 prefix-cache hits out of\n1,347 prompt tokens. Its scheduled feature had `data=None`, and the fatal stack\nended at:\n\n```text\nworker_base.py::_apply_mm_cache\n  -> BaseMultiModalReceiverCache.get_and_update_features\n  -> ShmObjectStoreReceiverCache.touch_receiver_cache_item\n  -> assert mm_item is not None\n```\n\nWith this change, all five requests returned HTTP 200 in 0.220, 0.132, 0.125,\n0.157, and 0.125 seconds. Requests 1-3 used the first image, request 4 introduced\na second image, and request 5 returned to the first image. The cumulative server\nmetrics reported a 59.9% prefix-cache hit rate and a 60.0% multimodal processor\ncache hit rate, matching the three reused requests. The engine remained healthy.\n\nA focused SHM probe exercised both fully covered input forms. A cached address\nwas preserved, resolved by the worker, and remained reclaimable; an uncached raw\npayload was stripped to `None` and accepted by the receiver without an SHM\nlookup. This confirms that the fix neither strands a writer reference nor\ncrashes on the oversized/cache-full fallback path.\n\nThe SHM probe also simulated two worker readers and filled a 1 MiB ring buffer\nwith 40 objects after repeated cache hits. Writer and reader references remained\nbalanced, the oldest acknowledged objects were evicted, and 10 objects remained\nresident. A mixed list containing a stripped item, an SHM address, and an inline\nitem preserved its order and resolved both live items.\n\nThe end-to-end oversized-object run used a 1 MiB per-object limit. The processed\nimage was approximately 2.65 MiB, and server metrics confirmed a 0.0% multimodal\nprocessor cache hit rate. Three identical requests all returned HTTP 200 in\n0.236, 0.148, and 0.151 seconds; the prefix-cache hit rate reached 66.5%, and the\nfinal `/health` request returned HTTP 200. This directly exercises the uncached\n`data=None` branch on the second and third requests.\n\nA model-accuracy evaluation is not applicable. The change preserves an internal\nSHM address and restores the receiver path used before #52041; it does not change\nmodel inputs used for execution, weights, kernels, sampling, or\nsuccessful-request output.\n\nNo documentation update is needed because this restores compatibility between\ntwo existing cache options without changing their public interface or\nconfiguration.\n\n## AI-assisted contribution\n\nThis contribution was developed with AI assistance.\n\n---\n\n<details>\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\n\n- [x] The purpose and production trigger chain are explained.\n- [x] The test plan includes focused tests, static checks, and an end-to-end A/B scenario.\n- [x] The test results include before-and-after HTTP, cache-hit, and engine-health evidence.\n- [x] Documentation impact was considered; no update is needed for this internal bug fix.\n\n</details>\n\n\n<!-- This is an auto-generated comment: release notes by coderabbit.ai -->\n\n## Summary by CodeRabbit\n\n* **Bug Fixes**\n  * Fixed shared-memory multimodal caching when prefix caching strips an uncached payload.\n  * Preserved shared-memory references needed to resolve payloads and acknowledge resource ownership.\n  * Prevented cache updates for multimodal items without payload data, while retaining valid shared-memory descriptors.\n* **Tests**\n  * Added regression coverage for shared-memory cache resolution, reference acknowledgment, and resource reclamation.\n\n<!-- end of auto-generated comment: release notes by coderabbit.ai -->"
    },
    {
      "sha": "27a94d1ce4e3fc100c4732439ccec10f8246a804",
      "pr_number": 55042,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55042",
      "pr_title": "[CI] Fix DeepSeek-V4 registry platform guard",
      "pr_body": "## Purpose\r\n\r\n#54566 added an NVIDIA-only DeepSeek-V4 vision implementation with a stub for unsupported platforms. Its registry-test guard was inverted: it skipped CUDA while allowing ROCm to validate the unsupported stub as a real generation model. This caused the AMD MI300 `basic-models-other` job in #54177 to fail because the stub intentionally lacks `embed_input_ids`.\r\n\r\nThis change corrects the guard so unsupported non-CUDA platforms skip the registry interface checks while CUDA continues validating the real implementation.\r\n\r\n- Originating PR: https://github.com/vllm-project/vllm/pull/54566\r\n- CI failures: \r\n- [Build 86967](https://buildkite.com/vllm/ci/builds/86967#01a06488-581a-491d-bc18-e3712e2e2e4b)\r\n- [Build 86965 / PR #54962](https://buildkite.com/vllm/ci/builds/86965) \r\n- [Build 86966 / PR #54651](https://buildkite.com/vllm/ci/builds/86966) \r\n- [Build 86967 / PR #54177](https://buildkite.com/vllm/ci/builds/86967)\r\n\r\n\r\n\r\n## AI assistance disclosure\r\nOpenAI Codex (GPT-5) assisted in root cause analysis of this CI issue"
    },
    {
      "sha": "5d09eb2cf310486c03385dd8d97aac5ceee5abc1",
      "pr_number": 46009,
      "pr_url": "https://github.com/vllm-project/vllm/pull/46009",
      "pr_title": "[Bugfix][MoE] Preserve unquantized weight storage on ROCm",
      "pr_body": "## Summary\n- Preserve unquantized MoE parameter storage during ROCm post-load padding by copying the padded view into existing storage instead of rebinding `.data`.\n- Return directly after AITER unquantized MoE `shuffle_weights()` so the AITER runtime layout is not forced through an extra `.contiguous()` conversion.\n- Add a ROCm MoE weight replay test covering both AITER and TRITON backends.\n\n## Motivation\nIn RL-style serving and training loops, external weight updates can trigger `process_weights_after_loading()` more than once after model initialization. Reassigning `param.data` in that path can replace the underlying parameter storage. That is unsafe once CUDA graphs or runtime kernels have captured parameter storage addresses, and can leave later rollouts reading stale or invalid storage.\n\nWe observed this as a training-time rollout corruption issue after weight updates: generated responses became invalid, and reward/score collapsed to 0. Preserving the existing parameter storage with `.copy_()` keeps captured addresses stable while still applying the padded layout.\n\nFor AITER unquantized MoE, `shuffle_weights()` already returns the runtime layout consumed by AITER. In local Qwen3.5-35B GRPO measurements, avoiding the final contiguous conversion for AITER was slightly faster after normalizing rollout generation time by response length.\n\n## Replay test\nThe new `tests/rocm/test_moe_weight_replay.py` test constructs a Qwen3.5 MoE LLM with sleep mode and CUDA graphs, generates a deterministic greedy response, runs level-2 sleep/wake, then replays checkpoint-format weights through a framework-agnostic load-then-post-load helper:\n\n1. stream checkpoint-format weights into `model.load_weights(...)` in buckets;\n2. call `process_weights_after_loading(...)` once after all buckets are loaded;\n3. verify the ROCm MoE padding branch was actually exercised;\n4. verify MoE `w13_weight` / `w2_weight` parameter storage addresses do not change;\n5. verify greedy token IDs are deterministic after replay.\n\nThis covers both AITER and TRITON MoE backends as first-class correctness cases.\n\n## Local validation\nStatic/lightweight checks in the PR checkout:\n- `python3 -m py_compile tests/rocm/test_moe_weight_replay.py vllm/model_executor/layers/fused_moe/unquantized_fused_moe_method.py vllm/model_executor/layers/fused_moe/oracle/unquantized.py`\n- `python3 -m ruff check tests/rocm/test_moe_weight_replay.py vllm/model_executor/layers/fused_moe/unquantized_fused_moe_method.py vllm/model_executor/layers/fused_moe/oracle/unquantized.py`\n- `python3 -m pytest tests/kernels/moe/test_unquantized_backend_selection.py tests/kernels/moe/test_moe_weight_loading_padded.py`\n  - 38 passed, 2 skipped\n\nHeavy replay validation with Qwen3.5-35B-A3B and tensor parallel size 2:\n- pre-fix AITER: failed as expected; the test entered the ROCm padding branch and detected MoE parameter `data_ptr` changes.\n- pre-fix TRITON: failed as expected; the test entered the ROCm padding branch and detected MoE parameter `data_ptr` changes.\n- fixed AITER: passed.\n- fixed TRITON: passed.\n\n## Benchmark note\nLocal 10-step Qwen3.5-35B GRPO runs kept score/reward normal with zero aborted responses. Normalized rollout timing for AITER was slightly better without the final contiguous conversion: about 22.95 ms/token vs 23.11 ms/token."
    },
    {
      "sha": "e47356c63e4ab013bfe2af166b0f3952c30ed30b",
      "pr_number": 55002,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55002",
      "pr_title": "[ROCm][Installation] Add mooncake package to image using public wheels",
      "pr_body": "## Purpose\r\n\r\nUpdating the recent [PR#52650](https://github.com/vllm-project/vllm/pull/52650) by removing those changes and simply installing mooncake via the public wheels that are now published.\r\n\r\nSame testing was done as original PR, and the results are the same.\r\n\r\nAddressed issue #51193\n\n\n\n## Summary by CodeRabbit\n\n- **Changes**\n  - ROCm build requirements now include the Mooncake transfer engine, version 0.3.13 or later, supporting KV-cache transfer and offloading.\n  - Mooncake is no longer built, packaged, installed, or recorded in ROCm Docker images.\n  - Mooncake-specific ROCm CI cache targets and cache configuration have been removed.\n  - Existing ROCm support for NIXL, ROCShmem, and DeepEP remains unchanged.\n\n"
    },
    {
      "sha": "5e4e927b5e3a1cc7c1debe11e503a78ea241689f",
      "pr_number": 55044,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55044",
      "pr_title": "[CI] Force HTTP/1.1 for runtime Git installs",
      "pr_body": "## Summary\n\n- force Git's smart protocol to use HTTP/1.1 in the H200 Hybrid and V1 Core/KV/Metrics steps\n- keep the workaround scoped to the jobs that install source dependencies from GitHub at runtime\n- prevent HTTP/2 upload-pack 401 responses from turning into interactive username-prompt timeouts\n\n## Root cause\n\nThe failing H200 jobs can fetch public GitHub refs, but Git 2.43/libcurl 8.5 in the CI image sends the upload-pack POST over HTTP/2 and receives HTTP 401. Git then prompts for a username and the job hangs until its 65/80-minute timeout. Host-side Git on the same machines succeeds, so checkout and host egress are not the failing boundary.\n\nThe same CI image reproduces the protocol split exactly:\n\n- forced `HTTP/2`: exit 128, `fatal: expected flush after ref listing`\n- `GIT_CONFIG_COUNT=1`, `GIT_CONFIG_KEY_0=http.version`, `GIT_CONFIG_VALUE_0=HTTP/1.1`: public tag lookup succeeds\n\nRepresentative failures:\n\n- Hybrid shard 1: https://buildkite.com/vllm/ci/builds/86957#01a06435-5c3a-4e5f-97b7-3ba97e89b532\n- Hybrid shard 2: https://buildkite.com/vllm/ci/builds/86957#01a06435-5c3a-4a73-b38c-04d7bb702d3e\n- V1 Core/KV/Metrics: https://buildkite.com/vllm/ci/builds/86957#01a06435-5c39-42aa-ae00-07e4657ef9fc\n\n## Duplicate work\n\nI searched open vLLM PRs and issues for `http.version`, `HTTP/1.1`, `expected flush after ref listing`, `could not read Username`, and the Mamba/causal-conv1d install path. I found no open PR addressing this CI transport failure.\n\n## Validation\n\n- Reproduced forced HTTP/2 failure and HTTP/1.1 success in the same current vLLM CI image on `h200-ci-4`\n- Parsed both modified YAML files through the ci-infra `Step` schema and asserted the exact environment mapping\n- `pre-commit run --hook-stage pre-commit --files .buildkite/test_areas/models_language.yaml .buildkite/test_areas/misc.yaml` passed\n- `git diff --check` passed\n- Model evaluation: not applicable; this changes CI transport configuration only\n\nThis contribution was prepared with AI assistance and reviewed line by line by the human submitter.\n"
    },
    {
      "sha": "0e14198a63c03f899a10f3e782e88eca7f11265b",
      "pr_number": 54995,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54995",
      "pr_title": "[Skills] Add kernel benchmark sanity references",
      "pr_body": "## Summary\n\n- add rough Blackwell sanity-check references for memory-bound, GEMM, and\n  attention kernel benchmarks\n- clarify that the figures are diagnostic reference points rather than gold\n  standards, guaranteed peaks, or hard limits\n- note the FlashAttention-4 source for the B200 BF16 attention result\n\n## Duplicate-work check\n\nSearched open PRs for `kernel-microbenchmark`, `PFLOP/s`, and\n`FlashAttention-4 skill`; no matching PR was found. This change is not linked\nto an issue.\n\n## Tests\n\n- `.venv/bin/python /home/woosuk/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/kernel-microbenchmark` — passed (`Skill is valid!`)\n- `git diff --check` — passed\n- `.venv/bin/pre-commit run --files .agents/skills/kernel-microbenchmark/SKILL.md` — passed (applicable hooks, including markdownlint and typos)\n\nModel evaluation is not applicable because this changes contributor skill\nguidance only and does not affect model output, accuracy, or serving behavior.\n\n## AI assistance\n\nOpenAI Codex assisted with editing, duplicate-work checks, validation, the\ncommit, and drafting this PR description. The human submitter reviewed every\nchanged line and is responsible for the change.\n"
    },
    {
      "sha": "ad127d9a0fb16c92de563e674e3737463e7f8688",
      "pr_number": 55012,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55012",
      "pr_title": "[Perf][Rust Frontend] Coalesce decoded chunks per engine update",
      "pr_body": "## Purpose\r\n\r\nStreaming engine updates can carry multiple generated tokens with MTP or speculative decoding. The Rust frontend currently extracts and appends a decoded chunk after every token, then yields one `TextDelta` for the whole engine update.\r\n\r\nThis change extracts at most one decoded chunk after each nonterminal engine update. Per-token decoding and stop-string checks remain in place, while terminal updates continue through the existing `flush()` path.\r\n\r\nLocal release microbenchmark results for the full incremental decode loop (`push_token` + decode + `next_chunk` + append), using the Qwen3.5-0.8B tokenizer, 1,000 requests with 256 output tokens each, CPU 69 affinity, and the median of 7 samples:\r\n\r\n| MTP batch | CPU time | Loop throughput | Allocations |\r\n|---:|---:|---:|---:|\r\n| 1 | Flat | Flat | Flat |\r\n| 2 | -10.7% | +12.0% | -14.2% |\r\n| 4 | -16.9% | +20.4% | -18.3% |\r\n| 8 | -19.9% | +24.9% | -18.2% |\r\n| 16 | -20.9% | +26.4% | -18.3% |\r\n\r\nThe benchmark harness was temporary and is excluded from this PR. End-to-end gains depend on the share of frontend CPU spent in incremental detokenization.\r\n\r\n## Test Plan\r\n\r\n- `cargo nextest run -p vllm-text`\r\n- `cargo clippy -p vllm-text --all-targets -- -D warnings`\r\n- `git diff --check origin/main...HEAD`\r\n\r\n## Test Result\r\n\r\n- `vllm-text`: 90 passed, 1 skipped.\r\n- Clippy passed with warnings denied.\r\n- Diff check passed.\r\n- Model evaluation is not applicable because decoded text, token IDs, logprobs, stop handling, and streaming event boundaries remain unchanged.\r\n\r\nAI assistance was used for code changes, validation, benchmarking, and PR preparation. Every changed line and the reported benchmark scope were reviewed by the submitter.\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [x] The test plan, such as providing test command.\r\n- [x] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [x] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "443febe723f62381cda46a9d4f989b8e74a8a857",
      "pr_number": 55028,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55028",
      "pr_title": "[Agents] Expose Triton kernel-writing skill to Claude",
      "pr_body": "## Summary\n\n- Expose the Triton kernel-writing skill added by #55019 under `.claude/skills`.\n- Follow the existing repository convention by symlinking to the canonical `.agents/skills` copy, avoiding duplicated guidance.\n\n## Duplicate-work check\n\nOpen PR searches for `triton-kernel-writing Claude`, `Expose Triton skill to Claude`, and `.claude/skills/triton-kernel-writing` found no overlapping change. There is no linked issue.\n\nThis is a focused follow-up to merged PR #55019. That PR added the canonical skill under `.agents/skills`; this PR makes the same skill discoverable from `.claude/skills` without copying it.\n\n## Testing\n\n- `.venv/bin/python /home/woosuk/.codex/skills/.system/skill-creator/scripts/quick_validate.py .claude/skills/triton-kernel-writing` — passed.\n- `.venv/bin/pre-commit run --files .claude/skills/triton-kernel-writing` — all applicable hooks passed or skipped.\n- Verified that the symlink resolves to `../../.agents/skills/triton-kernel-writing` and its `SKILL.md` is readable.\n\n## Model evaluation\n\nNot applicable. This changes skill discovery only and does not affect model output, accuracy, or serving behavior.\n\n## AI assistance and accountability\n\nOpenAI Codex assisted with implementing and validating this change. The human submitter must review the changed line and understand and defend the contribution end-to-end.\n\n\n<!-- This is an auto-generated comment: release notes by coderabbit.ai -->\n\n## Summary by CodeRabbit\n\n* **Chores**\n  * Added a link to the Triton kernel-writing guidance for improved access to development resources.\n\n<!-- end of auto-generated comment: release notes by coderabbit.ai -->"
    },
    {
      "sha": "cf3263d571dfa21e82787356b9506cbf80b68ad6",
      "pr_number": 55019,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55019",
      "pr_title": "[Agents] Add Triton kernel-writing skill",
      "pr_body": "## Summary\n\n- Add a concise, repository-local Triton kernel-writing skill.\n- Cover generated-code inspection, tuning and specialization choices, launch-grid limits, 64-bit offsets, read-after-write synchronization, and decode-versus-prefill tiling.\n- Require representative shape sweeps and numerically difficult correctness inputs.\n\n## Duplicate-work check\n\nOpen PR searches for `Triton kernel skill`, `agent skill Triton`, `kernel-writing`, `triton-kernel-writing`, and `Triton Kernel Writing` found no PR addressing this change. There is no linked issue.\n\nThis is materially different from closed PR #55009. That PR imported and adapted a much larger TensorRT-LLM skill under `kernel-triton-writing`, including provenance and reference documents. This PR instead adds a clean, focused, vLLM-specific skill under `triton-kernel-writing` with only the guidance requested here and no imported content.\n\n## Testing\n\n- `.venv/bin/python /home/woosuk/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/triton-kernel-writing` — passed.\n- `.venv/bin/pre-commit run --files .agents/skills/triton-kernel-writing/SKILL.md .agents/skills/triton-kernel-writing/agents/openai.yaml` — all applicable hooks passed.\n\n## Model evaluation\n\nNot applicable. This adds agent guidance only and does not change model output, accuracy, or serving behavior.\n\n## AI assistance and accountability\n\nOpenAI Codex assisted with drafting and validating this change. The human submitter must review every changed line and understand and defend the contribution end-to-end.\n\n\n<!-- This is an auto-generated comment: release notes by coderabbit.ai -->\n## Summary by CodeRabbit\n\n* **Documentation**\n  * Added guidance for writing and reviewing Triton kernels, covering implementation practices, launch configuration, indexing, synchronization, data types, boundary validation, benchmarking, and compilation overhead.\n  * Added recommendations for avoiding unintended runtime compilation and accounting for compilation and autotuning costs during performance evaluation.\n  * Added agent guidance that helps apply these recommendations when implementing or reviewing Triton kernels, with clear metadata and default instructions.\n<!-- end of auto-generated comment: release notes by coderabbit.ai -->"
    },
    {
      "sha": "6c6376a09e89acfbe62954246739f0dd52b892c7",
      "pr_number": 55026,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55026",
      "pr_title": "[CI] Remove deleted nvidia/Nemotron-3-Nano-Omni-30B-A3B-Reasoning-BF16 and its arch aliases",
      "pr_body": "## Purpose\n\nThe HF repo `nvidia/Nemotron-3-Nano-Omni-30B-A3B-Reasoning-BF16` has been removed from Hugging Face (404). Tests that parameterize on it now fail, e.g. in https://buildkite.com/vllm/ci/builds/86943#01a063ed-36c0-435f-84ec-efd837f9eec7:\n\n```\nFAILED models/multimodal/processing/test_common.py::test_processing_correctness[1.0-32-0.5-nvidia/Nemotron-3-Nano-Omni-30B-A3B-Reasoning-BF16_0]\npydantic_core._pydantic_core.ValidationError: 1 validation error for ModelConfig\n```\n\nThat deleted repo was the only checkpoint ever associated with the three `NemotronH_*_Omni_Reasoning_V3` architectures (two of them were already marked `is_available_online=False` with a TODO to update the repo id once public). This PR removes them entirely:\n\n- `tests/models/registry.py`: drop the `NemotronH_Nano_Omni_Reasoning_V3`, `NemotronH_Super_Omni_Reasoning_V3`, and `NemotronH_Omni_Reasoning_V3` entries (plus the now-unused `PretrainedConfig` import).\n- `vllm/model_executor/models/registry.py`: drop the three arch aliases (they mapped to `NemotronH_Nano_VL_V2`, which remains registered and is still tested via `nvidia/NVIDIA-Nemotron-Nano-12B-v2-VL-BF16`).\n- `vllm/config/speculative.py`: drop the now-dead MTP `text_config` promotion for the two omni arch names.\n\nRemoving the aliases (not just the test entries) is required to keep `tests/models/test_registry.py::test_hf_registry_coverage` passing — it asserts every registered arch has a test-registry entry.\n\n## Duplicate-work check\n\nSearched open PRs and issues for `Nemotron-3-Nano-Omni` / `NemotronH_Nano_Omni` — no existing PR addresses this removal.\n\n## Test plan\n\n- All touched files parse cleanly (`ast.parse`); no references to the deleted repo or the three arch names remain in the repo.\n- Could not run pytest locally (env missing deps); the affected CI jobs are the multimodal processor shard (currently failing on the 404) and Basic Models (Other) (`test_registry.py`).\n\n## AI assistance\n\nThis change was made with AI assistance (Kimi Code CLI).\n\n<!-- This is an auto-generated comment: release notes by coderabbit.ai -->\n\n## Summary by CodeRabbit\n\n- **Chores**\n  - Removed three NemotronH Omni Reasoning V3 variants from multimodal model examples and architecture listings.\n  - Simplified related test configuration.\n\n- **Behavior Changes**\n  - NemotronH Omni VLM checkpoints are no longer automatically rewritten to MTP configurations during speculative decoding.\n  - Removed automatic vision configuration overrides for the affected Nano variant.\n\n<!-- end of auto-generated comment: release notes by coderabbit.ai -->"
    },
    {
      "sha": "a0d3e5c16e96b62ec5c7f741952cfddc99534faf",
      "pr_number": 53678,
      "pr_url": "https://github.com/vllm-project/vllm/pull/53678",
      "pr_title": "[XPU] Add fused GemmaRMSNorm path for eager execution",
      "pr_body": "## Purpose\r\nGemmaRMSNorm on XPU fell back to separated elementwise ops under enforce-eager, causing a large prefill latency gap versus torch.compile (which Inductor-fuses the same ops). \r\n- Add GemmaRMSNorm.forward_xpu that dispatches to the fused vllm-xpu-kernels gemma_rms_norm / fused_add_gemma_rms_norm ops, passing the raw (bf16/fp16) weight so the (1 + weight) offset and fp32 multiply are folded into the kernel (matching forward_native numerics). \r\n- Falls back to forward_native when the kernels are unavailable in the installed package. \r\n- Depends on https://github.com/vllm-project/vllm-xpu-kernels/pull/532\r\n- Also add the corresponding _custom_ops wrappers and guarded register_fake entries.\r\n\r\nAI assistance (GitHub Copilot CLI) was used to author this change.\r\n\r\n## Test Result\r\n\r\n### Performance (enforce-eager prefill on Intel B70 input_len=8192)\r\n\r\n| Model            | TP | eager (native fallback) | eager (fused) | torch.compile |\r\n|------------------|----|-------------------------|---------------|---------------|\r\n| Qwen3.5-9B       | 1  | 1.256 s                 | **1.023 s**   | 1.013 s       |\r\n| Qwen3.5-35B-A3B  | 4  | 0.580 s                 | **0.419 s**   | 0.421 s       |\r\n\r\n### Accuracy (GSM8K, lm-eval-harness, max_gen_toks=4096)\r\n\r\n| Model            | TP | exact_match (strict) | exact_match (flexible) |\r\n|------------------|----|----------------------|------------------------|\r\n| Qwen3.5-9B       | 1  | 0.8923               | 0.8901                 |\r\n| Qwen3.6-27B      | 4  | 0.6748               | 0.6755                 |\r\n| Qwen3.5-35B-A3B  | 4  | 0.9462               | 0.9538                 |\r\n\r\n"
    },
    {
      "sha": "963054ed580824e0f3901e3a53f7994349434d13",
      "pr_number": 55023,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55023",
      "pr_title": "[CI] Exclude nightly-dev tags from nightly DockerHub cleanup",
      "pr_body": "## Purpose\n\nThe nightly cleanup script (`.buildkite/scripts/cleanup-nightly-builds.sh`) runs after every nightly release build and deletes all but the newest 14 DockerHub tags matching the `nightly-` prefix. Since `nightly-dev` tags also start with `nightly-`, they are matched by the prefix filter, counted toward the keep-14 window, and can be deleted.\n\nThis PR excludes any tag starting with `nightly-dev` from the jq selection so those tags are never deleted and no longer count toward the keep window.\n\n## Test plan\n\nVerified the jq filter locally:\n\n```bash\necho '{\"results\":[{\"name\":\"nightly-abc123\",\"last_updated\":\"2026-09-01\"},{\"name\":\"nightly-dev\",\"last_updated\":\"2026-08-01\"},{\"name\":\"nightly-dev-foo\",\"last_updated\":\"2026-08-02\"},{\"name\":\"nightly-def456\",\"last_updated\":\"2026-08-03\"}]}' | jq -r --arg prefix \"nightly-\" '.results[] | select(.name | startswith($prefix)) | select(.name | startswith(\"nightly-dev\") | not) | \"\\(.last_updated)|\\(.name)\"'\n# Output:\n# 2026-09-01|nightly-abc123\n# 2026-08-03|nightly-def456\n```\n\n`nightly-dev` and `nightly-dev-foo` are excluded; regular `nightly-*` tags pass through. `shellcheck` on the modified script passes with no warnings.\n\n## Duplicate check\n\nSearched open PRs for \"nightly-dev cleanup\", \"cleanup-nightly-builds\", and \"nightly tag delete\" — no existing PR addresses this.\n\n---\nThis change was made with AI assistance (Kimi Code). All changed lines have been reviewed by the human submitter.\n\n<!-- This is an auto-generated comment: release notes by coderabbit.ai -->\n\n## Summary by CodeRabbit\n\n* **Chores**\n  * Nightly cleanup now excludes DockerHub tags beginning with `nightly-dev`, preventing them from being selected for deletion.\n\n<!-- end of auto-generated comment: release notes by coderabbit.ai -->"
    },
    {
      "sha": "e3e1241003473e33197e9b5d73e8f091fc75aad1",
      "pr_number": 55011,
      "pr_url": "https://github.com/vllm-project/vllm/pull/55011",
      "pr_title": "[ROCm][CI] Extend Multimodal Processor Shard timeout on AMD CI",
      "pr_body": "\r\nAs seen in https://buildkite.com/vllm/amd-ci/builds/12553/list?jid=01a0615a-0215-4914-8a00-d7b55431dbee&tab=output, the timeout for `(CPU) Multimodal Processor Shard %N` in AMD CI is sometimes hit. Here we extend the timeout to allow it to finish.\n\n\n\n## Summary by CodeRabbit\n\n* **Chores**\n  * Increased the timeout for AMD CPU multimodal processor shard tests from 60 to 90 minutes.\n\n"
    },
    {
      "sha": "60857baa53a743913d4e919f12b5fd8d608ded86",
      "pr_number": 54854,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54854",
      "pr_title": "[Bugfix][Rust Frontend][Renderer] Align DeepSeek V4 historical developer message handling",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\n\r\n  The Rust DeepSeek V4 renderer kept developer messages that appeared before the\r\n  last user-like turn when `thinking=true` and effective `drop_thinking=true`.\r\n\r\n  The Python tokenizer and the official DeepSeek encoding remove those historical\r\n  developer messages while preserving assistant answers and dropping historical\r\n  reasoning.\r\n\r\n  Align the Rust DeepSeek V4 renderer with the Python tokenizer and the official\r\n  DeepSeek encoding when dropping historical thinking messages.\r\n\r\n## Test Plan\r\n\r\n```\r\n  cargo test -p vllm-chat developer -- --nocapture\r\n\r\n  cargo test -p vllm-chat \\\r\n    renderer::deepseek_v4::tests \\\r\n    -- --nocapture\r\n```\r\n\r\n## Test Result\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "a56654d6de060495ff2db3b1d9ff0b187084d1a9",
      "pr_number": 54565,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54565",
      "pr_title": "[K3 Perf] Enable DSV3 GEMM for inner-contiguous and row-strided tensors, 12%~81% kernel performance improvement",
      "pr_body": "## Summary\r\n\r\nEnable DSV3 GEMM for inner-contiguous and row-strided tensors\r\n\r\nThey previously fell back to `F.linear`/NVJet because they were not fully contiguous.\r\n\r\nThis PR fixes the issue\r\n\r\n## Test\r\n\r\nAcc covered in unit test\r\n\r\nPerf can be seen in this AI generated script\r\n\r\n```py\r\nimport torch\r\nimport torch.nn.functional as F\r\n\r\nfrom vllm.models.kimi_k3.nvidia import low_latency_gemm\r\nfrom vllm.triton_utils import triton\r\n\r\n# name, M, N, K, source row width, slice offset\r\nCASES = (\r\n    (\"B1 KDA f_b_proj\", 1, 1536, 128, 6288, 6144),\r\n    (\"B4 KDA f_b_proj\", 4, 1536, 128, 6288, 6144),\r\n    (\"B16 KDA f_b_proj\", 16, 1536, 128, 6288, 6144),\r\n    (\"B1 MLA q_b_proj\", 1, 2304, 1536, 2112, 0),\r\n)\r\n\r\n\r\ndef bench(fn) -> float:\r\n    return 1000 * triton.testing.do_bench_cudagraph(fn, rep=5, return_mode=\"median\")\r\n\r\n\r\ndef main() -> None:\r\n    torch.manual_seed(0)\r\n    print(f\"GPU: {torch.cuda.get_device_name()}\")\r\n    print(\"| Case | [M,N,K] | x.stride() | NVJet (us) | DSV3 (us) | Reduction |\")\r\n    print(\"|---|---:|---:|---:|---:|---:|\")\r\n\r\n    for name, m, n, k, row_width, offset in CASES:\r\n        storage = torch.randn(m, row_width, dtype=torch.bfloat16, device=\"cuda\")\r\n        x = storage[:, offset : offset + k]\r\n        weight = torch.randn(n, k, dtype=torch.bfloat16, device=\"cuda\")\r\n\r\n        reference = F.linear(x, weight)\r\n        output = low_latency_gemm.try_low_latency_gemm(x, weight)\r\n        assert output is not None\r\n        torch.testing.assert_close(output, reference)\r\n\r\n        nvjet_us = bench(lambda x=x, weight=weight: F.linear(x, weight))\r\n        dsv3_us = bench(\r\n            lambda x=x, weight=weight: low_latency_gemm.try_low_latency_gemm(x, weight)\r\n        )\r\n        reduction = 100 * (nvjet_us - dsv3_us) / nvjet_us\r\n        print(\r\n            f\"| {name} | [{m},{n},{k}] | {x.stride()} | {nvjet_us:.3f} | \"\r\n            f\"{dsv3_us:.3f} | {reduction:.1f}% |\"\r\n        )\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()\r\n```\r\n\r\nAnd we can get\r\n\r\n```bash\r\nGPU: NVIDIA B300 SXM6 AC\r\n| Case | [M,N,K] | x.stride() | NVJet (us) | DSV3 (us) | Reduction |\r\n|---|---:|---:|---:|---:|---:|\r\n| B1 KDA f_b_proj | [1,1536,128] | (6288, 1) | 2.129 | 1.116 | 47.6% |\r\n| B4 KDA f_b_proj | [4,1536,128] | (6288, 1) | 3.785 | 2.209 | 41.6% |\r\n| B16 KDA f_b_proj | [16,1536,128] | (6288, 1) | 2.827 | 2.472 | 12.6% |\r\n| B1 MLA q_b_proj | [1,2304,1536] | (2112, 1) | 20.357 | 3.845 | 81.1% |\r\n```"
    },
    {
      "sha": "0e3ac4907d21e77cb4781338c49fef17bfea8f2b",
      "pr_number": 54989,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54989",
      "pr_title": "[ROCm][CI] Fix false multi-node detection on native CI",
      "pr_body": "Native AMD CI on [build 86848](https://buildkite.com/vllm/ci/builds/86848/canvas?jid=01a0624a-61b6-471a-97f6-c1cb2ca6a3e0&tab=output) (`amd-entrypoints-integration-api-server`) exited immediately with `Native CI does not support multi-node jobs yet.` after [#52344](https://github.com/vllm-project/vllm/pull/52344) added an `if [ \"$BUILDKITE_PARALLEL_JOB\" = \"1\" ]` guard. `is_multi_node()` treated that bash `[ ... ]` test as the `[node0] && [node1]` multi-node syntax. This tightens the fallback to the real `] && [` delimiter so sharded single-node jobs still run."
    },
    {
      "sha": "1945a9457563e28a32baa03dca620ed52bd18d10",
      "pr_number": 54996,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54996",
      "pr_title": "[Bugfix][Tests] Stabilize B12X linear kernel checks",
      "pr_body": "## Summary\n\n- Allow the expected normalized error from B12X four-way split-K while tightening the cosine-similarity requirement.\n- Make the B12X W4A16 fallback test independent of the host GPU and unrelated NVFP4 backend registrations.\n\n## Rationale\n\nB12X block-FP8 split-K CTAs atomically accumulate into a BF16 output. Atomic ordering is nondeterministic, so individual outputs can alternate between adjacent BF16 values one ULP apart. On the 48-SM four-slice route, the normalized mean absolute difference was `0.00240`-`0.00252`, while cosine similarity remained `0.9999935`-`0.9999940` across ten seeds. This changes the normalized-error limit from `0.002` to `0.003` and tightens the cosine floor from `0.9999` to `0.99999`.\n\nThe W4A16 fallback test previously depended on the complete CUDA NVFP4 registry. #53014 registered `FlashInferCuteDslNvFp4W4A16LinearKernel` ahead of Marlin, so SM12x CI selected FlashInfer even though the test had explicitly made Marlin available. The test now installs a minimal B12X/Marlin registry with `monkeypatch`, directly exercising its intended contract. Future backend registrations therefore cannot change its result; adding W4A16 support to the B12X linear kernel will still invalidate the fallback test as intended.\n\n## Duplicate-work check\n\nI searched open PRs for B12X block-FP8 tolerance and W4A16 fallback changes. #43929 concerns NVFP4 MoE W4A16 routing and does not modify these tests or the dense linear selector. No open PR addresses these two CI failures.\n\n## Validation\n\n```text\nuvx --from pre-commit pre-commit run --files \\\n  tests/kernels/quantization/test_block_fp8.py \\\n  tests/model_executor/kernels/test_b12x_linear.py\n# all hooks passed\n\nCUDA_VISIBLE_DEVICES=11 .venv/bin/python -m pytest -v -s \\\n  tests/model_executor/kernels/test_b12x_linear.py::test_b12x_backend_preserves_w4a16_fallback \\\n  tests/kernels/quantization/test_block_fp8.py::test_w8a8_block_fp8_b12x_matmul\n# 5 passed\n```\n\nThe tests used `b12x==1.3.0` and source at `605c3ddcba`. The precompiled extension carrier was from `3b45d053b4` because the wheel for the newly published main SHA was not yet available; the two intervening commits only add documentation annotations and update the CUTLASS source revision. These test paths use Python selection logic and the separately installed B12X kernel.\n\nModel evaluation is not applicable because this changes test acceptance and isolation only; runtime code is unchanged.\n\n## AI assistance\n\nAI assistance was used for failure investigation, implementation, validation, and PR drafting. The submitter reviewed every changed line and the validation results.\n"
    },
    {
      "sha": "9b38e3ad53967856df6dd18dbaf91f8f5e3a5f32",
      "pr_number": 54954,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54954",
      "pr_title": "[CI][MoE] Moe kernels test cleanup",
      "pr_body": "## Purpose\r\n\r\nTwo independent changes to the MoE kernels tests.\r\n\r\n**1. Drop the per-test global cleanup in `tests/kernels/moe/`.** A full\r\n`gc.collect()` plus an emptied caching allocator runs after each of the ~1450\r\nsmall kernel tests per shard, which costs more than the tests themselves. This\r\noverrides `should_do_global_cleanup_after_test` for the directory, the way\r\n`tests/conftest.py` suggests for subdirectories, and gives the few tests that\r\ndepend on isolation a clean state on entry instead.\r\n\r\n**2. Enable the `fused_topk` router tests on ROCm.** They were gated on\r\n`current_platform.is_cuda()`, which is False on ROCm, so all 720 of them were\r\nskipped on AMD. They pass on MI300. `test_topk_softplus_sqrt.py` in the same\r\ndirectory already uses `is_cuda_alike()` for the same purpose.\r\n\r\n## Test Plan\r\n\r\nBoth pytest invocations of the group, for every shard, on MI300X with one GPU\r\nvisible, exactly as CI runs them:\r\n\r\n```bash\r\npytest -v -s kernels/moe --ignore=kernels/moe/test_modular_oai_triton_moe.py \\\r\n    --shard-id=$N --num-shards=5\r\npytest -v -s kernels/moe/test_modular_oai_triton_moe.py \\\r\n    --shard-id=$N --num-shards=5\r\n```\r\n\r\n## Test Result\r\n\r\nAll five shards pass with no failures, in two independent runs. Against `main`,\r\n`passed` rises by exactly 720 and `skipped` falls by exactly 720 (4358 to 5078,\r\n3116 to 2396), which is the second change enabling those tests and nothing else.\r\n\r\nWall-clock numbers for a whole shard are not quoted, because the machine\r\navailable to me is shared and they drift with the load of other tenants. A\r\nback-to-back single-file comparison is meaningful: `test_topk_softplus_sqrt.py`,\r\n317 tests, identical outcome, 78.88s with the per-test cleanup and 3.74s\r\nwithout it.\r\n\r\nThe cleanup change is not platform-gated, so this PR's own `MoE Kernels` run on\r\nL4 exercises it. The list of files cleaned on entry was derived on ROCm, where a\r\ndifferent subset of tests executes, so ten files that execute no test on ROCm\r\nare cleaned on entry as a precaution.\r\n\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "3e9d364ff727f002680d242ea76e8a0bbdc0163c",
      "pr_number": 54984,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54984",
      "pr_title": "[CI/Build][ROCm] Guard the two CUDA-only tests in test_bf16_skinny_gemm",
      "pr_body": "## Purpose\r\n\r\nThe `(MI355) Miscellaneous Kernels` job fails on two tests in\r\n`tests/kernels/test_bf16_skinny_gemm.py`. Neither failure indicates a bug in\r\nthe code under test. Both are unstated CUDA assumptions inside the tests\r\nthemselves.\r\n\r\nThat job is added by [#50519](https://github.com/vllm-project/vllm/pull/50519), which extends ROCm CI coverage to the portable\r\nparts of upstream CUDA test groups.\r\n\r\nFailing run: https://buildkite.com/vllm/amd-ci/builds/12548\r\n\r\n## test_kda_qkvg_autotune_enables_full_overlap\r\n\r\nThe test body does a bare `import flashinfer.gemm`. FlashInfer is an optional\r\ndependency, so the test errors instead of skipping whenever it is absent. That\r\nis always the case on ROCm, and it is possible on CUDA too.\r\n\r\nThe test is FlashInfer-only by construction. `autotune_kda_qkvg` does\r\n`from flashinfer.gemm import mm_bf16` itself, and the assertion pins the\r\n`\"cute-dsl\"` backend. Replaced with `pytest.importorskip`, matching the\r\nexisting idiom in `tests/kernels/attention/test_mha_attn.py` and\r\n`tests/kernels/moe/test_marlin_vs_trtllm_mxint4.py`.\r\n\r\n## test_fallback_preserves_default_method\r\n\r\nThe test builds CPU tensors so that `_runtime_ok` fails and the mixin defers to\r\n`UnquantizedLinearMethod`, then asserts the output equals `F.linear`.\r\n\r\nThat base method is platform dispatched in `dispatch_unquantized_gemm`. On CUDA\r\nit resolves to `default_unquantized_gemm`, a plain `F.linear` that is happy\r\nwith CPU tensors. On ROCm it resolves to `rocm_unquantized_gemm`, which calls\r\nthe `vllm::rocm_unquantized_gemm` custom op. That op is registered only for the\r\nCUDA and Meta dispatch keys, so CPU tensors raise `NotImplementedError`.\r\n\r\nThe premise is CUDA-specific, so the test is gated on\r\n`current_platform.is_cuda()` and its body is unchanged. The method under test\r\nlives in `vllm/models/kimi_k3/nvidia/` and is not installed on ROCm, so gating\r\ncosts no real coverage.\r\n\r\n## Test Plan\r\n\r\n`pytest tests/kernels/test_bf16_skinny_gemm.py` on MI355X (gfx950).\r\n\r\n## Test Result\r\n\r\nBefore: 2 failed. After: 125 passed, 265 skipped, with both tests reporting\r\ntheir skip reason.\r\n\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "2691c6cc535692d173986e93384c84970a0fe009",
      "pr_number": 54957,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54957",
      "pr_title": "[Bugfix][CI] Set cudagraph_mode=FULL for the Ernie4.5-VL ViT cudagraph test",
      "pr_body": "Ernie4.5-VL ViT cudagraph test fails after #54782\r\n\r\nFailing test: `test_vit_cudagraph_image[ernie45_vl]`\r\n\r\nAMD CI: <https://buildkite.com/vllm/amd-ci/builds/12553/list?sid=01a0615a-019d-4492-a92c-a4018dfdcc8b&tab=output>\r\n\r\n**The same group failed on both MI300 and MI355.**\r\n\r\n## Error\r\n\r\n```\r\nRuntimeError: Ernie4_5_VLMoeForConditionalGeneration: piecewise CUDA graphs\r\n(cudagraph_mode=FULL_AND_PIECEWISE) unavailable, model is not torch-compiled\r\nand breakable CUDA graph is off. Set VLLM_USE_BREAKABLE_CUDAGRAPH=1 or\r\ncudagraph_mode=NONE/FULL.\r\n```\r\n\r\nRaised in `vllm/v1/worker/gpu/cudagraph_utils.py`, during engine start.\r\n\r\n## What exposed it\r\n\r\n`1c26e57d3c` — [Bugfix] Raise for unavailable piecewise CUDA graphs (#54782).\r\nIt added a guard that raises when piecewise cudagraphs are requested for a\r\nmodel with no active `@support_torch_compile` wrapper.\r\n\r\n## Root cause\r\n\r\nThe guard is correct; the misconfiguration predates it. `Ernie4_5_VLMoeModel`\r\nkeeps its `@support_torch_compile` decorator commented out, since the model\r\nwas added in #22514:\r\n\r\n```python\r\n# Since Ernie VL distinguishes between text experts and vision experts,\r\n# enabling torch.compile will cause errors.\r\n# @support_torch_compile(\r\n```\r\n\r\nSo the model is not compiled, piecewise cudagraphs have nothing to partition,\r\nand the test asks for the default `FULL_AND_PIECEWISE`. Until #54782 this\r\nsilently captured nothing.\r\n\r\nNot platform-specific: `VLLM_USE_BREAKABLE_CUDAGRAPH` defaults to `False`\r\neverywhere and the test is gated on `is_cuda_alike()`, so CUDA should hit it\r\ntoo.\r\n\r\nErnie is the only affected model in that file — `glm4_1v` and `step3_vl` pass,\r\nand `deepseek_ocr` already pins `cudagraph_mode`.\r\n\r\n## Fix\r\n\r\nPin the mode for the Ernie entry, the way `deepseek_ocr` in the same file\r\nalready does:\r\n\r\n```python\r\ncompilation_config_overrides={\r\n    \"cudagraph_mode\": 2,  # FULL\r\n},\r\n```\r\n\r\nVerified on MI300X: fails in 31s before, passes in 40s after.\r\n\r\n\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "bf7a14d307bb495afc8c2587343bdc7d88c0f135",
      "pr_number": 54852,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54852",
      "pr_title": "[CI][ROCm] Add DSpark evals",
      "pr_body": "Stacks on #54817 and adds MI355 ROCm/AITER DSpark eval coverage for Kimi K3 and DeepSeek V4.\n\nThis complements rather than duplicates draft #50519 by using the current lm_eval mirror, with DeepSeek on the required V2 runner and explicit AITER MoE path.\n\nFull 1,319-question GSM8K runs passed on 4x MI355: Kimi accuracy 0.3283 with 5.008 mean acceptance, and DeepSeek accuracy 0.9431 with 4.455 mean acceptance.\n\nPre-commit, YAML/config validation, and pytest collection passed; this PR was prepared with OpenAI Codex assistance and reviewed by the submitter."
    },
    {
      "sha": "1356635d837c4ef002ec98c1a0296e7ff60be3c1",
      "pr_number": 54566,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54566",
      "pr_title": "[New model][Multimodal] Add DeepSeek-V4-Flash-Vision-Exp support",
      "pr_body": "\r\n\r\n\r\n## Purpose\r\n\r\nWe have provided the docker image for this model, please refer to https://recipes.vllm.ai/deepseek-ai/DeepSeek-V4-Flash-Vision-Exp\r\n\r\n\r\n## Test Plan\r\nSetup: 4x NVIDIA GB200 (TP=4, expert parallel), fp8 KV cache, block size 256,\r\n\r\n```bash\r\nexport VLLM_FLASHINFER_AUTOTUNE_SKIP_OPS=\"trtllm_fp4_block_scale_moe,flashinfer::trtllm_fp4_block_scale_moe\"\r\n\r\nvllm serve deepseek-ai/DeepSeek-V4-Flash-Vision-Exp \\\r\n    --tensor-parallel-size 4 \\\r\n    --enable-expert-parallel \\\r\n    --kv-cache-dtype fp8 \\\r\n    --block-size 256 \\\r\n    --max-model-len 32768 \\\r\n    --speculative-config '{\"method\":\"dspark\",\"model\":\"deepseek-ai/DeepSeek-V4-Flash-Vision-Exp\",\"num_speculative_tokens\":3,\"draft_sample_method\":\"probabilistic\",\"enable_adaptive_verification\":true}' \\\r\n    --allowed-local-media-path /mnt/lustre \\\r\n    --port 8271\r\n```\r\n\r\n\r\n## Test Result\r\n**OCRBench (full 1000 samples)**\r\n\r\n| Metric | Value |\r\n|---|---|\r\n| **Final Score** | **835 / 1000 (83.5%)** |\r\n| Request errors | 0 |\r\n| Eval duration | 312.6 s (~3.2 samples/s) |\r\n\r\n| Category | Correct / Total | Accuracy |\r\n|---|---|---|\r\n| Regular Text Recognition | 47 / 50 | 94.0% |\r\n| Irregular Text Recognition | 44 / 50 | 88.0% |\r\n| Artistic Text Recognition | 47 / 50 | 94.0% |\r\n| Digit String Recognition | 30 / 50 | 60.0% |\r\n| Non-Semantic Text Recognition | 45 / 50 | 90.0% |\r\n| Handwriting Recognition | 29 / 50 | 58.0% |\r\n| **Text Recognition (subtotal)** | **242 / 300** | **80.7%** |\r\n| Scene Text-centric VQA | 179 / 200 | 89.5% |\r\n| Doc-oriented VQA | 180 / 200 | 90.0% |\r\n| Key Information Extraction | 185 / 200 | 92.5% |\r\n| Handwritten Mathematical Expression Recognition | 49 / 100 | 49.0% |\r\n\r\n## DSpark (MTP) acceptance — aggregated over the full OCRBench run\r\n\r\n| Metric | Value |\r\n|---|---|\r\n| Mean acceptance length (tokens/forward) | **2.99** (interval range 2.28–3.40) |\r\n| Overall draft acceptance rate | **66.3%** (266,788 accepted / 402,660 drafted) |\r\n| Per-position acceptance rate (mean) | 83.9% / 66.5% / 50.7% (draft pos 1/2/3) |\r\n\r\nNotes:\r\n\r\n- Acceptance is measured over mixed text+image traffic; image tokens in the\r\n  prompt do not degrade draft acceptance (the drafter reads image content via\r\n  the target's hidden states).\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [ ] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [ ] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n\r\n"
    },
    {
      "sha": "488e6fd53cb29b4255364b69a4d298b1bbc26062",
      "pr_number": 54991,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54991",
      "pr_title": "[CI] Revert flaky `test_quark_int8_w8a8_moe`",
      "pr_body": "## Purpose\r\n\r\nThis test is flaky as per\r\n- https://github.com/vllm-project/vllm/pull/53291#issuecomment-5484346261\r\n- https://buildkite.com/vllm/amd-ci/builds/12559/list?sid=01a062bb-c3b2-4f97-b5d1-5d02b8dca073&tab=output\r\n\r\nfrom the change in https://github.com/vllm-project/vllm/pull/53291.\r\n\r\nReverting to the previous test definition that was not flaky.\r\n\r\n## Test Plan\r\n\r\n`pytest tests/quantization/test_quark.py -s -vvvvv -k \"test_quark_int8_w8a8_moe\"\r\n\r\n## Test Result\r\n\r\nTBD"
    },
    {
      "sha": "605c3ddcbaaee06a2687a961ae7be51aec5dddc0",
      "pr_number": 54190,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54190",
      "pr_title": "[BUILD] Bump cutlass to v4.7.1",
      "pr_body": "## Purpose\r\n\r\nInspired by https://github.com/vllm-project/vllm/pull/43814#issuecomment-5433162795, we need to have newer cutlass for C extensions.\r\n\r\n## Test Plan\r\n\r\nWill be tested by CI.\r\n\r\n## Test Result\r\n\r\n* CI pipeline:\r\n  * https://buildkite.com/vllm/ci/builds/85976\r\n  * https://buildkite.com/vllm/ci/builds/86820 (retry of failed jobs)\r\n* Release pipeline: https://buildkite.com/vllm/release-v2/builds/5740\r\n\r\n---\r\n<details>\r\n<summary> Essential Elements of an Effective PR Description Checklist </summary>\r\n\r\n- [x] The purpose of the PR, such as \"Fix some issue (link existing issues this PR will resolve)\".\r\n- [x] The test plan, such as providing test command.\r\n- [ ] The test results, such as pasting the results comparison before and after, or e2e results\r\n- [ ] (Optional) The necessary documentation update, such as updating `supported_models.md` and `examples` for a new model.\r\n</details>\r\n"
    },
    {
      "sha": "d539de1c5d1a120ba3281d62c3bcb0e19a36348d",
      "pr_number": 54980,
      "pr_url": "https://github.com/vllm-project/vllm/pull/54980",
      "pr_title": "[Docs] Add missing return annotations flagged by griffe",
      "pr_body": "## Purpose\n\nThe strict docs build emits warnings like:\n\n```\nWARNING - griffe: vllm/model_executor/determinism/batch_invariant.py:233: No type or annotation for returned value 1\n```\n\nThese come from functions that document a `Returns:` section but have no return annotation, so `griffe` has nothing to render as the return type.\n\nI scanned the whole `vllm` package with griffe's Google-style docstring parser to find every function that triggers this warning. There were five:\n\n- `rocm_aiter_ops.get_aiter_activation_type`\n- `rocm_aiter_ops.get_aiter_quant_type`\n- `cutlass_w4a8_moe_mm`\n- `onednn_scaled_int8_quant`\n- `matmul_descriptor_persistent`\n\nEach now carries the return annotation implied by its body. `aiter`'s `ActivationType`/`QuantType` are imported under `TYPE_CHECKING` since `aiter` is an optional ROCm dependency.\n\nWhile there, the two `scaled_int8_quant` docstrings had the return type written inline in the `Returns:` body (`tuple[...] : Output int8 tensor, ...`), which duplicates the signature and renders poorly. Now that the signature carries the type in both cases, the prefix is removed.\n\nNot duplicating existing work: `gh pr list --repo vllm-project/vllm --state open --search \"griffe\"` returns only #51342, which annotates an unrelated parameter in `benchmarks/`. No open PR touches these functions.\n\n## Test Plan\n\nNo runtime behaviour changes: only annotations and docstring text.\n\n```bash\npre-commit run --files vllm/_aiter_ops.py vllm/_custom_ops.py \\\n    vllm/model_executor/determinism/batch_invariant.py\n```\n\nPlus a re-run of the griffe scan used to find the offenders, to confirm no function in `vllm/` still triggers the warning.\n\n## Test Result\n\n`ruff check`, `ruff format` and `mypy` all pass. The griffe scan now reports zero occurrences of \"No type or annotation for returned value\" across `vllm/`.\n\nNo model evaluation is included because the change cannot affect model output, accuracy, or serving.\n\nAI assistance was used to produce this change; I have reviewed every changed line.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01SKb6NYstLaq8VNv6kUod9P"
    }
  ]
}